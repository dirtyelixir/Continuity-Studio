## Studio warms local Qwen through the VRAM Manager before retrying - live (2026-09-17)

One image job makes three provider calls in order: the image-prompt preparer and the frame moment check both use Qwen, then the render uses ComfyUI. The VRAM Manager unloads Qwen whenever ComfyUI takes the card, so Qwen is typically not resident when that first Qwen call is made, and Studio deliberately sends `X-VRAM-Wait-Seconds: 0` for local_qwen so an unloaded or still-loading model is refused at once with 409/503. `providers.http_run` turned that straight into a terminal job error, so a job's first Qwen step failed in well under a second and the whole job had to be retried. Measured 2026-09-17: 4 of 4 back-to-back image submissions failed this way, then 6 of 6 succeeded once each submission was preceded by a manual warm. This was a lifecycle race rather than a fault - non-blocking first attempts are the manager's documented contract and loading Qwen is explicitly the manager's job.

`providers.warm_local_qwen(work, timeout)` now POSTs the manager's existing `/vram/qwen/warm` with the bounded `QWEN_WARM_TIMEOUT=240`, writes an auditable receipt to `<work>/qwen-warm.json`, and reports whether the manager became ready; it never raises and never substitutes a provider. A 409/503 from local_qwen now warms once and retries the SAME provider with the SAME payload exactly once, then re-raises the original honest error if still refused - no substitution, downgrade, fallback or loop. **The shared manager was not modified**, so toonflow-app, vramctl, comfy-vram-safe.sh and qwen-start.sh are unaffected; the larger job-scoped lease protocol was explicitly deferred.

48 focused cases pass, including three new ones: a 503-then-200 sequence makes exactly one warm call and exactly two chat attempts with the fallback guard never invoked; a refused warm makes one warm attempt and at most two chat attempts before the honest error; and a non-Qwen HTTP provider makes no warm call and writes no receipt. `test_qwen_failure_never_falls_back` still passes for all four of its cases and `X-VRAM-Wait-Seconds` still stays 0. Verified against the real manager rather than only a mock: `POST /vram/release-idle` dropped GPU use from 25,548,771,328 to 1,185,746,944 bytes with `qwen_active false`, and `warm_local_qwen()` then returned ready in a genuine 9.1 seconds with `qwen_active true` (an earlier call returned in 0.0s only because Qwen was already resident, which is why the release-then-warm sequence was run). Full suite 3 failed / 1103 passed - the same three pre-existing failures. Restart 2532257 -> 2572134 verified idle first; health 200, both project APIs 200, SQLite integrity ok, served static parity holds. No image job was re-run end to end inside a real pipeline, and no media was generated. Evidence: `data/acceptance/studio-qwen-warm-20260917/verification.json`.

## Canon OFF state made as explicit as ON - live, revision 17 (2026-09-17)

The lantern's off state was factually correct but under-specified where it mattered. `power='Off; unlit.'` and `toggle_position='Down.'` were right, yet the canonical-shot-state rules assign lighting instructions to compositions, and both off-lantern compositions described every light source except the lantern's own globe - they spoke of the rear-left amber sconce raking the brass base and giving the toggle a hard black silhouette against illuminated brass, and never said the lantern emits no light. Meanwhile the ON value read "On; steady warm amber light." against a bare OFF value, and the compiled prompt repeated warm-light language (amber x2, light x9, illuminated brass) plus the style block's "Warm amber practical illumination" against a single trailing negation. So the local renderer filled the gap rather than disobeying an explicit instruction; the earlier "provider did not obey" reading was incomplete.

Revision 16 -> 17 makes the OFF state as physical as the ON state while leaving the semantics untouched: the power fact became "Off; unlit - dark glass chimney, no filament glow; it emits no light of its own." across all 14 moments of shot_01 and shot_02 plus the shot_02 transition[2.6-3.2] boundary value, and each off-lantern composition gained one clause stating the lantern's own glass chimney stays dark with no filament glow, emitting no light, with the rear-left sconce and blue window light as the only sources. The compiled brief for both frames now carries all four phrases. The first submission was correctly rejected (HTTP 400, "Canonical transition prop_lantern.power at 2.6-3.2s contradicts its boundary state") because the moment states were updated without the transition boundary; a local boundary-consistency check was added and the corrected payload reports 0 problems. No revision was created by the rejected attempt.

Consequence, which is the pipeline working as designed: the revision bump changed the source hash, so the scene and every board candidate - including adopted board `5e5294438fcc4a4f` - are now stale, and 補齊缺少圖片 is blocked until the board is re-planned and re-adopted. The shot_01/shot_02 frame candidates `a9e4639c37514d41` and `3c3ad41480b6457c` are stale and must be re-rendered from the corrected brief; the shot_03 frames `8e5bca6f17a54fab` and `06e352f9aee9497b` remain pending and current because shot_03's canon did not change.

Cleanup (user-authorized): six redundant candidates were removed - the square 1024x1024 takes `ef27feaaee96455c`, `3d7f1d2bbe534e38`, `3b1264118aaa43e9` and `564ff47f91b341fe` superseded by 1376x768 versions, the stray `723d4e8621114552` created by a parent script bug, and the corrupt brown-noise render `f65052600d644e55`. All six files are confirmed absent from disk and all six rows absent from the assets table (project assets 54 -> 48, exactly -6; nothing else affected). No wrong-aspect candidate remains reviewable; historical stale and superseded versions stay as history per the asset-version contract. Evidence: `data/acceptance/canon-off-explicit-20260917/verification.json`.

All four board frames now carry the correct 1376x768 canvas. `frame_03_start` (`8e5bca6f17a54fab`) and `frame_03_end` (`06e352f9aee9497b`) also inspect as canon-correct: two characters, Ada's hands on the bench then her right palm against the side of Pip's head with the badge straight and the lantern lit and left of centre at 12s. `frame_01_start` (`a9e4639c37514d41`) and `frame_02_start` (`3c3ad41480b6457c`) are correct in aspect and pass the gate but render the lantern LIT against canonical `power='Off; unlit'`. That non-adherence is reproducible: three non-corrupt attempts across the two frames all produced a lit lantern, the compiled render prompt contains exactly one trailing off/unlit sentence while repeating vivid warm-light language (amber x2, light x9, illuminated brass x1), and the approved lantern reference is itself unlit. So the brief is correct and the renderer is not honouring the negation - prompt balance and model adherence, not a missing instruction. No prompt was hand-edited and no provider was substituted; both would be routing/creative decisions. No frame was approved.

## Pictureless image decodes are now rejected - live (2026-09-17)

The deterministic image quality gate rejected only exactly-all-black and exactly-all-white RGB, so a real local Klein render decoded to brown noise with no scene content still reported valid and could reach the review queue and be persisted as an asset. `inspect_image()` now additionally flags the noise signature: absent large-scale structure (an 8x8 box-downsample luminance spread below `STRUCTURE_FLOOR=64`) together with strong local variation (mean adjacent-pixel difference at or above `NOISE_DETAIL=14`). This is deliberately narrower than a contrast test - a uniform swatch and a merely soft frame keep their previous valid verdict - so the module's existing "no creative judgement" contract holds and `test_texture_dark_valid` and `test_uniform_non_black_white_passes` pass unchanged. The probe is skipped below `MIN_STRUCTURE_SIDE=64` because a coarse grid needs an image larger than itself to mean anything; every real render is at least 768px on its short side.

Calibration used real outputs with Pillow only: the corrupt asset `f65052600d644e55` scored spread 18.08 / detail 19.55, while eleven genuine assets (five generated frames, two Astra keyframes, three canon references) scored spread 117.94-174.77 / detail 6.27-11.97 - a 6.5x separation, with the floor inside that gap. Re-inspecting all 51 on-disk assets for The Smallest Fix gives 50 valid and exactly one flagged. The gate is already enforced after every local render (`comfy_images.py:187-192` marks the receipt `invalid_output` and raises before saving) and before any asset is persisted (`engine.py:417` `require_image`), so this class now fails the job with a readable message instead of silently producing an asset. The brown-noise class itself remains intermittent and unclaimed as fixed by this change. Evidence: `data/acceptance/image-noise-gate-20260917/verification.json`.

## Frame still canvas follows the video aspect - live, with regeneration findings (2026-09-17)

`studio/local_images.py` sized every non-character still at 1024x1024, so a board frame that conditions a 16:9 shot was rendered square - contradicting the brief's declared "Landscape 16:9" and the render contract's own "Preserve the specified visual medium and aspect ratio" rule, and mismatching both the accepted keyframe convention (1672x941) and the video canvas (864x480). `kind=='frame'` now takes its canvas from the same `h3_render_settings.dimensions()` helper the video path uses, at the unchanged 1 MP still budget, so the ratio has a single authority. Character, prop and location canvases are untouched. 19 focused cases pass, including new tests that a frame canvas follows the video aspect and stays 32-aligned across auto/reference/portrait while entity canvases do not move. Live plans now resolve to 1376x768 for all four board frames. Restart 2382791 -> 2515054 verified idle first; health 200 and SQLite integrity ok.

Real regeneration of the two frames whose lantern was lit produced two honest negatives. `3c3ad41480b6457c` (frame_02_start) has the correct aspect, scene and characters with the robot reaching for the toggle, but the lantern is still LIT against canonical `power: Off; unlit`; the compiled render prompt already states "The lantern itself is off and unlit." and the approved lantern reference is itself unlit, so this is provider non-adherence rather than a missing instruction. `f65052600d644e55` (frame_01_start) is a corrupt brown-noise render - an 8x8 downsample is near-uniform and adjacent-pixel contrast is 1.08 - yet `image_output_quality.inspect_image` reports valid, because that gate rejects only exactly-all-black and exactly-all-white pixels. Brown-noise corruption therefore passes as valid; the same class the klein launcher's own notes describe as intermittent. The two shot-03 frames still carry the old square canvas. No frame was approved.

GPU launcher: the user authorized making both modes keep the local-tab-guard front-end. `launch_h3.sh.opt-in-klein` gained the two front-end flags and the user's current launcher was promoted to `launch_h3.sh.v18.32.bak`, because the VRAM manager refuses to switch modes while `launch_h3.sh` differs from that baseline (`global_vram_manager.py:557-562`) - and both `mode_klein()` and `mode_h3()` would otherwise overwrite the user's edit. Old files are kept as `*.before-frontend-20260917-021620`. `bash -n` passed on both, the manager returned `{ok, mode:klein, changed:true, ready:true}`, and the running ComfyUI verifies `--disable-smart-memory` plus both front-end flags. Operational finding: each image job runs a Qwen moment check before its Klein render while the manager unloads Qwen for ComfyUI, so a job enqueued straight after a render fails instantly with a 503; pre-warming the Qwen gateway before each enqueue succeeded 6/6 times. Evidence: `data/acceptance/frame-canvas-aspect-20260917/verification.json`.

## Storyboard deadlock exit, provider-schema tolerance and output-budget staging — live (2026-09-17)

The Smallest Fix is unblocked and live: after a user-authorized preflight, `shot_state.check_artifact()` returned PASS for board `5e5294438fcc4a4f` (real local_qwen call via the VRAM Manager), so `POST .../storyboard/adopt/5e5294438fcc4a4f` succeeded (HTTP 200) against the cached receipt. Production revision 15 -> 16, board revision 30 -> 31, adopted job `c7f26e0dc1864510` -> `5e5294438fcc4a4f`, revisions rows 23 -> 24, and the false `frame_01_start` gaze constraint is gone (`unresolved_constraints: []`). `補齊缺少圖片` is now enabled with `generate_count` 4 (The diagnosis 0s, The switch 0s, Her kind of thank-you 0s and 12.0s). The 4 source images were deliberately NOT generated. Two further UI/staleness refinements followed: the scene status line no longer shows 安排失敗 for an adopted, still-fresh board (the progress slot now prefers an in-flight attempt, else the adopted attempt while the board is fresh, else the newest attempt), and the live page shows 還欠 4 格畫面 with 圖板安排已採用 and zero JS errors. Restart 2363814 -> 2382791 passed health, both project APIs and SQLite integrity.

A failed board retry no longer hides an earlier usable plan. `storyboard_board.state()` exposes every attempt (`candidates`) with real id/state/error/created plus stale/adopted/usable, and an in-flight attempt owns the progress slot instead of a newer terminal record. A queued/running/awaiting_input attempt takes precedence; otherwise the newest attempt is shown. The board UI lists earlier usable plans as 檢視較早方案 entries and resolves any attempt by id through `/api/jobs/{id}` rather than `scene.job`; blocked-board wording now names a real next step and distinguishes the case where an earlier usable plan exists. Live effect: The Smallest Fix showed only the newest failed attempt while `5e5294438fcc4a4f` and `4f426d086e61409f` were valid and within freshness.

Two validators were rejecting correct generation. Evidence excerpts may now join two real passages with an ellipsis, or differ only by whitespace runs, while every fragment must still be verbatim — paraphrase, rewording and invented text stay rejected. Real job `f1e97d4252614e8d` (local_qwen, `finish_reason=stop`, 1863 completion tokens, correct panels/modes/anchors) failed only because the provider joined two source sentences with `...`; it now replays to edit_01 I2VA / edit_02 I2VA / edit_03 FL2VA with zero unresolved constraints. Anchor/Panel/BoardPlan now inherit `ProviderPlan` (`extra='ignore'`) so an unknown informational key cannot discard a real plan; the requested schema still sends `additionalProperties:false` and semantic checks are unchanged. Real jobs `6f774a60e689415f` (`planning_notes_count`) and `6933dccdbcaf4ece` (`reuse_frame_note`) now validate. Four valid plans are reachable that were previously discarded.

Directing reviews now stage on output-budget risk, not only oversized input. `answer_items()` counts the answer lines a review must report (beats + edit entries + chapters); `output_risk()` compares `(items+1)*OUTPUT_ANSWER_LINE_UNITS` against `max_output_tokens//OUTPUT_BUDGET_SHARE` (3000 units/line, half the budget) because a reasoning model may spend the remainder before any answer. Critically, `prepare()` no longer collapses back to one call when the input fits a very large window like DeepSeek's 1,048,576: the monolithic branch additionally requires `not output_risk(...)`. Without that guard the trigger alone was inert. Real acceptance: all 8 real failed directing jobs would now take the staged path, including `2026-09-13T17:40:27` (DeepSeek, The Smallest Fix, 7 answer items, previously monolithic and output-exhausted) and `06:52:09` (下一站：長洲, 94 items). A genuinely small review still keeps its original single request.

18 directing-pipeline cases pass, including a regression that output risk cannot collapse back to monolithic and that an unfit atomic unit still fails with 最小必要. Full suite: 3 failed, 1087 passed — the same three pre-existing failures (`test_image_loras` ×2, whose fixture writes an all-white image rejected by the untouched `image_output_quality` gate, and `test_montage_methods`), reproduced unchanged on the pre-change tree. Node checks 63 pass, 1 pre-existing failure (`check_guidance_start.mjs`, `renderGenerationGroups is not defined`). Idle status polling now reads a project every 4 s only while work is queued/running/awaiting_input or the local runtime phase is not ready, and every 30 s when idle, with a failed read retrying promptly; measured live at 2 reads per 40 s with the project open and idle (previously ~10, at 2.07 MB per read). Graceful restart 2082 → 2326620 preserved all 13 durable table digests, SQLite integrity and both project APIs; no production generation, adoption, provider or GPU change. Evidence: `data/acceptance/storyboard-deadlock-20260916/verification.json`; backups `data/backups/deadlock-fix-20260916-214929` and `data/backups/deadlock-fix-restart-20260917-005133`.

## Frame detail layout and proposal-validation progress — live (2026-09-14)

Video/shot frame descriptions now use a closed disclosure; full canonical prose remains available while image actions stay visible. Page/project/frame-scoped open state survives refresh. Non-pipeline narrative/storyboard jobs expose read-only saved-output and canonical-check stages through both project and job APIs, with latest-attempt per-Shot counts and safe partial-JSON reads. Existing pipeline progress retains priority. Review and image-demand planning rows name their actual stage.

9 Python cases and three Node UI checks pass. Browser verifies collapse/expand, refresh preservation and directly accessible image controls. Idle ./run.sh activation PID 57739 preserves all 12 durable table digests and SQLite integrity; both projects and static parity pass. No production submission, adoption or provider change. Original 4d3affa1e82d418a is succeeded (concurrent endpoint recovery); downstream DeepSeek review 2ad97eca057f452b failed at the 32,768-token output limit before a formal answer. That review remains unresolved, and the candidate's revision-14 freshness boundary remains intact against current revision 15. Evidence: reviews/proposal-progress-20260914/REPORT.md and verification.json.

## Canonical shot state — architectural fix and live source conversion (2026-09-14)

`Shot.canonical_state` now owns dynamic entity facts and transitions. A deterministic compiler derives all endpoint/frame states and descriptions from exact moments and per-frame composition. Production's single writer and proposal completion share structural and independent semantic preflight before revision writes; prose-only edits cannot create a second authority. Restoration, chapter localization, editorial/board/endpoint paths preserve the same boundary and bind receipts to actual compiled output. The canonical editor exposes timeline/composition and frame IDs/times; FRAME_MOMENT_SYNC remains only a downstream safety net.

Real Astra positive and contradictory-beat negative checks passed; all three The Smallest Fix shots passed independent actual semantic checks. The live source is revision 15: Pip looks at Ada at 0s and through 6.2s, then turns toward the toggle, preserving I2VA one source start frame and one continuous source. All 44 media files are byte-identical; five old approved frame assets are stale for renewed review. No media generation was submitted. An actual isolated board plan has 1/1/2 required frames in I2VA/I2VA/FL2VA with no semantic conflict. The final live board was not adopted: its independent check rejected a separate camera-timing claim in planning notes. The canonical source itself is consistent; no manual gaze arbitration remains. See reviews/canonical-shot-state/migration/final-live-verification.json.

Broad relevant verification passed 253 cases; final affected outbound/board/preparation/grouping run passed 92 (overlapping counts). Explicit interrupted-check recovery additionally passed 37 canonical cases. Node editor/shots checks and portable schema verification passed. Browser confirms canonical-only editing. Idle activations preserved all 13 durable tables. The final planner wire representation shares identical facts/Shot/canon without omitting source content; exact real-source restoration is verified. See docs/CANONICAL_SHOT_STATE.md and reviews/canonical-shot-state/REPORT.md for acceptance, provider checks, migration and limits.

## Endpoint frame recovery and autonomous empty feedback — live, original proposal recovered (2026-09-14)

The Smallest Fix job 4d3affa1e82d418a is now succeeded. Its retained 1-scene/3-Shot/13-frame Astra candidate failed only the initial endpoint format gate: the first start frame redundantly copied time 0 and its exact Shot.start_state. Owning Shot validation removes only exact copies; wrong/coerced times, conflicting state and standalone/intermediate Frame invariants remain strict. Raw provider JSON remains byte-identical.

First recovery passed Shot 1, then a checker mistook Studio’s empty revision template for missing user intent. The user clarified they expect autonomous generation. Only exact empty Studio-owned wrappers are interpreted as no additional user constraint; authored feedback remains intact. Future blank optional UI feedback sends an explicit autonomous direction. Recovery records running and actual latest failure with prior-error receipts; failed-job detail exposes saved story/script and per-Shot checks.

The second original-job recovery completed in 429.22 seconds using the same frozen Astra and saved candidate; all three canonical extraction/independent checks passed. Automatic directing review 2ad97eca057f452b is running (not yet a directing verdict or adoption). Concurrent Director edit created revision 15 at 17:28:19 UTC; the recovered proposal remains based on revision 14 and must respect normal freshness checks. This task did not adopt or alter current production, and all 44 assets remain. Projects/revisions/assets digests match the second activation snapshot; SQLite integrity is ok.

Initial 84 Python cases and final 81 affected cases pass, plus job-detail rendering and both empty-feedback frontend routes. Local Qwen supplied a partial model edit; parent completed implementation/testing after its budget ended. Idle ./run.sh activation PID 20034 preserved durable hashes, and browser verified the recovered workflow progressed to automatic directing review. Evidence and readable saved draft: reviews/endpoint-frame-recovery-20260914/.

## Adaptive generation arrangements — live (2026-09-14)

## Autonomous Ref2VA demand planning — implemented and verified (2026-09-14)

Supersedes the earlier Ref2VA recommendation-only review below. The source AUTO strategy and image-demand board now share `ConditioningDecision` / `ReferenceDemand`, common mode rules and deterministic validation in `studio/conditioning.py`. New REF2VA sources resolve only required shot-specific approved canon, omit optional demands, deduplicate entity roles, preserve stable Picture/Subject slots, and block missing/unsupported/temporal requirements. Generation-needed-images-v2 has zero timeline anchors for REF2VA and reuses approved canon; v1 and legacy shared delivery remain readable. New source prompts use validated self-contained definitions, then existing freeze/upload/graph/receipt paths. The UI shows roles, reasons, selected assets and verified submission bindings.

Real Astra AUTO job `429ccb6a389143a3` selected REF2VA in an isolated The Smallest Fix canon replay: Ada / lantern / workshop, excluding offscreen Pip despite its presence in shot/canon scope. Board `c906f7de7e6e42fe` retained the three demands with zero new images; prompt `75b72b42f11743f1` used the same slots. H3 take `fc49013b4c0a45d2`, prompt `377f537f-4db3-4882-ac50-848e4ecde842`, completed at 864×480 / 107 frames / 4.459s. Frozen request, uploaded bytes, graph slot indexes, receipt and actual ComfyUI history match. Original The diagnosis AUTO baseline still selected I2VA, not Ref2VA because of its three frames.

174 relevant Python cases across focused runs and seven Node UI suites passed; actual browser preview was checked. Live API is healthy with the contract loaded; 3 projects / 62 assets / 22 revisions / 5 original takes have identical before/after digests. All new acceptance jobs/output remain isolated. Full A–H report, runnable verifier and actual evidence: `reviews/autonomous-ref2va-20260914/REPORT.md` and `evidence.json`. Internal segmentation and exact intermediate timestamp conditioning remain unresolved capabilities, not promises made by Ref2VA.


New non-manual generation plans adapt to frozen provider input/output budgets, including native Qwen tokenizer accounting. Complete source units are partitioned only when needed; every artificial split requires model boundary regrouping. Checkpoints and the complete assembly receipt gate success/adoption; explicit resume preserves the original provider and completed batches. Explicit output-length exhaustion permits subdivision, while transport ambiguity, unavailable tokenizer or an indivisible over-budget unit stops honestly. No source/video segmentation or provider fallback is invented. See docs/GENERATION_PIPELINE.md.

69 relevant Python cases and six Node suites pass. Browser verifies the live compact adopted card and current-work-only panel. Idle activation preserves all 13 durable table digests and SQLite integrity; both project APIs and served static parity pass. No production generation/adoption submitted. Qwen passive props returned 503 (not loaded), so native tokenizer/length/busy routing is covered with controlled protocol tests, not claimed as a real Qwen generation run. Evidence: reviews/generation-adaptive-20260914/verification.json. Concurrent frontend syntax and outdated job-detail contract-version defects were corrected narrowly; other concurrent work is preserved.

## Ref2VA selection review and frozen role evidence — live (2026-09-14)

Source h3_strategy AUTO already permits I2VA/FL2VA/REF2VA recommendations; native grouping can select all three, while independent grouping defers mode. The new ImageNeedPlan still only permits I2VA/FL2VA. VideoStrategy has no per-reference selection; delivery assembles scene-wide identity/environment and shot-specific prop references. Thus the main path is not a complete autonomous Ref2VA selector/compiler. Real evidence: 13 successful source I2VA recommendations, one FL2VA, zero Ref2VA; all five real takes I2VA. Old grouping Ref2VA suggestions based on nonzero trims had empty targets and violate the current contract.

Safe patch clarifies reference-need criteria, preserves role/name/target/subject metadata through frozen sources, and displays role-to-Picture mappings with exact submission evidence in work records. No enum-only fake Ref2VA board branch or unsafe pruning of shared prompt slots. 57 Python cases and three Node suites pass; controlled AUTO Ref2VA → adoption → delivery → mock transport/graph/receipt tests prove routing, not real model selection quality. Full minimal completion plan and A–H review: reviews/ref2va-selection-20260914/REPORT.md.

Idle activation PID 4061055 passes health, both projects and real take detail/static checks. Ten durable tables match the before snapshot; a concurrently created job c7f26e0dc1864510 accounts for new jobs/events/sequence changes. Integrity ok; no provider/media jobs or production adoption submitted by this review.

## Generation planner context compaction — live, real retries succeeded (2026-09-14)

A quiet repair job f4c4da6119c44e3d failed before provider execution: 98,342 byte upper bound exceeded the 97,280 input allowance (131,072 context, 32,768 output, 1,024 safety). Only embedded planning-source JSON formatting is now compacted; all string whitespace, fields, source objects, fingerprints, schemas, methods and provider policy remain intact. Frozen replay falls to 96,643 bytes. Current source is 96,668 after a concurrent board approval, with unchanged planning intent/hash.

25 relevant Python cases pass, including Unicode/literal-whitespace roundtrip and exact-wire budget enforcement. Local Qwen supplied the two-line edit but stopped before adding tests; parent completed tests and acceptance. After the concurrent storyboard work completed, idle ./run.sh activation preserved all 13 table digests and SQLite integrity. Both project APIs pass. Real same-Astra retry 72498a9331ed4278 passed the actual 96,668-byte preflight and succeeded with three groups and all eleven frame-use decisions. Concurrent storyboard changes then made that result stale; separately submitted newer job efb16090502145b5 also passed preflight (84,211 bytes) and succeeded. The live latest-plan API confirms succeeded, current contract and fresh; it was adopted through concurrent activity, not by this task. Old failed jobs remain unchanged; this task performed no adoption or media generation. Evidence: reviews/generation-plan-budget-20260914/verification.json.

## Generation-needed storyboard images — live (2026-09-14)

Latest user direction prioritizes image demand before image creation: I2VA opening by default, FL2VA opening/end when concretely justified, ordinary beats as text notes. New board proposals carry a versioned image_plan (mode/reason/exact evidence/unresolved constraints); adoption atomically saves it with existing source video settings. Deterministic validation rejects surplus intermediate anchors and false endpoint timestamps. Required inputs enter storyboard_usage before grouping; mode/reference downgrades are blocked. Unexecutable hard intermediate constraints stop batch generation and H3 handoff. Editorial Shot, Storyboard Frame and Generation Segment are formally distinct; arbitrary internal splitting remains unimplemented.

193 distinct relevant Python cases and nine Node suites pass; final mode guards were rechecked in 48 affected cases. Real revision-13 The Smallest Fix compiler fixture reduces the required source-image list from 11 board anchors to 4 under existing modes (1/1/2), without mutating source/edit data. This is not a newly adopted creative plan. Existing board/assets/approvals remain intact. Idle run.sh activation PID 4045518 passes both project reads and health; all 13 durable table digests match the backup, SQLite integrity is ok, and static parity passes. No provider/media generation or production adoption. See reviews/storyboard-image-demand-20260914/REPORT.md.

## Storyboard intent-to-image handoff — patched, backend activation pending (2026-09-13)

Independent real-job review found 1 of 11 then-approved The Smallest Fix board images in submitted H3 inputs; Shot 1 intermediate 4.8/7.8 frames were text/review context, and the other sources were not submitted. Actual CUT is scene 7.8 → Shot 2 source 0.2, not Shot 1's 4.8 key. Concurrent Ada identity adoption later made all 11 old frames stale; history remains.

Generation plan v3 now includes exact board intent and per-anchor image-conditioning/planning-only decisions, explicit boundary exceptions, validation against supported routes, propagation to source strategy, exact approved image binding and frozen graph checks. Existing plan/settings/revision/receipt mechanisms are reused. Board and plan UI separate intended use from receipted input. Arbitrary intra-source segmentation is not implemented; unsupported endpoint promises fail honestly. Old plans/manual routes are preserved without invented usage contracts.

151 Python cases including ownership and eight Node suites pass. Both projects return 200 on a read-only isolated preview, browser verified actual per-image usage, and offline real-request replay preserves original image wiring while rejecting a missing CUT-image promise. No media/provider jobs, production edits or service restart by this task. Formal backend activation remains pending; existing runtime jobs were left alone. See reviews/storyboard-handoff-20260913/REPORT.md and evidence/verification.json. Concurrent asset/take/selection changes are not attributed to this audit.

## Adopted H3 arrangements use a compact preparation list — live static update (2026-09-13)

The adopted-arrangement block now shows the scene, green adoption badge and one preparation row per independent source or native group. Repeated introductory prose is removed. Exact source/trim timing, planning reasons, checks, per-frame use information and original job records remain under a single collapsed 查看安排詳情 disclosure whose state uses the existing refresh-preservation hook. Stale arrangements retain a visible update action; archived native routes stay disabled.

Existing generation-arrangement and generation-group Node suites plus JS syntax pass. Served JS/CSS match disk; the live Chrome video page visually shows The Smallest Fix as three concise preparation rows. No backend, workflow, media or provider changes. Evidence: reviews/generation-plan-ui-20260913/adopted-arrangements-verification.json.

## Work status shows current work only — live static update (2026-09-13)

Removed recent and older terminal results from the shared work-status panel on every page, including live polling updates. Queued, running and attention-required work remain visible; existing historical row rendering remains available to other surfaces. Existing work-activity checks were updated and pass, along with JS syntax and served-static parity. No production actions or restart.

## Generation arrangement panel simplified — live static update (2026-09-13)

The screenshot’s adopted-plan warning block is now a compact green card with an explicit 已採用 badge and the review action. The heading is 生成安排; repeated workflow explanations are removed. Planning actions, job records and archived-group recovery sit under collapsed 更多選項, with initial planning actions visible for an empty project. Errors and stale/legacy notices remain visible.

Three existing Node suites and JS syntax pass; relevant Python suites report 16 passed and one unrelated existing fixture incompatibility (`wait_seconds` keyword in the render test). No Python code or tests changed. Live static parity and the existing Chrome video page were verified visually. No backend restart or production actions. Evidence: reviews/generation-plan-ui-20260913/verification.json.

## Video GPU contention queues instead of failing — backend activation pending (2026-09-13)

Failed Take `0c6dc2b23ef049fd` never reached ComfyUI: Studio requested a lease with timeout=0 while Qwen held GPU ownership. Native acquisition now waits (600-second default; video uses renewable 30-second windows). Exact pre-admission busy responses retain the same queued Take and retry admission; upload and /prompt remain outside the retry catch. Queued work is visible as waiting for GPU/service and resumes after startup with original frozen inputs and freshness gates. Prior failed work remains unchanged.

103 relevant Python cases and four Node suites pass. Coverage includes repeated contention then one submission, duplicate-click deduplication, same-token release, shutdown queue preservation, queued startup, paused/unknown/ambiguous refusals and errors after admission. Static labels are served from disk. Backend activation currently deferred for running directing review `482111088c554570` and concurrently submitted video `573b7a1ad2c04e68`; this task made no production retry, GPU service restart or sibling-project edit. Evidence: reviews/vram-queue-20260913/.

## Wait for transient VRAM transitions — UI live, backend activation pending (2026-09-13)

The old runtime status collapsed paused, maintenance and transition into one immediate error. Runtime status now waits up to 180 seconds of transition/maintenance polling, using only manager GETs, while actual paused state fails clearly without unpausing. Reservation still validates mode/PID after admission; busy manager refusals and ambiguous submissions are not retried.

The live service-prepare UI handles the old backend's exact pre-admission transition refusal by waiting and retrying only /video-provider/prepare, bounded to 180 seconds. It never wraps generation/upload or retries ambiguous transport errors. Once the backend is loaded, explicit paused and timeout messages stop this legacy retry path. 41 Python cases and three Node suites pass. Live preparation returned ready=true, H3 PID 3912377, without generation. Static parity verified. Backend activation deferred while directing job 482111088c554570 remains running; no automatic restart scheduled. Evidence: reviews/runtime-transition-wait-20260913/evidence/verification.json.

## Image generation selector honors renderer capabilities — live static fix (2026-09-13)

The image dialog's older wildcard filter exposed local_qwen as an image renderer, even though backend admission and profile settings correctly reject it. It now uses shared supportsProvider checks, excludes Qwen/DeepSeek from rendering, keeps Klein/Krea2 and configured image APIs, and submits the visibly selected renderer explicitly. Unsupported saved drafts/defaults require a new valid choice instead of silently falling back; draft feedback/references remain untouched. The local controls validate the same capability rule.

31 Qwen/profile Python cases, three Node suites and JS syntax pass. Served static matches disk. Existing image route remains Astra; no setting, production, generation or backend restart was performed. User clarified that local Klein/Krea2 supplies images while Qwen supplies creative/vision work. Evidence: reviews/image-provider-choice-20260913/evidence/verification.json.

## Automatic directing review respects context budgets — live (2026-09-13)

Oversized/output-heavy new directing reviews now freeze `directing-stages-v1`: complete linked-Shot/beat batches, original-source global reveal review and overlapping actual edit-boundary checks. Every exact request/schema is measured before spending; all required stages must complete before the aggregate can pass existing review/adoption gates. Source/provider/prompt/schema-bound checkpoints and a complete receipt support explicit same-job resume, cancellation and raw-output recovery without accepting partial/length-finished output. Small fitting reviews keep their previous path. Input labels distinguish UTF-8 upper bounds from native tokens; literal multiline quotations are checked against original source strings.

171 relevant Python tests and nine Node suites pass; isolated browser resumed the same job with 5/37 saved batches and frozen Astra settings. Real Astra local/global acceptance completed with valid evidence (local revise, global pass within its assigned scope); this is not whole-plan approval. The original seven-scene, 54-beat candidate fits 37 calls, maximum input upper bound 96,559 under its unchanged 131,072/32,768 budget. Phase authority prevents global QC requesting keyframes already assigned to mandatory local review.

Idle activation through ./run.sh PID 3846405 → 3855307 preserved all 13 DB table digests; health, both projects and served static parity pass. Original failed work remains unchanged. The stalled proposal was retried as **482111088c554570**, currently running with its first batch saved (1/37); no proposal was adopted. See reviews/directing-context-20260913/REPORT.md and live-progress.json. Minimum source units can still fail an insufficient budget; output allocation is not a completion guarantee.

## H3 service preparation switches through VRAM Manager — live (2026-09-13)

The generation worker already reserved H3 automatically, but the visible service check only inspected the current mode and instructed users to switch manually. Replace that dead end with an explicit 準備影片服務（自動切換 H3） action: POST /api/video-provider/prepare uses the same manager reservation, checks service/node readiness while held, then releases without uploading or submitting media. GET status remains read-only and explains automatic preparation. The button shows switching progress, prevents duplicate clicks and recovers after manager refusal. New video jobs still reacquire/revalidate their own reservation; recovery remains receipt-only.

37 Python cases and two Node suites pass. Real user-authorized preparation switched Klein PID 2932623 to verified H3 PID 3844858 through the existing VRAM Manager; no direct service control or generation occurred. Idle Studio restart 3786523 → 3846405 loaded the endpoint. A second live preparation succeeded without restarting an already-H3 process. SQLite integrity and all table digests were checked against the pre-restart backup. Evidence: reviews/h3-service-prepare-20260913/evidence/verification.json and live-switch.json.

## Storyboard review usability — live static update (2026-09-13)

User could not locate the visual-board review required by H3. The same read-only task summary now exposes approved/pending/missing/repair counts and a direct review entry from the scene's H3 workspace, board page and review room; the production overview uses the same continuation action. Navigation names the destination 分鏡畫面與審閱. Other pages collapse the full workflow map, and context-specific tasks replace duplicate next cards while retaining a stable target for background refresh.

The review opens the first pending image, shows its ordinal and neighbouring images, then saves one version-bound human decision and opens the next pending image. Missing or repair-needed frames remain outstanding. A replacement image is not hidden by an old image's repair receipt. Saving guards duplicate clicks, errors preserve the form, and project changes stop submission. Single-version image IDs are hidden from the normal flow; the decision bar stays visible and initial focus keeps image context in view. Human approval and provider generation remain explicit existing actions.

22 existing storyboard Python cases, 9 Node suites and 6 JS syntax checks pass. Live browser verified the review-room entry opening The Smallest Fix frame 4/11 with the original 3 approved frames, neighbour images and visible decision controls; no production review or media submission was made. Static bytes match the running server, with no restart required. Evidence: reviews/storyboard-usability-20260913/evidence/verification.json. The interrupted local Qwen scope left the video entry change; parent inspected and completed acceptance.

## Workflow architecture cleanup — live (2026-09-13)

The production chain now separates editorial baseline, Generation Plan, readiness, immutable frozen H3 request, attempt, Take review/compatibility/use, and unified Edit Segment material ranges. Audio and grouping projections no longer invalidate on supplemental visual frames or asset fulfilment. Legacy evidence is projected conservatively without rewriting approvals, requests or media. The normal editor previews change impact; no new Editorial Lock or final master renderer was added.

238 relevant Python cases and 13 Node suites pass; the final broad run includes the material-mapping corrections. Both current projects return 200 in an isolated read-only copy with background workers disabled; browser guidance, freeze blockers and edit-material UI verified. Production, revisions, takes, selections and human approval records match the acceptance snapshot. No media was generated by this task.

**The live 4760 backend is now activated.** After user authorization and verification that all job/video/voice queues were idle, Studio gracefully restarted through ./run.sh (PID 3626945 → 3786523). Health and both project/editorial APIs pass with workflow_contract_version 1; revisions remain 13 and 5. All 13 database table digests and 51 asset-file hashes match the pre-restart SQLite backup; integrity is ok and queues remain idle. No GPU services or providers were changed. See reviews/workflow-cleanup-20260913/REPORT.md and evidence/live-activation/verification.json.

## Workspace remains readable while storyboard review is pending — live (2026-09-13)

The Smallest Fix GET returned 400 because video_workflow.state called the gated prompt_source after its opening image became ready, propagating the unreviewed-board rejection into browser boot (暫時未能連接工作台). Readiness now records expected ValueError/OSError as that shot’s blocking reasons, matching the existing render/group state readers. Submission gates remain unchanged; no new review authority or permissive source is introduced.

76 focused Python tests pass. Four new I2VA/FL2VA × pending/stale board cases reproduce the original 400 before the fix, then verify successful project reads, per-shot/group blockers and continued prompt/packet/render refusal. Idle restart 3598849 → 3626945 passed health and SQLite integrity and preserved all 13 table digests. The original Chrome tab now displays The Smallest Fix revision 13, production overview, pending visual-board review and its navigation entry. No production or provider calls. Local Qwen reached its iteration cap without edits; parent implemented and verified the focused fix. Evidence: data/acceptance/workspace-read-gate-20260913/verification.json.

## Source-shot keyframe tabs follow time — live (2026-09-13)

The shot detail viewer sorts a copy of keyframes by source time (opening 0, interior source_time, ending duration), including its default selection, while retaining frame-ID selection. Existing stored order and production/media records are untouched. This corrects endpoint-first storage appearing as playback order after interior anchors were appended.

Two existing Node suites and app syntax pass; 18 storyboard Python tests pass. Live The Smallest Fix / The switch displays opening → 0.2 → 2.6 → 3.2 → 5.4 → ending. Clicking ending shows its saved planned description (no ending image exists); returning to opening restores the original asset. Served app matches disk; no restart or generation. Evidence: data/acceptance/keyframe-order-20260913/verification.json.

## Frozen image moments synchronize before rendering — live (2026-09-13)

The diagnosis failures a545a98a17fc4954 / 406ef3048e334bef exposed dual authority: endpoint prose said Pip looked at the lantern base while start_state and the timed performance required Ada. Image preparation now carries a versioned exact-moment contract: structured state owns explicit dynamic facts, descriptive prose supplies composition and compatible detail, and superseded clauses are recorded. Explicit requests and genuinely contradictory authoritative states/timing remain meaningful constraints. Production/revision prompts require joint updates of affected states, beats and descriptions; directing review explicitly checks them.

Every new automatic frame render now checks the ACTUAL compiled brief with the same selected preparer before media submission. Actionable findings get one automatic full-brief repair and a recheck; uncertainty or unresolved conflict stops before rendering. Durable intent, source/provider/reference/output hashes, cancellation and failed-attempt refusal prevent repeated spending on recovery. Explicit frame image_prepare uses the same flow. Read-only job receipts and job-detail notices expose synchronization/verification above technical records. Text checks do not certify generated pixels. Canon and old description strings are retained as history; the user no longer needs to manually patch this duplicate prose before a new generation request.

131 distinct focused Python cases passed across the implementation and related regression runs, three Node suites passed, and the unchanged portable package verified 66 files. Real text-only Astra acceptance used revision 13 and the original four frozen references: its FIRST brief automatically changed Pip's eyeline to Ada and kept the lantern off, logging the superseded phrase; the separate pre-render check passed with no issues. No media was generated. Idle deployment 3565506 → 3598849 passed health, SQLite integrity, static parity and exact preservation of every database table digest. Local Qwen timed out at 900 seconds with unverified partial files; parent corrected and completed implementation/acceptance. See docs/FRAME_MOMENT_SYNC.md and data/acceptance/moment-sync-20260913/verification.json.

## Visual storyboard workflow — deployed (2026-09-13)

The normal production guide now routes adopted direction → ordered visual board → missing images → image-bound per-panel review → H3 groups. `storyboard_frames` uses the configured creative provider and mandatory methods 1.5.0 (pinned visual-skills/montage included). Each edit use has an exact edit-in anchor, with justified additional frozen states; source Shots support start/end plus interior timed keys up to eight total. Reuse preserves source/canon/dialogue and unchanged endpoint image hashes. Durable batch receipts skip current/pending/active/uncertain targets. Human panel approval binds current source and exact image bytes; `revise` retains originals and history. Adopted boards gate downstream H3; legacy unadopted routes remain. Reviewed interior keys may condition trimmed native group endpoints, but never enter legacy “both endpoints” reference sets automatically. Portable export includes the board, metadata and current panel-approved images.

18 dedicated Python cases pass, related regression runs passed (see acceptance), six Node suites pass, and 66 portable package files verify. Real DeepSeek d6ed27d3809744a1 returned three ordered panels in an isolated copy. Browser adopted only that copy's result, imported an existing real image, and recorded its premature lit-lantern state as revise. Live three-panel/eleven-anchor board e686c81ce7e74f59 had already been adopted through concurrent activity; this task preserved production revision 13 and board revision 1. Current board images still need production/review; no new image/video or final-film quality claim.

Idle app deployment 3528171 → 3553614 passed health/integrity. Project, revision, asset, settings and media tables retained their digests; only jobs/events/sqlite_sequence changed because the existing startup reviewer attempted 下一站：長洲. That automatic job a9f04b6056f54217 failed context preflight (184965 input +32768 output >131072), with no external request, fallback or retry. Served app/board/group/CSS files equal disk and live layout was inspected. Local Qwen audit exhausted its budget without edits; parent completed the implementation/audit. See docs/STORYBOARD.md and data/acceptance/storyboard-20260913/verification.json.

## Generation grouping no longer prescribes independent-source modes — deployed (2026-09-13)

Real saved plans 67c582c3b9f041d4 and 83e1c0e164a1461f still incorrectly treated a 0.2–5.4 edit of an 8-second source as excluding I2VA/FL2VA. The earlier prompt-only acceptance was insufficient. Contract v2 now requires `mode: null` for independent sources and a concrete mode for native groups, with deterministic rejection rather than output normalization. Independent conditioning stays in the existing per-source strategy workspace. Full-source versus selected-interval rules are separated. Timing and continuity guarantees are explicitly disallowed in explanations and described as footage checks.

Frozen planning inputs carry the contract version, so old plans remain readable but cannot be newly adopted. Plan reviews and job records show correction guidance above collapsed historical advice and provide source-workspace navigation. Current saved modes, media and human approval remain separate. Structural success still does not prove all prose: live DeepSeek verification first proposed a different grouping with a false continuity guarantee, then repeated a wrong frame count. Parent reviewed and supplied a concise exact correction preserving three independent sources and 25 seconds of selected footage. Subsequent text calls preserve that reviewed correction while rebasing onto concurrent storyboard method/keyframe updates; no image/video generation or adoption was performed by this task.

35 initial relevant Python tests and three Node checks passed. Concurrent adopted-arrangement and storyboard endpoint integration was checked with 23 relevant cases; the endpoint error wording changed, the new regression expectation was updated, and all six contract cases passed again. Live browser verified historical advice collapsed and adoption disabled. Final candidate e3c6575a08a648e5 matches the supervisor-reviewed correction exactly, uses the latest source, and is saved pending user adoption. Live browser verified three independent-source cards, source navigation and an enabled adoption button; screenshot layout was checked. Freshness checks are recorded in data/acceptance/generation-plan-contract-20260913/verification.json; corrected readable text is in corrected-arrangement.md there. Local Qwen was busy; parent implemented directly. Concurrent work changed settings/jobs; measured project/revision, asset and video/voice tables remained intact. Do not claim every SQLite table was unchanged across concurrent activity.

## Adopted generation arrangements drive the H3 workspace — live (2026-09-13)

H3 now shows saved adopted arrangements independently of newer candidate jobs, with explicit independent-source duration/trim ranges, checks and native-group entry points. Selecting either member of an active saved native group opens its complete grouped image/prompt/render workspace; the top planning panel no longer duplicates those render forms. Source navigation remains available for independent shots. Group focus survives refresh and does not submit work. Current independent-source arrangement requirements enter the shared Shot prompt basis used by H3 source workflows, preserving full-source timing and later editorial trims. Original mode selections, source prompts and media are not overwritten.

Legacy planner receipts never auto-apply erroneous independent conditioning advice. Stale planning analysis remains labelled; active saved native definitions remain usable through their own current reference/prompt/render validation rather than disappearing on a method-only planning change. Archived groups lose their route. New candidate results cannot conceal an older adoption. Checks disclosures preserve open state across refresh.

35 relevant Python tests and five Node suites passed; isolated browser verified exact source ranges and navigation. Live browser verified the independently newly adopted 9b006b03e57744cc arrangement: edit_01+edit_02 opens mg_e5af00e511576f48, 13-second I2VA with one opening reference and grouped controls; edit_03 remains an independent 12-second source. Missing approved opening pixels still block generation. During this task other active project tasks changed planner/method/storyboard code and adopted a new arrangement; these changes were preserved, not attributed to this fix. Idle backend deployment PID 3528171 preserved every SQLite table digest; subsequent UI updates need no restart. No task-initiated creative/media jobs or production adoption. Evidence: data/acceptance/generation-arrangements-20260913/verification.json.

## Detailed image revisions survive preparation validation — live (2026-09-13)

Image job 6ae9d094bc3d4ebf for The Smallest Fix / frame_01_start failed before ComfyUI submission: the same-provider conflict correction returned 1,026 characters of requested_changes against a 1,000-character field cap. Its saved correction has no remaining conflicts. Runtime PreparedImage and the Studio-owned exported schema now allow 4,000 characters in that field; the complete compiled image prompt still has the existing 7,500-character cap. No truncation, creative rewriting or additional automatic provider attempt was introduced.

55 Python tests pass, including long correction, durable reuse, schema parity, exact field boundaries and total prompt rejection. The actual saved correction validates and compiles read-only to 6,258 characters with all seven revision requirements intact. Local Qwen was busy; parent completed the bounded fix. Idle Studio restart to PID 3510907 passed health and SQLite integrity and preserved all table digests. No generation/retry, approval, canon change or GPU operation occurred. Historical correction retains its incomplete decision record; it is not silently recovered or adopted. A new image-generation attempt can use the corrected validator; successful rendering and visual acceptance remain unverified. Evidence: data/acceptance/image-revision-length-20260913/verification.json.

## Explain actual DeepSeek output-budget exhaustion — live (2026-09-13)

Failed candidate review 04b290de8e464d31 froze and sent a 1,048,576 context / 32,768 output budget. Its measured text-input upper bound was 71,851 and preflight passed; transport returned length after 149 seconds with zero answer characters. Historical transport did not count reasoning or retain token usage, so reasoning exhaustion is plausible but cannot be established for this attempt. The context feature reserves output capacity; it does not guarantee bounded completion or automatically resize a monolithic directing review.

Job details now expose a safe root-attempt budget receipt and display budget exhaustion above technical records, separating input capacity, output cap, answer characters and missing historical accounting. Settings explains reasoning shares generated output and only new work receives changed limits. New DeepSeek requests include usage receipts, count reasoning characters without storing private text, retain allowlisted numeric token usage and requested output cap, and distinguish observed reasoning-only exhaustion. No automatic retry, budget increase, reasoning downgrade or provider change. Partial output remains ineligible for adoption.

38 Python tests and three Node checks pass, including transmitted max_tokens, reasoning-only length, no private-text retention, one request only, old missing-usage semantics and readable budget guidance. Idle deployment preserved all database table digests and integrity; actual failed-job GET reports the verified receipt. Evidence: data/acceptance/output-budget-20260913/verification.json. This fixes diagnosis and visibility; the failed review still requires an explicit retry with suitable output budget or a separately designed bounded review workflow.

## Readable directing-review work records — live (2026-09-13)

The 19:09 screenshot exposed method manifests and file hashes before the actual review, with a generic completion/adoption message and a self-link inside its own record. Job records now lead with the saved directing verdict, summary, named-shot problems and concrete recommendations; evidence and per-beat coverage remain expandable. Applicable current-plan records expose revision and human-approval entry points, candidate records route to their proposal, and historical/deferred records cannot present approval of the current plan. Human approval remains separate from the AI verdict. Completion rows explicitly state the saved directing verdict. Technical manifests, context usage, full requests and raw directing results are collapsed below the readable result; execution errors remain visible.

Read-only job-dialog polling now updates the result as well as the header, retaining the frozen request/shot labels missing from the lightweight project response, independently preserving disclosures and leaving unchanged DOM intact. Opening reads the fresh project and full job, validates ownership and ignores responses after project navigation. No provider, review, approval or adoption semantics changed.

24 relevant Python tests and six Node checks pass, including new result-state, provenance, applicability, escaping, pending-to-completed refresh and actual click-handler coverage. Live browser opened The Smallest Fix's saved review 14c366d2b7af4918 and verified 需要修訂, its two concrete findings, both next actions, no self-link and collapsed technical details below. Screenshot layout inspected; all three served static files match disk. No production submission, approval, adoption, database mutation, restart or GPU operation. Evidence: data/acceptance/job-detail-readable-20260913/verification.json.

## Current-plan revision opens concrete saved review suggestions — live (2026-09-13)

User found 修訂劇情表達方案 still opened the generic empty feedback form. That button now opens a dedicated current-adopted-plan form with visible problem/recommendation cards, labelled scene/Shot context, optional supplemental requirements and 按以上建議開始修訂. Suggestions come only from current applicable scene review records; stale/unrelated proposal results cannot fill the form. Shared review records and exact repeated findings are deduplicated. Outline projects choose a real adopted chapter, scope recommendations to it and use its source-kind capability. No current advice exposes an honest pending/missing result message and requires an explicit edit request rather than pretending to have suggestions. Existing proposal revision notes now start expanded.

Submitting carries saved advice against the currently adopted production, without a proposal_id for a historical candidate. Project, chapter and feedback are frozen; a read-only preflight catches changed production/editorial/chapter sources. A project or dialog switch cannot close another form or open late progress. Current revision checks are a frontend preflight; the job service still freezes the source current at submission. A tiny cross-request race remains governed by existing job/adoption freshness checks.

24 relevant Python tests and five Node scripts pass. New tests exercise visible grounded suggestions, chapter scope/capability, read-only opening, empty optional-input submission with advice, stale-source refusal, missing advice and pinned project handling. Live browser opened The Smallest Fix revision form and showed its actual switch-scale and occluded-hand/badge advice plus the direct apply button. All three served files equal disk. No production submission, generation, approval or restart. Evidence: data/acceptance/current-directing-revision-20260913/verification.json.

## Storyboard views and native H3 generation groups — live (2026-09-13)

User approved separating storyboard views, image requirements and H3 request count. The video page now offers routed director grouping proposals plus manual grouping. Groups cite consecutive edit IDs, allow short internal views within one 4–15-second H3 clip, and select I2VA opening, FL2VA outer endpoints or 1–9 role-labelled Ref2VA images. Missing references have preparation/import actions. Group prompts preserve explicit timed hard cuts and canonical mapped dialogue; reference-role preambles are allowed for Ref2VA. Existing per-source production remains available. Mandatory Studio methods v1.4.0 distinguish these contracts and retain pinned visual-skills montage planning.

Native groups use the existing owned Comfy workflow as one Director segment, immutable input snapshots, receipt recovery and whole-clip adoption. Group selections never impersonate constituent source takes. Planned mappings and user-observed actual boundaries are separate; export includes ready handoffs and selected clips with both provenance/mapping, and navigation counts observed edit coverage without duplicating takes. Group planning is read-only; existing AI/human directing approval still gates H3 prompts/media.

Isolated protocol and browser acceptance pass, including 12 group tests, reference modes, source/ordering guards, one mocked submission, selection/export, real form saves, group disclosure preservation and ready generation entry. Real unchanged DeepSeek produced a 13-second/two-view/four-reference prompt with a 7.8-second cut and exact 4.8–6.0 dialogue; it retains concrete image/staging concerns. Real planner reasoning exposed a trimmed-source misconception, fixed in its contract; its later precision-sensitive scene recommendation is independent sources, not forced grouping. Empty provider note fields normalize without discarding meaningful unknowns; raw failures/outputs remain. No actual image/video generation or production adoption occurred. Final idle deployment PID 3228031 replaced 3219186, preserved every SQLite table digest, passed integrity and matches the verified backend/static files. Live browser confirms the new entry points at project revision 11. Formal projects/revisions and provider routing remain unchanged by this task. Details/limits: docs/GENERATION_GROUPS.md; evidence: data/acceptance/generation-groups-20260913/.

## Human directing approval alongside AI review — live (2026-09-13)

User requires final creative authority when directing review does not pass. Current formal plans now offer 人工批准此方案 beside review status; readable fresh proposals offer 人工批准並採用此方案 alongside normal guidance. The form shows AI feedback and optional notes. A separate durable human approval records actor/time/revision/content/review provenance; original AI jobs/verdicts remain unchanged. Human approval clears directing readiness and proposal adoption, including exact chapter Scene inheritance. Late AI results and method-only updates cannot revoke it. Relevant scene/editorial changes invalidate applicability; stale/foreign/incomplete proposal submissions are rejected. Automatic review reconciliation respects human approval. No automatic approval of user productions.

94 integration checks passed before the final audit-provenance refinements; the final 12 focused approval tests pass, covering all AI lifecycle states, unchanged AI records, real H3 gate traversal, late AI completion, source/revision guards, method stability, proposal adoption, chapter scope and editorial invalidation. Six Node scripts plus app/editorial syntax pass. Isolated browser submitted the actual form with blank optional notes, advanced production navigation and showed human approval while retaining AI revise. Live endpoint/source hash verified. Idle deployment PID 3219186 preserved every SQLite table digest and passed integrity; no production approval/generation/provider changes. Parent handled this cross-module decision directly after the previous local Qwen iteration-budget failure. Evidence: data/acceptance/directing-human-20260913/verification.json and deployment.json.

## Saved directing intent automatically continues into review — live (2026-09-13)

User rejects an extra review click after saving directing intent. Post-save, startup and current-production review terminal hooks now reconcile missing/stale formal plans through the existing routed queue. Exact plan/editorial/method fingerprints prevent duplicate attempts; a newer saved source waits for older active work and schedules when it ends. Current passed adoption skips redundant review. Same-source failure, interruption, cancellation and substantive revise/uncertain results do not auto-loop; pre-job submission errors are durable and cannot turn a committed save into an apparent failure. Manual routing remains awaiting_input. GET views remain read-only and scene certification gates remain authoritative.

Current review receipt drives saved-intent, editorial and production-navigation feedback. Running/queued/deferred/manual states hide duplicate submission; failures expose retry and results expose the substantive next step. Blocked keyframe/H3 requests state actual review progress and its navigation path. Shared temporary API test fixture prevents background external provider dispatch; tests execute workers explicitly. An existing editorial UI test fixture was missing the return-progress header node and was brought into sync.

140 relevant Python tests, five Node scripts and app/editorial syntax pass. Isolated browser verified queued/running/no duplicate controls and failed/retry; live browser verified running review and direct navigation expanding the correct directing section. Live PID 3204694 replaced 3198343 only with all work queues idle; backup and SQLite integrity pass. Projects, revisions, assets, chapters and voice/video takes are unchanged. Startup automatically queued two method-stale existing plans with the configured DeepSeek provider (14c366d2b7af4918 and 329ca426b7da41bd); only job/event bookkeeping changed at deployment. The Smallest Fix automatic review completed with revise; 下一站：長洲 is still running at handoff. No claim of creative pass, no automatic adoption or media generation. No provider routing/authentication/GPU services changed. Local Qwen exhausted its iteration cap without edits; parent completed implementation and verification. Evidence: data/acceptance/directing-auto-20260913/verification.json and deployment.json.

## Qwen3.8 whole creative-engine profile — live (2026-09-13)

Settings now offers Powered by Qwen3.8 alongside Astra and DeepSeek. Explicit application atomically routes all creative capabilities, installed/old skill routes and future capability defaults to local_qwen (qwen3.8-27b) through the existing VRAM Manager gateway. Image rendering remains separately selected; wildcard creative support never permits Qwen image generation. Existing context budgets and Studio production methods remain active. Qwen application preserves DeepSeek settings/credentials and existing jobs keep their frozen provider. Passive connection checking lists models and reads available vision metadata without loading a model; busy execution reports failure without provider fallback.

52 focused Python tests, existing/new provider UI checks and app syntax passed. Isolated browser applied Qwen + ComfyUI, confirmed creative routes, sidebar/header, connection feedback and persistence after reload with no console errors. Real adapter acceptance returned Traditional Chinese direction (13.70 s) and correctly identified a diagnostic red square on the left / blue circle on the right (1.32 s). These small transport checks do not certify full-film creative quality. No production content or media was generated or adopted. Live Studio PID 3198343 was restarted only after all state-bearing tables had no queued/running work; all 13 SQLite table digests remained equal and integrity passed. Live mode remains DeepSeek with ComfyUI images; user can select Qwen explicitly. No GPU service or authentication files were modified. Qwen execution delegation was busy, so parent implemented directly. Evidence: data/acceptance/qwen-profile-20260913/verification.json; see docs/QWEN.md.

## Mandatory Visual Skills montage planning — live, with bounded real-text evidence (2026-09-13)

User requires montage to affect everyday automatic directing. Method bundle v1.3.0 loads pinned Serge Shima visual-skills dramaturgy and Studio adaptation in narrative/storyboard/qc/directing_qc, including full and scoped chapter prompts. Scene outlines decide coverage before sources freeze; existing intent/Shot/edit fields carry ordered setups, evidence, reactions, rhythm and cut-state continuity. Source still/H3 writers stay scoped to the adopted continuous take. Workspace exportable method copy, licenses and provenance are complete. Existing productions and media remain unchanged; updated method requires current directing review. Late reviews retain their frozen method hash instead of receiving the method installed at completion.

Plan/proposal scenes show saved edit order, repeated source uses, framing, screen duration, purpose and expandable cut reasons before beat detail. 226 relevant Python tests and four Node scripts pass; live browser verified three existing edits, closed cut details, no overflow/errors. Idle deployment PID 3149563 passed health, every database table hash, integrity and unchanged routing. Qwen was busy; no polling/preemption.

Real DeepSeek staged screenplay test preserved the exact source and produced 10 sources / 51.5 seconds, 10 edit uses / 42.7 planned screen seconds (shortest 2.6 seconds). Both the first monolithic storyboard request and the full ten-shot independent review exhausted the existing output budget; failures and partial outputs are retained. The generated candidate remains unadopted with adoption correctly blocked. Two smaller real review controls completed: closer coverage received revise on state/knowledge/readability findings; deliberately unreadable distant coverage received revise with explicit UNREADABLE_CARRIER repairs. These establish integration and concrete review responses, not a positive creative pass, a completed film or guaranteed provider reliability. All real acceptance uses isolated storage and unchanged provider/model; no user media/generation/adoption changes. See docs/MONTAGE.md and data/acceptance/montage-integration-20260913/verification.json.

## Direct identity-image adoption, editor uploads and scene asset batches — live (2026-09-13)

User likes the current Pip despite its stale/failed-sheet status and needs direct adoption, existing-image selection/upload in the identity editor, and scene-based batch production. Visual identity cards now expose 採用目前圖片 when the shown image is not approved. Explicit confirmation can adopt current candidates through the existing gate or copy stale pixels into a new current applicability record, preserving the historical source hash, references, review and image. Human adoption does not claim automated sheet approval. Source provenance is retained in settings/events and protects historical sources from deletion; dependent approved references still invalidate normally.

Identity editing offers 保留目前圖片, 選用現有圖片 and 上傳圖片並採用, with previews and an explicit 儲存設定並採用圖片 action. Source/project/target/revision, whole-plan and file validation precede saves; chosen pixels are adopted against the saved identity revision. No image-generation or review provider runs for explicit human adoption.

Each scene disclosure includes 批量製作所需資產. A read-only preview derives actual visual canon from its location and shots, including only required public identities and excluding voice-only cast. Current approved, pending, active and unresolved local work are skipped. Explicit generation submits the remaining targets through the existing queue, with durable per-item intent/receipts, provider selection, stale-preview checks, cross-scene shared reuse, duplicate protection and partial-failure reporting. Fresh previews can retry known failures; uncertain interrupted submissions never replay automatically.

47 relevant Python tests and JS syntax/identity/scope/group/approval/provider/activity checks pass. Isolated browser exercised direct stale adoption, existing selection plus identity editing, upload preview plus adoption, and a batch that skipped approved Pip and queued only Workshop without executing a provider. Live Pip card shows 採用目前圖片 and all checked UI files match disk. Current PID 3149563 started after final backend edits; new routes verified. A concurrent task had already restarted Studio, so our guarded old-PID deployment stopped before signalling; no second restart. No production generation or adoption was performed by this task. Local Qwen hit its iteration cap with an incomplete batch module; parent corrected it and completed verification. Evidence: data/acceptance/identity-assets-scene-batch-20260913/verification.json.

## Guard proposal opening against unfinished and non-proposal jobs — live (2026-09-13)

The user's 14:30:38 screenshot showed Cannot read properties of null (reading production). The proposal click route dereferenced j.result.production without checking state, type or result. Historical job records show the contemporaneous re-review bee4f91fe3a442ec running from 14:30:01 until successful completion at 14:32:33; the screenshot alone cannot identify which job ID the old client attempted to open. This client-side error does not establish that generation failed.

A shared read-only selector now requires a succeeded narrative/storyboard job with canon/scenes/shots arrays before any proposal rendering. Pending, failed, missing-result and wrong-capability jobs open safe progress/record guidance. Project ownership is checked; proposal guidance in either view never offers opening/adoption for unreadable results. Pending review stays at step 2 with no premature open button. Direct and chapter-wrapped results remain supported; adopted production is unchanged.

Six relevant Node checks and app syntax pass, including actual click-handler tests for null results in every lifecycle state, malformed envelopes, wrong capability, project ownership, direct/chapter plans and pending review gates. Isolated browser reproduced a stale completed-proposal button receiving running/null and verified safe progress, no crash or JS errors. Static files served by the existing Studio match disk. No real review/generation/adoption/restart or data changes. Evidence: data/acceptance/proposal-null-20260913/verification.json.

## Re-review submission feedback inside proposal dialogs — live (2026-09-13)

Follow-up exposed that proposal guidance still used generic waiting/retry wording despite the new activity list. Proposal header and footer now explicitly distinguish sending, queued (including selected review queue position), running, manual handoff, failure/interruption/cancellation and missing review. Pressing re-review synchronously replaces both retry controls with sending feedback, stops the old watcher, and renders the actual returned job before subsequent reads. Closing/changing dialog or project prevents a late response from reopening it. Existing review details cannot show an old verdict or revision/retry controls while a review is pending. No review/adoption gates or provider routing changed.

Isolated browser acceptance exercised the actual button with a delayed test POST: visible sending → queued at position 2 with waiting project → automatic running update in both header and footer, no retry button while pending, no JS errors. Deferred-request regression tests cover immediate receipts, cancellation of the old watcher, closed dialogs and project switching; seven related Node checks and 12 Python directing tests pass. Production changes are static only, with no real review submission, generation, adoption, restart or GPU operation. Evidence: data/acceptance/review-feedback-20260913/verification.json.

## Production overview omits recent results — live (2026-09-13)

Per user request, 製作總覽 no longer displays 最近結果 or the older-results disclosure in its shared work-status panel. Initial rendering and status polling both use the same page-specific option; active/queued/action-needed work remains visible. Existing work activity checks and JS syntax pass, plus direct rendering verification for hidden completed/failed rows. Live browser confirms 製作總覽 has zero recent-result sections and still displays the running Ada image job. Static-only change; no restart or production mutation.

## Visible queues and live work status across production pages — live (2026-09-13)

User found queued/running work invisible, especially the story stage card which only said 建議先處理. A shared activity panel now precedes the workflow path on every production page, with separate running/queued/user-action counts, named jobs, saved queue positions and waiting projects, real chapter-stage/shot counts, cancellation state and exact work links. Latest result stays visible; older recent results are collapsed and successful generation is explicitly distinct from adoption. Story/reference/frame/video/voice stage badges show their active work alongside adopted readiness; queued story recommendations explicitly say waiting to start.

Status-only polling continues while forms/dialogs/media/requests are active. It does not replace editable project state, fields or media, rejects responses after project navigation, and keeps unchanged status buttons in place. Full refresh rechecks active editing/playback before rendering; network loss marks stale progress and recovery clears the notice. Job-record status updates inside its existing dialog. Concurrent proposal-guide and watcher changes were preserved.

14 relevant Python tests and seven JS checks passed; isolated read-only browser fixture verified queued → running during unsaved text entry (values and focus preserved), running → failed → succeeded in an open job dialog, saved counts, explicit failure and result states, and 390×844 mobile layout (panel y=441–673, no page overflow). Existing production browser showed the panel; all five served files match disk. No generation, adoption, database mutation, restart, provider change or GPU operation. Qwen bridge was busy; implemented directly without polling or preemption. Evidence: data/acceptance/work-activity-20260913/verification.json.

## Guided proposal revision and adoption — live (2026-09-13)

User asked why a blocked proposal did not guide them step by step. Proposal dialogs now show revision → automatic directing review → human review/adoption, with the applicable next action at top and bottom. Full QC is expandable; optional revision feedback clearly carries saved notes. Submission opens progress for the exact new job and read-only polling updates review/progress without collapsing unchanged disclosures; closing/replacing the dialog or switching project prevents late updates. Stale candidates lead to planning from current production, pending review does not request another revision, manual/failure states have explicit next actions. Existing backend acceptance/freshness gates remain intact.

Six relevant Node checks, app syntax and 12 Python directing integration tests passed. Browser verified live stale-candidate guidance and labelled isolated revise/form UI at desktop and 390×844 (no horizontal overflow, primary action ends at y=412). No live generation, review submission, adoption or restart by this task. Separate activity adopted The Smallest Fix from revision 9 to 10 during acceptance; preserved. Local Qwen hit its iteration cap with unverified output; parent completed and verified. Evidence: data/acceptance/proposal-guidance-20260913/verification.json.

## LoRA evidence validation and failed-brief reuse — live (2026-09-13)

Job eecdfd0d692349b9 stopped before ComfyUI because one of three LoRA quotations came from source instructions; two quotations exactly matched its returned visual brief. Preparation now deterministically removes surplus non-quotes only when each selected adapter retains an exact nonempty quote. IDs/reasons/summary, fixed adapter weights/triggers and visual content remain unchanged; the original selection and discarded strings are saved and exported. Strict final validation still rejects all-invalid evidence and unrelated schema/catalog/compatibility faults.

Explicit identical retries may reuse this older pre-render brief after source/request/fingerprint, complete frozen image hash, compiler output and final plan checks. Cancellation and any ComfyUI receipt exclude this fallback. Original failed job and all files remain unchanged. Its five real reference inputs were checked; the two retained citations preserve the off lantern and Ada's forward lean, with Klein consistency strength 0.6. No generation was invoked by this repair. The user independently generated a newer frame while this change was being verified.

103 relevant Python tests, JS syntax/retry/status checks and live browser verification pass. Failed work details now explain the preparation stop instead of showing structured result null. Evidence: data/acceptance/image-lora-evidence-20260913/. After production and review queues became idle, checkpointed SQLite and restarted Studio (PID 3086775). Health and integrity pass; every database table is unchanged across deployment and backend source hashes match tested code. Concurrent proposal UI edits were preserved and the JS checks rerun.

## Model context budgets and grounded retrieval — live and verified with Qwen (2026-09-13)

New text jobs freeze input/output budgets and count before transport. Local Qwen is selectable through the existing VRAM Manager gateway with runtime per-slot metadata and real chat-template tokenization; routing remains unchanged. Long-context requests retain complete backgrounds. Oversized chapter stages create checkpointed original-source spans, direct-source navigation and verbatim anchors, then retrieve current-scene originals, transitive references, unchanged canon, global scene direction and previous-shot state. Known scene recalls expand to all their source units. Required context is never clipped; unresolved capacity/ref dependencies stop with saved work intact. Foundation writing deduplicates source copies but keeps the complete original; non-chapter jobs currently have preflight rather than chapter-specific retrieval. Per-model limits and per-job context receipts are visible in Settings/details. See docs/MODEL_CONTEXT.md.

Full suite passed 725 tests; final settings/context refinements passed 32 and 24 targeted tests. Isolated browser saved a Qwen 65,536/4,096 test budget without changing routing. Original 天水圍 requests measured 151,592 and 202,951 native Qwen tokens, exceeding its 131,072 runtime slot. Real Qwen completed 21 source-index checkpoints covering all 663 original units and generated the final shot from an 89,554-token scoped request; stage structure validated. Final pack additionally preserves the complete frozen screenplay, measuring 91,126 tokens with the same original units, canon and previous-shot boundaries. Seven index checkpoints remained byte-identical across controlled interruption/resume. This is not creative-quality approval: the original frozen final-shot brief has a 15-second timing constraint for lengthy actions. No test candidate was adopted. Original 22 production checkpoints and project production remain unchanged. Final idle deployment PID 3054257; evidence in data/acceptance/context-adaptation-20260913/.

## Cross-page production navigation — live (2026-09-13)

User found the overall workflow unclear and could not identify the current step or next action. Opening a work now defaults to 製作總覽. A shared guide on all production pages separates 正在瀏覽 from 建議先處理, displays five saved-state stages, and offers one concrete next action. Sidebar page order places references before frames; each stage explains its work and completion criterion. The editorial page identifies step 5 and links back to the same project's overview. Mobile puts the next action before the full route; at 390×844 its full-width button ends at y=612 without horizontal overflow.

The read-only selector handles pending image decisions, current-revision text proposals and their aggregate directing review, in-flight/interrupted revision work, authoritative reference readiness, selected mode and source freshness, mode-specific frames, render prerequisites, current unselected/selected video takes and unresolved receipts. I2VA does not require unused end frames; REF2VA does not require storyboard frames. Preparation counts include actual saved render checks, and adopted video counts are unique per current Shot. Final-stage wording does not claim a completed film. Navigation can open a review/form or select the exact unfinished Shot; generating, retrying, adopting and resuming still use existing explicit controls. Local video section labels no longer compete with global step numbers.

18 relevant Python tests and eight JS check scripts passed, with browser acceptance on both existing productions and across seven production routes. Verified current proposal review with disabled blocked adoption, exact Shot URL/focus, editorial return and desktop/mobile layout; no browser errors. Both productions, revisions (9 / 5), existing asset statuses and job counts remained unchanged during the acceptance interval. All nine served files match disk. No restart, production save, media generation or GPU modification. Concurrent proposal-adoption UI improvements were preserved with a focused app.js patch. Local Qwen reached its iteration cap without edits/checks; parent implemented and accepted. Evidence: data/acceptance/production-navigation-20260913/verification.json.

## Director proposal actions explain blocked adoption and carried feedback — live (2026-09-13)

Follow-up: the user could not understand the disabled adoption or revision action. A fresh browser reproduced the disabled adoption (correctly required by overall revise) and successfully opened the existing revision form; no broken click handler was established. The form concealed automatically carried review notes behind an empty generic textarea. The proposal now explains overall QC and revision freshness at its top and beside disabled adoption, with a prominent revision entry. The dedicated revision form shows carried notes in a disclosure, labels extra text optional and offers 開始修訂 with a submission receipt. Its submit handler pins the original project across navigation. Existing adoption gate and production remain unchanged.

Actual handler VM test verifies blank optional input still submits the correct proposal and review issues to the original project. Directing/UI responsiveness checks, app module syntax and 12 Python directing tests pass. Static files verified served without restart. No live generation or adoption. Evidence: data/acceptance/directing-actions-20260913/verification.json.

## Generated directing proposals are visible before adoption — live (2026-09-13)

The Smallest Fix revision 9 still uses legacy scene_workshop, while two current-revision formal director proposals already exist. The latest proposal 60e297e7ef15463b completed generation and review, but its overall review verdict is revise (Pip’s shot-02 starting arm/keyframe position conflicts with the preceding shot), despite all individual beat coverage rows passing. The directing panel previously ignored these candidates and misleadingly requested another generation.

The panel now identifies the newest current-revision formal proposal for each missing-intent scene, distinguishes pending review/revision/adoption, explains the retained adopted version, and links directly to the existing proposal review modal. Stale, failed, nonformal and unrelated candidates cannot stand in for current scene intent; chapter candidates respect localized scene ownership. Adopted production and original QC/adoption gates remain authoritative. Live browser verified 拍攝意圖已生成 · 需要修訂, the exact latest proposal, full intent, continuity findings, revision action and disabled adoption. Node directing/overview checks plus 12 Python directing tests pass; served module matches disk. No generation, adoption, database mutation or restart. Evidence: data/acceptance/directing-candidate-status-20260913/verification.json.

## Pip electronic audition — produced and available (2026-09-13)

User explicitly requested an audition for Pip's nonverbal voice. Created a real 4.8-second, mono 48-kHz PCM16 electronic beep/boop clip with authored additive/FM synthesis: short confirmation, rising curious pair, lower spaced hesitation and gentle acknowledgement. No speech/model impersonation, human audio or GPU work. Synthesis source, tone schedule and audio hash are retained in `data/acceptance/pip-beep-audition-20260913/`.

Imported via the existing version-checked finished-effect route as current candidate `76196028f3b54b20`; profile v2 and canon remain unchanged and adoption remains the user's choice. Exact downloaded bytes match the synthesized WAV hash, peak -13.22 dBFS (no clipping). Browser shows the direct Pip player and closed history. This is explicitly authored electronic synthesis imported as a real file, not a VoxCPM result or a new automatic generation provider. Evidence: `data/acceptance/pip-beep-audition-20260913/verification.json`.

## Direct voice audition with collapsed histories — live (2026-09-13)

Candidate/history disclosures now start closed, including dialogue version lists. Each role shows its adopted current voice directly, or the latest successful current candidate clearly labelled 尚未採用; neither failed nor outdated clips are substituted for a current preview. The current preview is excluded from the history count and also appears inside voice settings. Missing audio has an explicit generation/import explanation. Same-project rerenders retain only user-opened voice histories and reuse audio elements with the same take/source URL.

Ada had no adopted voice but an existing matching 4-second / 48-kHz candidate hidden in history. This is now directly accessible, without auto-adoption. Browser verified both cards' histories closed initially, Ada's page/editor playback controls, Pip's missing-effect explanation and only manually opened Ada history retained on rerender. WAV endpoint returns 200 with valid bytes. Voice profile JS checks, app syntax and UI responsiveness checks pass; served files match disk. No database/profile/audio mutation, generation or restart. Evidence: `data/acceptance/voice-playback-20260913/verification.json`.

## Nonverbal character sound identities — live (2026-09-13)

The user clarified Pip can communicate entirely through electronic beep/boop sounds without human speech. Added `speech` / `nonverbal` to versioned voice settings and semantic default proposals. Nonverbal settings have no language or audition text; the editor hides and disables speech controls/Clone, shows sound-effect presets within the closed customization disclosure, and preserves independent drafts when switching modes. Voice design now chooses a communication mode from the character context instead of assuming a speaking voice. No dialogue is added to canon.

Nonverbal sound production currently uses explicit WAV import (0.1–30 seconds, PCM16), playback, adoption and the existing export path. Optional effect-reference attachments support the same short duration before profile save. TTS audition and dialogue endpoints reject nonverbal profiles before queuing; Clone is rejected for effect references. Speech validation, legacy hashes, selected takes and 2–30-second speech imports remain compatible. Changing mode invalidates old take selection without removing source files.

Pip was changed through a versioned save from v1 to v2: pure electronic beep/boop, no human voice/breath/words, expressive pitch/duration/spacing. Existing Pip speech candidates remain historical; Ada, other profiles and canon were unchanged. No new audio synthesized. 34 relevant Python tests and JS form/mode/lifecycle checks passed; live browser verified hidden language/audition/Clone, closed optional controls, mode roundtrip and effect import. Live TTS endpoint returned 400 for Pip. Idle restart via ./run.sh preserved all DB table hashes; final static refinement served without another restart. Evidence: `data/acceptance/nonverbal-voice-20260913/verification.json`.

## Image-derived identity drafts — live (2026-09-13)

The identity editor now previews its exact current entity image and offers 按圖片反推. New identity_from_image text-lane jobs use the configured vision provider and a validated, ownership-checked, hashed job-local PNG plus frozen identity/revision. Outputs are description/facts/uncertainties drafts, never canonical or approval writes; this capability also skips unrelated rejected-asset cleanup. UI supports review, explicit apply, undo, versioned save, closed-modal recovery, missing-image/voice states and stale-source rejection. New backend capability is gated in the UI until loaded.

20 initial backend tests, 15 cancellation/deletion tests and 33 final provider/queue tests passed; UI scope/identity/locale checks passed. Two actual DeepSeek trials used Ada pixels in an isolated database. The first exposed age drift/verbosity; revised prompt retains original age/profession context and requests concise drafts. Second trial retained adult lighthouse-keeper context; uncertain small details still require human review. UI apply/undo/save verified isolated revision 9→10; no production identity or assets modified. Local Qwen stopped after inspection; parent completed implementation.

Live Studio PID 2971971 started at 05:11:06 UTC with final backend sources already on disk; live capabilities and editor button verified, original production Ada description and revision 9 preserved. A concurrent task/user restarted Studio before this task attempted deployment; our guarded helper exited on the absent old PID without a signal, and no second restart was performed. Chapter-resume task was notified to verify and resume its original job. Keep the process stable during that acceptance run. Evidence: data/acceptance/identity-from-image-20260913/{verification,real-trial,deployment-final}.json.

## Chapter finalization resumes in saved batches — live, original chapter completed (2026-09-13)

The 天水圍 storyboard had 22 saved stages / 40 shots but three DeepSeek final outputs truncated while mapping 663 source units. Finalization now separates the edit order from coverage batches of at most 80 units, using versioned checkpoints beside untouched legacy attempts. Resume skips saved substeps; completed legacy finals retain their original contract. Global source/shot coverage and independent director review remain required. The UI prioritizes 接續剩餘整理 when shots are complete and removes the adjacent start-over action for resumable saved work. Full shared suite: 698 passed; Node staged-progress and app syntax checks passed. Live original job e2126da10989427c resumed through 接續剩餘整理 on PID 2971971 and succeeded at 13:19:55 HKT: 23/23 stages, 40/40 unchanged shots and 663/663 source units. Ten finalization substeps completed across 13 calls (three bounded structural corrections), without truncation. The original 22 checkpoints remain byte-identical and the adopted production remains revision 5 unchanged. Independent director review 0580e42dbe114fcb is running; completion is not adoption. Evidence in data/acceptance/chapter-final-resume-20260913/.

## Semantic character voice defaults — live (2026-09-13)

Replaced the client keyword/age matcher and universal human baseline with a durable `voice_defaults` text design job using the selected creative provider. It reads the full character description/facts, relevant performance context, story direction and dialogue language; produces a complete standalone voice instruction and explains design choices with validated quotations from the character. Machine, creature, crowd and silent roles are considered explicitly; no storyboard dialogue is added. Unknown or failed design remains empty with retry/manual editing, never a fabricated default.

Unsaved profiles reuse a source-matched cached proposal. Existing saved/adopted voices stay authoritative. The default is visible first; optional multiselect features remain collapsed. Loading runs outside the UI action guard, survives dialog closure as a durable job and cannot reopen a closed/replaced dialog. Changed sources invalidate cached proposals, duplicate requests reuse pending work, and same-provider grounding correction is bounded to one attempt with both outputs retained. Language remains separate from the reusable timbre description.

49 relevant Python tests and actual JS form/lifecycle checks pass. Real DeepSeek job `edb41eab0d8f4206` produced Pip's small maintenance-robot voice with restrained mid-low pitch, light synthetic/metallic resonance, clean articulation and steady pacing, grounded in Pip's original facts. Browser confirmed this default, collapsed options, English audition and both attachment modes; Ada's existing custom voice remained exact. No Pip profile was saved and no new audio was synthesized. Idle Studio reload preserved all database table hashes and integrity. Evidence: `data/acceptance/semantic-voice-defaults-20260913/verification.json` and retained provider jobs.

## Stale references are not review tasks — live (2026-09-13)

The Smallest Fix had three correctly stale keyframes after approved reference replacement, but the shared review selector counted stale as pending. Review selection now accepts only pending assets; sidebar/overview counts and review cards share that rule. Histories keep stale images, independently current pending replacements still surface, and stale cards no longer offer re-review as if it could change their inputs. Copy explains making a replacement using current references. No asset status, production or job mutations, generation or restart.

Asset-version and overview JS checks, app syntax, and 34 relevant Python tests passed. Live browser shows no review badge and 暫時沒有待審素材 for The Smallest Fix. All three stale asset IDs remain, production revision 9 retained; served modules match disk. Evidence: data/acceptance/stale-review-20260913/verification.json.

## Managed ComfyUI fault recovery — live and exercised (2026-09-13)

User explicitly authorized designing and proceeding with Studio→VRAM Manager fault recovery, including the narrow manager integration previously excluded by the general GPU-service boundary. Studio now owns a lifecycle watchdog, preflight recovery request, durable local status and original-receipt reconciliation. The manager owns a new /vram/comfy/recover operation: independently verifies the exact local dead prompt_worker and service PID/start identity, fences admissions including repeated worker leases, refuses unknown queued work or other granted GPU work, archives queue/history, stops only the identified ComfyUI service/children through its existing identity-safe helper, starts the same configured mode, then checks the new PID and HTTP/arbiter readiness. No global-release API, unrelated service stop, workflow downgrade or graph replay.

One durable attempt per process identity survives duplicate/lost HTTP responses. A second verified crash within five minutes latches that new incident against future automatic attempts, even after the window expires. Active LLM work blocks recovery; pending LLM/GPU requests stay queued behind the maintenance fence and are not cancelled. Stop/start/health failure pauses admissions with a durable failed outcome. A manager restart mid-recovery fails closed. Submitted image receipts stay byte-identical: archived completed history can be recovered; lost/interrupted prompts get sidecars and an explicit retry path. Never-started Studio jobs can continue. Generic external restart plus absent original prompt also terminates polling instead of waiting thirty minutes.

673 Studio tests, 100 manager tests, 15 focused manager tests after a fixture refinement, and three JS checks passed. Original offline test fixtures disable the real watchdog; dedicated mock-transport tests verify the actual Manager call, PID mismatch, unknown ownership, refusal, cancellation, lost response and receipt preservation. Real browser verified the original failed image now reports service restored/original job interrupted, while the cancelled image remains cancelled.

At 12:45:09, Studio's watchdog itself initiated operation 2044587-27953196. Manager replaced only the failed ComfyUI PID 2044587 with 2932623; ready in 30.55 seconds, unchanged Klein mode, empty queue and stale leases removed. A duplicate HTTP incident request returned the same completed operation without another restart. All nine local image recipes pass dependency readiness. No new image/media generated for acceptance; underlying HIP illegal-address recurrence is not claimed fixed.

Studio PID 2931532 was launched via ./run.sh. Its idle deployment and manager coordinator upgrade preserved granted leases using a short-lived joining reservation; the manager upgrade did not cancel requests or erase its durable state. Checkpoint: data/backups/managed-comfy-recovery-20260913-124450/. Original production JSON, assets, revision rows and both image receipts verified preserved. Concurrent project revision/voice activity means whole-database equality is deliberately not asserted. The complete pre-restart remote queue/history is archived by Manager and copied to acceptance. Evidence: data/acceptance/managed-comfy-recovery-20260913/{verification,deployment}.json. Contract: docs/MANAGED_COMFY_RECOVERY.md.

## Voice GPU admission and audition language — live, real generation passed (2026-09-13)

Ada failed take 2e0fa1f4eca84dec was refused at VRAM Manager acquire409 before VoxCPM2 loaded: dead ComfyUI PID 2044587 still owned GPU memory/lease. Concurrent task 定位排队卡住的工作 performed managed recovery to PID 2932623 and reloaded Studio; this task did not alter GPU services or force release. VoxCPM readiness now reads Manager admission state, and acquire409 retains a bounded reason with a readable message. Historical traces remain under 技術錯誤記錄.

New role defaults infer unambiguous canonical/project dialogue language. System audition templates follow language changes both in the form and profile save; custom text is untouched. Fixed Ada's saved English profile containing the built-in Cantonese sample by versioned save (v1→2); old failed request retained. Real v2 repair audition 00551e829e8b4dda succeeded at 48kHz / 4.00s. User then independently changed Ada to v3 女聲／童聲／中音 and submitted a9c94797978a419b, which also succeeded at 48kHz / 4.00s. V2 audio correctly became historical; no override, duplicate generation or automatic adoption. Pip's unsaved default now shows English with English audition text.

25 relevant Python tests and frontend checks passed; final admission subset 7 passed after tightening the error route match. Browser verified readable legacy error, English-to-Taiwan template switching and custom sentence preservation. Actual WAV hashes verified; listening/voice suitability remains for the user. Evidence: data/acceptance/voice-admission-fix-20260913/verification.json, saved before/after profile and both completed take receipts.

## Asset classification changes require identity-editor save — live (2026-09-13)

User found the direct public checkbox too easy to click. Image and voice cards now show only plain classification text (公共資產 or 場景資產 · 按需求自動歸類). Change public status through the existing identity/asset editor checkbox and explicit save; cancel discards it. Removed direct card save handler, listener and obsolete busy cleanup. Multiple public assets and automatic actual scene membership remain supported. Updated scope checks cover read-only cards, editor-only saving, multiple selections, independent removal and legacy preservation. Seven Python asset-role tests, grouping checks and JS syntax pass. Live browser verified plain card status, editor checkbox and unchecked-then-cancelled Ada remaining public. No production edits saved or restart. Evidence: data/acceptance/asset-scope-editor-only-20260913/verification.json.

## Compact canon image cards — live (2026-09-13)

Image cards now use a 160px contain-fit preview, compact name/type/status header and single-line review status. Public checkbox and four existing actions remain visible. Identity description, all facts and uploaded reference details are under a native initially closed 身份詳情與固定設定 disclosure; expanded identity details survive same-project rerenders. Cards align to the top instead of stretching beside a long neighbour. Live Ada/Pip cards measured about 461px tall without overflow; browser verified full description/five facts on expansion and retained open state after guarded navigation. Existing scope/group checks, JS syntax and seven asset-role Python tests pass. Served files match disk. No production edits or restart. Evidence: data/acceptance/compact-canon-cards-20260913/verification.json.

## Multiple public asset selection unblocked — live (2026-09-13)

Fixed a UI regression from the new scope checkbox: refresh rendered replacement checkboxes while guarded busy=true, but completion enabled only the detached original node. All current scope checkboxes now re-enable in the guarded finalizer. Public classification has no single-item limit. Regression reproduced the failure before the fix and verifies consecutive public saves, independent removal and failure rollback afterward. Six existing asset-role tests plus a new isolated API test pass (public counts 1→2→1, unchanged shots/scenes and approvals); asset-group and module checks pass. Live browser confirms all four controls enabled after guarded navigation; current Ada-only public selection was preserved. Served source matches disk; no production mutation or restart. Evidence: data/acceptance/public-asset-multiple-20260913/verification.json.

## Public asset checkbox with automatic scene membership — live (2026-09-13)

Canon image and voice cards now expose 設為公共資產 separately from the action buttons. Unchecking saves scene scope; the existing grouping derives actual membership from shot entity IDs and scene locations, with no user scene picker. The identity editor replaces three scope choices with the same checkbox, using effective grouping for legacy auto assets and preserving an unchanged legacy scope on identity edits. Revision-checked saves preserve image approvals; card failures roll back the checkbox and busy interactions do not submit. Existing four image actions remain. Six relevant Python tests, asset grouping checks and actual card/editor handler checks passed; live browser verified cards and editor. No live production edits, generation or restart. Evidence: data/acceptance/public-asset-control-20260913/verification.json.

## Attached voice references and Clone mode — live (2026-09-13)

角色音色 settings now accept optional persistent WAV attachments with playback, reference selection/detach, and two actual VoxCPM2 routes: timbre-only reference with text control, or Clone with exact reference transcript and reference+prompt audio. Upload creates an unselected reference record, works before first profile save, and never submits synthesis. Save freezes mode/reference/transcript into profile configuration; design auditions freeze reference identity/hash and mode. Adopted Clone auditions carry their actual audition text as the transcript for subsequent line cloning. Clone omits conflicting text voice/performance controls. Legacy no-reference hashes, selections and audio remain compatible. Current-reference source validation and export include attached original audio; no schema/table migration.

76 relevant Python tests and frontend checks pass. Isolated browser exercised actual WAV upload, preview URL, missing-transcript rejection, Clone save/reopen and timbre switching. Live browser verified attachment entry and both modes. Parameter routing is verified against installed VoxCPM and official API; no new GPU synthesis or speech-similarity claim. Upload test used a synthetic WAV only as a transport fixture. Current accepted format: 2–30-second 16-bit PCM WAV, up to 20 MB.

Idle-only restart after SQLite checkpoint started ./run.sh as PID 2912077. All table hashes unchanged, integrity ok, served static bytes match disk. No production profile/audio changes or GPU service operations. Backup: data/backups/voice-references-deploy-20260913-123239/. Evidence: data/acceptance/voice-references-20260913/verification.json and deployment-final.json.

## Simplified canon card actions — live (2026-09-13)

User requested only 製作新版, 編輯身份設定, 傳送到 ComfyUI, 在資料夾中顯示 on existing image cards. Removed standalone production-reference upload, finished-image import and copy-path buttons from this card action row; shortened generation/transfer labels and used a scoped equal-width two-column grid. Empty cards retain their initial generation label, and active jobs retain their disabled progress label. Existing action handlers and reference/generation workflows are unchanged. Live browser verified all four image cards in The Smallest Fix have the exact four labels and two equal columns without action overflow; served JS/CSS match disk. Module syntax, asset-groups checks and 11 relevant Python tests passed. Acceptance: data/acceptance/canon-actions-20260913/verification.json. No production changes or service restart.

## Complete voice default with optional customization — live (2026-09-13)

User clarified the intended UX: use a complete default voice normally and expand feature choices only when needed. The modal now leads with the full 角色預設音色 description; 自訂音色特色（可多選） is initially closed. Language/accent and audition text stay visible. Existing saved voice descriptions are retained exactly. New blank profiles receive explicit known character traits plus a complete neutral, clear-speaking baseline that can be saved without opening customization. Removed the prior prefill/undo actions; those historical acceptance entries are superseded by this layout. Custom changes update the full description preview; validation exposes hidden invalid description fields before reporting errors.

Actual form-handler checks and eight postproduction Python tests pass. Live browser verified full default, hidden checkboxes, visible language, expand and feature-selection preview. Twelve saved production profiles remain unchanged; served module matches disk. No restart or media generation. Evidence: data/acceptance/voice-default-panel-20260913/verification.json.

## Independent job queues and dead-renderer handling — live (2026-09-13)

The 11:58 ComfyUI HIP illegal-address failure killed prompt_worker while its HTTP/arbiter monitor continued reporting an active task. Studio's shared two-worker pool let that dead render and a chapter job block all text; local-image polling did not observe cancellation. Added bounded independent lanes (text 2, staged chapters 1, images 1), global queue position/project context in job views, cancellation guards around image submission/poll/download, short local HTTP poll timeouts, and a read-only exact-process journal detector for the installed dead executor. Confirmed failure blocks new image preparation/submission; original receipts remain recoverable and late cancelled output cannot be adopted. No global GPU interrupt or automatic media retry. Queued jobs retain IDs/frozen inputs across startup; running work is never automatically resubmitted, and persisted cancellations settle on startup.

Final relevant Python suite: 98 passed. Full suite initially 654 passed with three fixture mismatches resolved without weakening assertions (mark the simulated running stage accurately; mock the new preflight in offline renderer fixtures). Five JS checks passed. Real browser verified cancelled/failed image states, queue position and other-project blockers; the previously queued prompt, two video strategies and narrative candidate actually completed after deployment. No candidate adopted. Existing canon/asset/voice/video/settings tables and both image receipts were unchanged across the restart; all job IDs preserved, SQLite integrity ok. Checkpoint: data/backups/queue-recovery-20260913-122010/. Evidence: data/acceptance/queue-recovery-20260913/verification.json and deployment.json.

Studio PID 2892720 started with ./run.sh after old Studio exited following SIGTERM. The attempted follow-up SIGKILL found no process and was not delivered; no GPU process received a signal. No genuine text/chapter generation was running at the stop: a newly queued chapter continuation was preserved and dispatched on startup. The second image was reattached by original receipt for read-only recovery, then failed promptly on the confirmed dead executor without reposting. This restart also loaded the previously validated image-conflict repair source; its prior isolated acceptance remains under its own evidence directory.

Remaining: ComfyUI PID 2044587 is still unhealthy, with the original failed/running and pending remote prompts preserved. This task did not modify sibling projects or GPU services, as required by AGENTS.md. Local Qwen delegation was unsuitable while VRAM Manager retained the failed renderer's GPU leases. Journal detection is specific to a positively identified local prompt_worker crash; absent journal evidence does not prove liveness or diagnose every possible hang. Local cancellation ends Studio waiting, not the remote GPU task. Restore the GPU service through an authorized managed recovery before attempting new images.

## Character-based voice defaults — live (2026-09-13)

Empty voice descriptions prefill checkboxes from explicit character description / labeled voice facts. Existing saved profiles retain their text and selections. 按角色設定預填 replaces only the open description draft, with 復原預填 restoring previous draft; normal version-checked save makes changes durable. Age, explicit voice identity and stated pitch/texture/pace map to existing presets. Unknown traits stay unselected; physical appearance, relatives in facts, and scene dialogue delivery do not supply defaults. Crowd gender/age are not inferred. This is conservative form assistance, not a creative-model voice design service.

Frontend checks cover actual submit/apply/undo, new-profile auto-prefill, legacy identity, negative/unknown cues and selected-value roundtrips; eight Python postproduction tests passed. Live browser verified 陳樂言 → 年輕成年 prefill and exact undo. All 12 live profiles unchanged, served module matches disk. No restart or media generation. Parent retained this bounded UI/default decision after prior Qwen GPU-busy failure. Evidence: data/acceptance/voice-defaults-20260913/verification.json.

## Character voice selection controls — live (2026-09-13)

角色音色 now offers 30 checkbox descriptors across six groups, with optional custom prose, plus 英文 / 粵語（香港） / 國語（台灣） / 中文（大陸） language/accent choices. Existing string-based voice configuration and backend remain unchanged. Exact preset tokens reopen checked; arbitrary legacy prose and nonstandard language values are preserved, and unchanged saves keep the original description. Combined description validation handles empty values and the 1200-character limit.

Eight postproduction Python tests and the actual frontend edit/submit-handler checks pass. Live browser confirmed simultaneous selections, language switching, original prose and final accessible four-option dropdown. Live static files match disk; all 12 production character profiles are unchanged. No generation or server/GPU restart. Qwen could not execute because the GPU was busy; parent completed implementation. Evidence: data/acceptance/voice-options-20260913/verification.json.

## Image interpretation conflict self-correction — validated, deployment pending (2026-09-13)

Huang Tai job 549be70f5a174f62 stopped before rendering because DeepSeek misread a current-image crop defect as a desired output constraint. Brief policy v6 distinguishes observations from desired changes and performs at most one same-provider conflict re-evaluation using the original request and frozen pixels. Original and correction candidates remain separate. Exact request/provider/candidate/pixel identity and completion hashes guard reuse; incomplete attempts never resubmit. Cancellation, schema, reference, target, compiler and genuine-conflict gates remain. No media retry or provider downgrade.

118 relevant tests passed; isolated real DeepSeek using the original frozen vision model corrected the actual misreading in 13.4 seconds and retained a deliberately genuine conflict in 27.48 seconds. No production media generated, adopted or restored. The missing, subsequently purged edit-target reference was read from a hash-identical backup solely for isolated acceptance. Original failed job and remaining evidence stay unchanged. Local Qwen's tool session was interrupted; parent stopped its own orphaned incomplete worker and completed the code.

Studio was briefly offline, then another task started PID 2831248 before this fix was complete and resumed storyboard e2126da10989427c. Deployment awaits a safe idle restart; do not stop that user job. Additional image work continues to arrive. The read-only idle observer was stopped; no background restart or automation is scheduled. The guarded deploy_idle.py acceptance helper refuses active work and creates a fresh checkpoint before any restart. SQLite snapshot already saved, but refresh the checkpoint immediately before deployment because user work continues. Evidence: data/acceptance/image-conflict-repair-20260913/verification.json and deployment.json.

## Formal editorial integration — live (2026-09-13)

「剪接與預覽」 now supports every existing Scene and formal editing after adoption. User had already adopted the one-scene trial into production revision 5 (33 Shots); this integration preserves that exact plan and all assets. Fixed adopted-is-stale, duplicate adoption, atomic plan/audio/mapping/history saves, unrelated-scene merge protection, current full-plan prompt/image output, independent project-offset audio export and frozen directing-review annotations. Old→new source Shot/frame mappings are traceable; stale media/voice takes are not rebound to split Shots. Direct links select project and exact Shot; normal project/Shot changes update the URL so reload cannot restore a stale deep link.

Full Python suite 623 passed before the final additions; final editorial/timing/directing subset 52 passed, including the new actual ZIP and selected-voice mapping tests. Unified-video and directing JS checks pass. Temp browser verified formal revision 2→3, edit 1.5→1.8, 53.5→53.8 seconds, one saved history, and exact non-first Shot navigation. Real browser confirms formal revision 5 / 9 cuts / 53.5 seconds and seven-scene selector.

Initial deployment refused to stop active work. After the conversation interruption, neither the server nor the waiting deployment process remained alive and port 4760 was closed; cause was not established. Backed up again, migrated only editorial_scenes sidecar, restored ./run.sh (PID 2831248), and resumed the original interrupted user storyboard job e2126da10989427c via its existing resume endpoint. No duplicate job or plan adoption. Live production and asset hashes unchanged, SQLite integrity ok, HTTP static bytes match; receipt data/acceptance/editorial-formal-20260913/verification.json.

Current creative review is not passed: user-run review a0c87a097b474937 returned revise, including pilot stop/reveal position conflict and original chapter dialogue/cut issues. It predates the new independent-audio review input. The continuing new chapter proposal remains separate from formal revision 5. Actual footage trimming/mixing and generated-pixel evidence remain unverified. See docs/EDITORIAL_MAPPING_AUDIT.md.

## Clarify directing-plan labels — live (2026-09-13)

User found the expanded directing section unclear. Renamed it 劇情表達與剪接安排, explained what scene intent records, replaced legacy missing-plan wording with 已有分鏡 · 尚未補充拍攝意圖, and distinguished saved-but-unreviewed intent. Scene statuses now appear on separate rows; the nested heading describes scene intent instead of repeating the outer title. Existing actions and formal production/review data remain unchanged. Twelve directing Python tests plus the existing directing UI and overview integration checks passed; live labels and static bytes verified. Evidence: data/acceptance/directing-labels-20260913/verification.json.

## Portable Continuity Studio Skill and recovery backup (2026-09-13)

User requested packaging the system's production method as a Skill and backing it up. Portable continuity-studio v1.0.0 is authored under skill-packages/continuity-studio/ and installed as a full copy at /home/navievroom/.codex/skills/continuity-studio/. It contains mandatory methods 1.2.0, attributed shot/keyframe/director/editorial references, four exported schemas, mode-specific H3 handoff, standalone source/canon/review instructions and a standard-library package verifier. It is an instruction package, not a replacement rendering backend or a second runtime Studio extension. Runtime methods, providers and adopted productions are unchanged.

Skill-format validation, 60-file digest verification, unrelated-directory relocation and deliberate corruption rejection passed. Final acceptance: 68 relevant tests passed; backup verified 4,369 regular files and 13 relocated internal symlinks (2,207,567,614 bytes), with consistent SQLite integrity and all table contents matching its transaction checkpoint. The Skill ZIP was extracted and verified. One storyboard job was in flight and is preserved as such; its bounded diagnostic files are not completed outputs. Independent standalone forward test produced a faithful 12-second scene without app/data/media tools; parent verified its actual output. Recovery checkpoint destination: backups/Continuity-Studio-0.1-with-skill-20260913-112607/. Backup includes source/workflows and current data with an online SQLite snapshot; excludes older backups, reproducible caches, credentials, transient logs and external compute dependencies. See that directory's RESTORE.md and data/acceptance/skill-package-backup-20260913/verification.json for final verified status and exact exclusions. Preserve original workspace paths when restoring historical absolute-path job evidence. No automatic restoration or production generation requested.

## Task-first story overview — live (2026-09-13)

Authorized usability review implemented as static UI changes. 故事與章節 replaces 製作手冊 and 影片製作 replaces the main navigation's 影片準備. The overview opens with real image-review counts, unique approved-reference/keyframe totals, current adopted-video counts, active/manual/recoverable work and a five-step navigation path. Next actions use existing routes or the first unfinished Shot/frame; rendering itself never submits work or claims video readiness from image counts.

Outline chapter cards separate the adopted production and its chapter-scoped Shot count from the most recent generation job. Older attempts are collapsed with any still-active attempt disclosed; all active chapter attempts block duplicate development. Original chapter forms, action identifiers, job recovery and adoption semantics remain. Outline/source text, director styles and full directing/edit tables remain accessible under details; expanded overview details survive same-project rerenders. Revision form explains inputs, output and adoption. No backend/service restart or production generation was performed by this task. Other live work continued independently.

Acceptance: 19 relevant Python tests, five frontend check scripts, module syntax and focused whitespace checks passed. Browser verified real review navigation, adopted chapter continuation, revision dialog without submission, fifth-step directing expansion and preserved open details. Desktop and 390px layout inspected; mobile primary chapter action stacks without document overflow. Live static files match disk. Evidence: data/acceptance/overview-ux-20260913/verification.json. Local Qwen used its iteration budget without edits; parent completed the scope.

## Playable performance method — live (2026-09-13)

User requested improvement-only comparison with 文戏技能-表演推理.md. Studio already owns stronger production/canon/shot/edit/QC infrastructure; the selected addition makes supported objectives, stimulus/assessment/control/action, listener timing, frame-readable signals and residual performance explicit across planning, H3 Shot execution and text QC. Parent authored Studio-owned references/performance.md; mandatory bundle v1.2.0 routes it to nine relevant capabilities. No new schema, provider, duration rule, reference interview, confirmation flow or external runtime dependency. Existing style/genre choices and adopted source remain authoritative.

65 relevant offline tests and skill validation passed. Live /api/settings reports v1.2.0 with the matching hash; the uncached bundle loader applies the files without restart. No active jobs at the audit, no service/GPU changes and no live production/media mutation. Qwen was busy; parent completed the bounded wiring. Existing global method-hash freshness still applies: method-sensitive preparation/QC may require renewal, and historical staged jobs require a new proposal instead of cross-method resume (checkpoints remain retained). This is verified instruction integration, not a generated-performance quality claim. Evidence: data/acceptance/performance-method-20260913/verification.json.

## DeepSeek incomplete response — repaired and deployed (2026-09-13)

User job 8bd2f33791f84149 failed at the first chapter-writing stage after 60.88 seconds with HTTP RemoteProtocolError (incomplete chunked read), without result/checkpoints. Three earlier equivalent failures took approximately 61/121 seconds. These were premature response-body closures, not Studio's 900-second HTTP read timeout. The historical trace cannot identify DeepSeek versus an intermediary as the closer; neither parent nor observed Studio process had proxy environment variables configured.

DeepSeek chat now requests SSE streaming, preserves incremental content separately as provider-partial.txt, and writes safe counters/status/timing to transport.json. It handles keep-alive comments, UTF-8 byte boundaries, reasoning-only/usage events, and complete JSON responses without resubmission. SSE requires stop plus DONE; incomplete, truncated, cancelled and failed responses never become result.json. Complete output still enters existing schema/correction validation. Generic HTTP/image/Codex, provider/model, credentials and checkpoint contracts remain unchanged. No automatic retry/fallback. Historical errors have a clear Chinese explanation and existing continuation guidance. Cancellation/deadline checks occur between incoming lines and at completion; silent reads retain the existing bounded HTTP timeout.

80 relevant tests passed. A real tiny same-model stream succeeded in 1.31 seconds. An isolated call using the exact original 235,322-byte first-stage prompt and frozen deepseek-flash config succeeded in 84.89 seconds with a 38,042-byte schema-valid result containing seven scenes. This is transport/schema acceptance for one stage, not whole-chapter or creative-quality acceptance. Original failed jobs and adopted revision 4 remain untouched; no chapter resumed or output adopted.

Idle-only deployment backed up SQLite, started ./run.sh as PID 2752153, and verified identical content in every table plus integrity ok and live health. Browser verified translated historical errors and continuation controls. Local Qwen returned an incomplete draft at its iteration limit; parent completed and tested the implementation. Evidence, exact isolated output and backup path: data/acceptance/deepseek-transport-20260913/verification.json and deployment.json.

## Shots page regression repaired — live (2026-09-10)

The automatic-LoRA UI edit accidentally inserted imageLoraMarkup(input.local_image_plan,esc) inside shots(), where input is undefined. This caused every adopted-production Shots page to throw ReferenceError and show the misleading connection-error screen. Removed that stray call; LoRA provenance remains in image-version and job-detail contexts where job input exists. Static-only fix is live immediately; no server restart, production mutation or generation.

New scripts/check_shots_page.mjs executes the actual shots() renderer and reproduced the error before repair; it now passes missing/pending/approved-image, reference-action, no-plan and valid LoRA-provenance checks. Existing shot-reference JS checks and six Python shot-reference tests pass. Live browser loaded all 29 Shots of 下一站：長洲 revision 4 and switched from empty Shot 01 to existing-image Shot 02 with its reference and review controls intact. Live app.js equals disk. Evidence: data/acceptance/shots-page-repair-20260910/verification.json.

## Automatic local image LoRA selection — deployed (2026-09-10)

New local image jobs (v6) automatically choose up to two installed, reviewed, compatible LoRAs during the existing routed image-preparation call. Decisions cite the actual still-image brief; program validation resolves fixed strengths/triggers and freezes the full catalog/selection into the job. No suitable match returns none. Functional Turbo/head/identity adapters and original source assets remain intact. UI shows choices/reasons; ZIP contains frozen plans, actual graphs and receipts. Owned catalog: workflows/local-images/loras.json. Contract and limitations: docs/IMAGE_AUTO_LORA.md.

Real Krea automatic generation first chose realism V2 but failed visible-blood quality. Same-seed baseline was also faint. Adding the automatically chosen, hash-verified local text-fusion delta at 1.0 plus realism 1.0 produced clear dark-red stains on intact cloth with the exact same render prompt/seed. A subsequent full Studio job 5fc5bfb746ab4924 independently selected both and rendered the required stains. Blood adapter automatic scope is Krea Turbo text-to-image only, non-graphic fictional blood marks; other NSFW files and Muse/edit blood behavior remain unqualified. Real Klein job 416fc84e0d6a4f22 automatically selected consistency 0.6 plus existing Turbo 0.2, preserving cloth/tray/blood pattern for a subtle cooler-light edit. A real vector-icon/red-color-metaphor control chose no LoRA. All acceptance data and images are isolated; no user production adoption or mutation.

Full tests: 552 passed; final validation/export subset: 53 passed. Browser visibly verified real selection/name/strength/reason; live static bytes match. Local Qwen GPU admission was unavailable; parent completed the work without service changes. Idle-only deployment via ./run.sh replaced PID 1048296 with 1832172; SQLite backup data/backups/image-auto-lora-20260910-171221/. Every table row unchanged; integrity ok. All real generation receipts, exact prompts, decisions, visual findings and hashes: data/acceptance/image-auto-lora-20260910/verification.json.

## Resumable chapter generation — deployed and real acceptance complete (2026-09-10)

New automatic chapter narrative/storyboard jobs use chapter-stages-v2: verified adopted writing/canon reuse when source provenance matches, otherwise writing generation; per-scene outlines; batches of up to three full shots; then global edit/source assembly. Immutable attempts, atomic input/result-bound checkpoints, explicit same-job resume and source/cancellation/duplicate guards preserve completed work. V1 staged jobs remain resumable under their contract. The Codex adapter now uses file-backed request stdin, fixing a reproduced slow-reader pipe stall. Neither historical timeout can be conclusively attributed to that local defect. Provider/model, authentication and GPU services are unchanged.

Real Astra job 04e0bf5c885b4c11 completed 23 stages (22 provider calls, no correction/retry) with seven scenes, 40 full shots and 40 edit decisions. Total generation took 3,651 seconds; the longest provider call took 233.85 seconds, below the unchanged 1,200-second per-call deadline. Story, screenplay, canon, title, logline and style exactly match adopted writing. Independent QC a4eadae41bda46d8 also completed: revise, six issues (28 beats pass, five revise, two uncertain), covering familiar-person recognition, hand/framing visibility, extra dialogue, water-duration legibility, photo-reading geometry and final music continuity. This is a generated candidate awaiting creative revision, NOT an adopted or QC-passed production. Original production remains revision 4. No media generated. Do not resubmit either completed job as timeout recovery.

Full v2 suite: 525 passed; later source-provenance guards: 44 relevant tests passed. Node progress checks and isolated browser failure/resume verified only unfinished stages rerun; raw-after-crash, source drift, cancellation, checkpoint mismatch, exact writing reuse and delayed-child full-input checksum are covered. Live browser shows 23/23 stages and 40/40 shots; cancelled jobs no longer imply an adoptable result. Diff whitespace check passed.

Final idle-only deployment on 2026-09-09 23:48 UTC started Studio via ./run.sh as PID 1048296. SQLite backup is data/backups/chapter-stages-final-20260910-074815/; every table row matched before/after and integrity is ok. All source-provenance guards are live. The real generation changed only jobs/events/sequence records versus its baseline. Both read-only watchers finished; no acceptance job remains active. Evidence: data/acceptance/chapter-stages-20260910/verification.json, completed-generation.json, live-outcome.json and deployment-final.json. Contract and limits: docs/CHAPTER_STAGES.md.

The first v1 acceptance 2e7145de072c495d returned a real 62,767-byte outline in about 10.6 minutes but entered correction over scope-only canon metadata; the agent cancelled its own correction, retained the raw result and refined v2. It was not a transport timeout and remains cancelled, not successful. Both historical user failures remain intact.

## Repeated chapter timeout — follow-up 2026-09-10

Retry e1bcfef408394036 also failed at 21:55:49 UTC after 1201 seconds with no result.json. Unlike the first failed attempt, its stderr contains no explicit WebSocket retry/idle-timeout entry; a shared transport cause is not established. Earlier 29-shot narrative job 238a7527ba2f44ed took 795 seconds and produced about 81 KB; a larger 218 KB director-style input succeeded in 90 seconds. Input byte count alone does not explain this failure. Whole-chapter generation plus the fixed total deadline is a likely sensitivity; the current completion-only logs cannot distinguish active model processing from a stalled request. No further production attempts or service changes were made. The preceding fix remains diagnostic wording only. Evidence: data/acceptance/provider-timeout-20260910/second-timeout.json.

## Location reference sublocation scope — deployed and verified (2026-09-10)

Fixed the reported 逐層聽 opening-frame preparation conflict without changing its nineteenth-floor target or twelfth-floor canon. Brief v5 / mandatory method 1.1.1 scope shared location references, Scene appearance notes and review to the exact sublocation/time. Required in-scene lettering survives compiler restrictions; actual same-place conflicts still block. Six new regressions pass; final character/scope suite 17 passed, plus relevant image preparation/input-reference/method/ownership checks.

Real Astra with all seven original images produces a nineteenth-floor brief with 19 and excludes the twelfth-floor scrape/stone/bin. A twelfth-floor control retains 12 and the dark scrape. Both pass preparation; canonical hash, original seven image files and live production are unchanged. No media generated or approved. Historical failed job remains intact. Idle-only deployment completed after the existing narrative job ended through its own provider timeout. No user job was cancelled. Backed up SQLite, restarted with ./run.sh, verified all table rows unchanged, integrity ok and live method hash matches. Backup: data/backups/location-reference-scope-20260910-055601/. Evidence: data/acceptance/location-reference-scope-20260910/verification.json.

## Chapter Astra timeout diagnosis — 2026-09-10

User-reported 第一章：天水圍 job be68e4e5efce402a hit the existing 1200-second Studio deadline. Its stderr records a WebSocket idle timeout and internal retry; events contain only thread/turn start, and no result.json exists. There is no saved chapter result to recover. The 168,601-byte request is a possible load factor, not a proven cause. The later user retry e1bcfef408394036 was still running at acceptance snapshot; no duplicate submission, cancellation or adoption was performed.

Corrected misleading timeout reporting: legacy text now distinguishes retained logs from a completed result, and new backend errors distinguish absent/empty results from nonempty files requiring validation. Existing cancellation, timeout, provider and validation behavior remains intact. 19 provider/cancellation/timeout tests passed; three Chinese translation assertions and live static-byte verification passed. Static translation is live on browser reload; backend reporting code is tested on disk but awaits a safe idle restart because a user job is active. This is a diagnosis/reporting correction, not proof that provider non-response is resolved. Evidence: data/acceptance/provider-timeout-20260910/verification.json. Local Qwen was busy; parent completed the bounded change without polling or altering GPU services.

## Formal director planning — deployed (2026-09-10)

New proposals now persist scene dramatic beats/directorial intent, one primary Shot purpose with visual carrier/readability/cut reasons, explicit generation duration and a separate reusable-source edit plan. An independent routed directing_qc job evaluates every planned beat with exact source evidence; pass is required for new adoption. Changed planned scenes require renewed QC before keyframe/H3 generation. Existing unplanned scenes remain honest legacy history; empty compatibility fields preserve historical keyframe hashes. Chapter merge preserves local beat links and namespaces edits; ZIP includes director plan/review and edit ranges with available selected source-video references. All trim ranges remain planned/unverified; no final video edit is rendered.

Full current tests directory: 489 passed; final directing checks 87 passed; final downstream/image/H3/workflow-ownership checks 95 passed. JS and skill checks passed. Root pytest collection found duplicate modules in the user's complete backup, so tests/ was used explicitly without altering backups. Isolated browser verified fractional range editing (1.6 → 1.7 seconds), review invalidation and two uses of one source. Real Astra created 4 source Shots (19 seconds), including Milo close-up and Chloe reaction close-up; planned edit is 16.5 seconds with only 1.5 seconds of the 4-second reaction source. Independent QC passed the normal plan after one evidence-format correction and rejected a deliberately unreadable wide reaction as UNREADABLE_CARRIER. Original outputs, correction and negative-case evidence retained. No media generated.

Idle-only Studio deployment backed up SQLite and restarted via ./run.sh. Every table's rows matched before/after; integrity ok. Live capability, policy and static bytes verified. Existing productions/assets/jobs/voices/videos unchanged. Evidence: data/acceptance/director-planning-20260910/verification.json. Details and legacy/manual limits: docs/DIRECTOR_PLANNING.md. Backup path is in deployment.json.

## Shot 01 text regeneration — review candidate (2026-09-10)

User requested only Shot 01 text in 下一站：長洲. Studio chapter-level Astra job d16b1d4ddbb2420a returned no result after over ten minutes; cancelled only this task (terminal state cancelled). Supervising Astra directly authored and reviewed a focused ten-second candidate, preserving original dialogue words, hand/prop ownership, original keyframe description and Shot 02 entry direction. Schema, timing and scope checks passed; other 28 shots and all canon/scenes/writing remain identical. Candidate is saved under data/acceptance/shot01-text-regeneration-20260910/ and populated in the user's Shot 01 edit modal, not saved/adopted. Live production remains revision 4; no media generated. Use reviewed-shot.json / reviewed-plan.json for durable recovery after checking current revision. Do not mistake the cancelled automated job for a generated candidate. Evidence: verification.json in that directory.

## UI response and modal correction — 2026-09-10

User reported lag, swallowed clicks and no backdrop dismissal. Frontend now coalesces overlapping refresh calls, guards obsolete project responses, renders page navigation immediately with existing state, permits close/navigation/Shot selection outside the mutation lock, and shows visible progress for pending operations. Backdrop clicks dismiss while inside-to-outside drags do not; Escape/close remain available and modal body scrolling is locked. Initial load has a visible status and parallel list/settings reads. Hidden tabs pause interval polling to avoid multiplying server load.

Backend project response reuses decoded job history within that single response only; no cross-request cache or stale production reuse. Live GET was observed at 10.129s and later 3.329s under existing server load. Direct Python state profile found 16 repeated job-history loads; new direct timings 0.148s/0.135s. 438 Python tests passed, including identical response/fresh subsequent read/cache-error cleanup checks. JS checks exercise coalescing, backdrop drag protection and close/navigation independence; existing H3/settings/advice/profile checks passed. Live browser verified backdrop and close button dismissal without a production mutation. Backend deployment timing is recorded in data/acceptance/ui-responsiveness/verification.json.

## Continuity Studio 0.1 — user-named release (2026-09-10)

User named the current application Continuity Studio 0.1. Recorded VERSION and updated browser title/sidebar/README. Static-only change; no service restart or production data mutation. This product version is independent of skill and workflow adapter versions. Current backup behavior is manual pre-update SQLite snapshots; Studio has no built-in recurring backup. The latest shared-method deployment snapshot contains studio.sqlite3 only, not original media or a complete application copy.

## Mandatory shared production methods — deployed (2026-09-09)

User authorized preserving Astra's established prompt-writing methods for every Studio provider. Added a Studio-owned, hash-pinned mandatory skill bundle routed across all 15 built-in capabilities, structured image fields with deterministic fixed constraints, and exact cross-provider reuse of matching new saved image preparations. Requirement/skill/source/reference-role/pixel changes invalidate reuse; failed renderer attempts can retain valid preparation. Settings and job details expose the method and original/reused prompt provenance; ZIP includes complete used-method records. Existing canon/adoption rules and Astra defaults remain unchanged.

Fixed both earlier outage-audit gaps: automatic image preparation now includes image_prepare-only extension instructions, and HTTP storyboard output is saved before validation for the existing single correction pass. 429 Python tests passed, including 16 new method tests; skill/JS checks, isolated DeepSeek+local browser reload, independent skill forward test and focused acceptance review passed. Real Astra prepared one existing prop prompt; an unsubmitted Krea2 graph received its exact text. No new media generation. DeepSeek tests used mocked HTTP; real creative quality and full Astra-free acceptance remain unverified.

Idle-only deployment checked jobs/voices/videos, backed up metadata and restarted Studio. Every row in every SQLite table matched before/after; integrity ok. Live browser shows the mandatory method and retains Astra profile. Backup: data/backups/production-methods-20260909-235255/. Evidence: data/acceptance/production-methods/verification.json. Details: docs/PRODUCTION_METHODS.md. This implementation supersedes the unimplemented findings in the earlier outage audit below.

## Workflow ownership — verified and required (2026-09-09)

User requires Studio to survive deletion of external ComfyUI workflow JSON. All three complete original workflow copies are regular files under Studio workflows/; image routing/graphs and H3 I2VA/FL2VA/REF2VA graphs are owned Python code. External manifest source paths are provenance only. Added a permanent AGENTS.md contract, workflows/OWNERSHIP.md and 13 offline checks that relocate the owned bundle and make external workflow reads/probes fail. All 9 image recipes and all 3 H3 modes build successfully. The test caught 4 missing core node schemas in Studio's archived snapshot; copied the live definitions into its owned archive. Combined relevant tests: 73 passed. No production logic, services, DB, external files or generated media changed. ComfyUI/models/custom nodes/VRAM Manager remain explicit execution dependencies. Evidence: data/acceptance/workflow-ownership/verification.json.

## Astra outage alternatives and skill quality audit — researched, not implemented (2026-09-09)

User requires full functionality alternatives and retained creative quality when Astra is unavailable. See docs/ASTRA_OUTAGE_RESEARCH.md for official provider research, all 15 built-in capabilities plus the installed extension, live readiness, failure domains and a proposed quality qualification plan. Read-only live settings still show Astra and no DeepSeek credential. No true DeepSeek creative or complete Astra-free outage acceptance exists in the reviewed evidence; local rendering samples do not establish end-to-end independence or equal quality.

Parent isolated experiments reproduced two gaps: automatic image generation loads image skills but misses image_prepare-only extension instructions in its internal preparation step; HTTP/DeepSeek storyboard failures do not save the raw result.json required for the existing correction pass. Evidence: docs/ASTRA_OUTAGE_VERIFICATION.json. Relevant existing regression suites: 108 passed (two existing dependency warnings). These findings are not fixed by this research. No production settings, canon, assets, services or credentials changed; no real generation submitted. Local delegated audit did not return completion evidence; parent completed focused inspection and reproduction.

## 2026-09-09 — Portrait padding removed, v5 deployed

Fixed the user-reported white blocks: portrait now generates at full 704×1152, replacing the 704×960 image centered on pure white. Real same-seed Klein output has no artificial top/bottom strips; full head and shoulder/chest remain visible. Thirty-four focused tests pass. Idle Studio deployment preserved every database table and passed integrity check. Old files/receipts/approved assets untouched. Evidence: data/acceptance/character-sheet-standard/demo-v5-full-height/verification.json and render.png.

## Klein/H3 runtime selection — deployed (2026-09-09)

Local images now reserve Klein mode and local videos reserve H3 through the existing VRAM Manager. Active work/paused admissions block switching; recovery never changes mode or resubmits. Existing external patches/launchers remain unchanged. Same-seed v4 four-view generation succeeds in managed Klein mode after black-panel failures in H3 mode; this validates the runtime profile, not smart-memory alone. Real sample: data/acceptance/character-sheet-standard/demo-v4-klein-mode/render.png. Minor clothing/framing continuity differences remain subject to review. Details: runtime-investigation.json and docs/LOCAL_IMAGE_WORKFLOWS.md.

# Current state — 2026-09-09

## Full Studio bug audit and fixes (2026-09-11)

Audited backend routes, frontend actions, generation/adoption mappings, video render handoff, postproduction and live UI smoke paths. Fixed the two application gaps found in the regeneration audit: adopted video blueprint changes now invalidate the old frame/prompt handoff until a replacement frame is approved, and generic retry now preserves `source_prompt` for Scene/Shot/video prompt jobs. Added pytest discovery isolation for the backup copy and fixed `scripts/check_image_reference_evidence.mjs` by injecting its imported helper dependency. Verification and regression evidence are recorded in [REGENERATION_MAPPING_AUDIT.md](REGENERATION_MAPPING_AUDIT.md). No production data, approvals or media were changed.

## Character four-view standard corrected to user's new example — deployed (2026-09-09)

User supplied a new standard that supersedes the earlier portrait/left/right/rear layout: LEFT TO RIGHT front full body, one image-left side full body, rear full body, enlarged frontal face/shoulders. First three body views share scale/head height/foot baseline; final portrait is wider, on continuous white without separators. Updated generation, vision review, UI description, import/export metadata and local Klein graph. Layout version four-view-v2; adapter studio-local-images-v4 builds a 2048×1152 sheet with 448px equal body panels and a 704px final portrait panel. Each later view conditions on the generated front full-body candidate. Identity, wardrobe, held-object and anatomical-side continuity remain review requirements.

The user's exact JPG is saved at data/guides/character-four-view-v2.jpg and workflows/local-images/reference/character-four-view-v2.jpg as layout-only evidence; old guide and all historical images/jobs/approvals are preserved. 97 relevant tests, JS checks, live ComfyUI schema validation and graph dimensions/order passed. Idle deployment checked jobs, voices and video queues; every DB table's rows matched before/after and integrity was ok. Live guide bytes/MIME and adapter version verified. No media generation or reapproval in this correction; the new layout's visual quality awaits the next explicit generation. Evidence: data/acceptance/character-sheet-standard/verification.json. Metadata backup: data/backups/character-sheet-standard-20260909/.

## Adopted prompt status uses pale green — deployed, static update (2026-09-09)

Extended adoption colors to the current H3 prompt editor, adopted/current preparation job notes and ready handoff notice. Green requires saved text matching the current source hash with no unsaved draft; drafts and outdated sources retain attention colors. A newly generated but unadopted candidate remains distinct from the adopted editor. Original frame-issue notes remain visible. Live Shot 01 prompt job ec368c1d4043451b was confirmed adopted/current, and rendered output uses green while a simulated unsaved edit stays amber. Existing workflow/editor checks and syntax/diff checks pass; served files match disk. No saved prompt, adoption, job or media changes.

## Local Klein / Krea2 image alternatives — available (2026-09-09)

Archived the exact two user-named workflows and runtime inventory under workflows/local-images/. Added image-only comfy_local provider with separate overall/per-job renderer selection, structural workflow routing, frozen model/recipe/seed/reference provenance, managed ComfyUI queue submission and same-prompt output recovery. Astra creative/image defaults remain unchanged; DeepSeek profiles can explicitly pair with the local renderer. Source-first reference order is renumbered before both vision preparation and rendering; all selected inputs remain accounted for. Recipes cover text-to-image, reference images, portrait, sheet, face transfer and explicit rectangular edit/outpaint with capacity guards.

Real Krea Turbo text generation and a browser-submitted Krea Muse edit passed; the edited asset returned pending with passing Astra review and matching input hashes. Klein one-pass sheet failed view direction; separate-view v2 had a noisy rear. Final v3 uses a square portrait, distinct side views and side-conditioned rear at 16 steps/view, composing one 2048×1024 candidate. Its real output shows all four views; fine garment continuity remains a review concern. Two-input Krea, face transfer and mask/outpaint have graph/schema/regression checks but no branch-specific real visual acceptance. Sole-source Krea masked operations do not accept arbitrary multi-reference frames.

348 Python tests passed (two existing dependency warnings), relevant JS checks passed, real same-prompt recovery retained the original ComfyUI id/token. Browser verified Astra + local and DeepSeek + local profiles in isolated 4762, actual generation submission, candidate review and provenance. Production 4760 already served current v3 and matching static files when checked; task did not restart it or change its settings/assets, and preserved an active user job. Evidence: data/acceptance/local-images/verification.json. Details and limits: docs/LOCAL_IMAGE_WORKFLOWS.md.


## Approved imagery uses pale green — deployed, static update (2026-09-09)

Ready approved opening/ending frame containers now match the pale green reference cards, including image letterboxing and retained review-note background. An approved frame with advisory issues reads 已批准・保留審查意見; the original issues remain visible. Canon and asset/version cards also use pale green only when the displayed asset status is approved. Pending/stale states retain their existing treatment. No approval/review/data changes or service restart.

Existing video-workflow, image-mapping and generation-status JS checks pass, as do syntax/diff checks. Served static files match disk. Live Shot 01 browser verification confirmed the approved frame and retained-review label with all three existing issues, then the private verification tab was closed.

## User cancellation of running jobs — deployed

Active job rows and shared image progress panels now offer cancellation. Queued/manual jobs cancel immediately; running jobs retain running state with an explicit cancellation-request label until execution ends, preventing duplicate admission and false claims that work has stopped. A durable per-job marker prevents late results/reviews from becoming assets or succeeding. Codex checks the marker every second and terminates only its isolated process group; preparation checks stop before rendering. Remote/other providers may finish their request, but returned results are discarded. No global GPU service interruption.

10 cancellation/provider tests passed, followed by all 3 cancellation tests after adding the isolated-process termination check. UI status checks and syntax pass. Deployed after existing generation finished and both queues were idle. No user job cancelled by the agent. Evidence: data/acceptance/job-cancel/verification.json.

## Shared image progress mapping — deployed

Image progress now maps canonical target IDs across reference cards, opening/ending frames, asset/version review cards, character/location cards and lightbox/version modals. Video preparation previously omitted status; it now uses the same resolver and job links as storyboards. Existing approved images remain identified as old versions while replacement generation runs. Reviews resolve from the latest image back to the same target. Target-bound generation controls disable while active.

Modal/input focus no longer prevents progress updates: a read-only poll patches only status slots and relevant buttons, preserving the form, draft and image. Normal page refresh remains in place. Existing generation/video-workflow regressions and new image-mapping checks pass. Live running frame 28ef05efab4e42b8__fr01s was verified on both video preparation and storyboard pages with matching work status and progress links. Frontend-only deployment; no job submission, media generation, approval or service restart by this change. Evidence: data/acceptance/image-progress-mapping/verification.json.

## Recoverable project deletion — deployed and verified (2026-09-09)

作品管理 now has per-work 刪除作品, an impact warning, exact-title confirmation, server revision/work-queue guards, and 已刪除作品 with restore. Data/files remain intact; deletion does not free disk space. New nullable deleted_at migration and guarded write routes prevent edits/generation against deleted work. Browser selection and remembered/current-project refresh handle removal. A health capability flag keeps controls unavailable until the backend update is loaded.

333 full-suite tests passed (two existing dependency warnings); nine new deletion tests cover confirmation/version guards, all active job/voice states, other-work isolation and exact row/file restoration. Browser on isolated 4761 verified wrong-name disable, cancellation, current-work deletion, trash reload/restore and other-work deletion preserving current selection. Qwen stopped at its 24-call limit without validation; parent completed and tested directly. Production restart waited for the user image job and both queues to finish, then backed up and restarted successfully. All rows in 10 production tables match the backup (3 projects, 143 jobs, 26 assets); SQLite integrity is ok. No live project was deleted. Backup: data/backups/project-delete-20260909-222740/. Evidence: data/acceptance/project-delete/. Final focused deletion/folder tests: 15 passed.

## Rejected images are discarded — deployed (2026-09-09)

User explicitly requested that rejected review versions not remain stored. Rejection now deletes an unreferenced original, its sidecars, verified matching generated output and known completed-job reference copies/transport boards. Text job/decision history remains. Selected uploads and other versions are protected. In-use/shared/settings references block deletion with an explicit response reason; rejected dependency chains are drained leaf-first, with retries when project jobs finish/fail or queued/manual work is cancelled. Existing separate delete control remains a retry for protected leftovers. Deleted version paths are reserved through deletion events and are never reused by new generated images.

297 tests passed (two existing dependency warnings), including immediate reject deletion, active-job deferral/cancel cleanup, rejected chains, copy removal, unrelated output protection and version path reservation. Static syntax/version checks pass. Idle-only deployment preserved all rows. Then, under the user's explicit deletion request, the current 下一站：長洲 project's 9 existing rejected versions and 69 verified originals/copies/sidecars were deleted through the live API; 25 other asset rows/pixels and all projects/revisions/jobs/voices/settings remained identical, integrity ok. No rejected versions remain in that project. Other projects' historical rejected versions were not included in this cleanup. Evidence: data/acceptance/rejected-image-disposal/. Metadata-only DB backup: data/backups/rejected-disposal-20260909/; no new image backup was created.

## Regeneration mapping audit — findings, no application changes (2026-09-09)

Audited generation/retry/adoption paths including unified video preparation. Retained live image job-to-asset mappings: 34 checked, zero project/target/job mismatches (read-only SQLite). Confirmed two isolated reproductions: adopting a changed blueprint in the same mode leaves the old frame/prompt ready for handoff; generic failed-job retry drops source_prompt for Scene/Shot/frame-conditioned prompt regeneration. Not fixed in this investigation. Relevant suites: 126 passed, one older asset-library rejection-sidecar expectation failed against current deletion behavior; four JS checks passed. Details and scope: [REGENERATION_MAPPING_AUDIT.md](REGENERATION_MAPPING_AUDIT.md). No live generation, adoption or canon edits.

## Project management navigation — deployed, static update (2026-09-09)

Added the always-visible 作品管理 menu entry and reloadable #projects route. Its page lists all works with title, brief, revision and current-work marker, plus existing open/create/import actions. Returning to the list retains the current project context.

Validation: JavaScript syntax and unified-video checks passed; 17 production tests passed (two existing dependency warnings). Live 4760 browser verified three cards, opening the current work, returning through the menu and active-state/layout. Served app matches disk. No production data changes, generation or service restart. Evidence: data/acceptance/projects-menu/verification.json.

## Older candidate reminders after newer approval — fixed, static deployment (2026-09-09)

Review grouping previously selected the newest pending/stale row even when a newer version of that same target was already approved. Shared needsAssetReview now suppresses candidates at or before the current approved version's creation time; newer candidates still surface and a stale adoption without a current approval remains actionable. The sidebar badge, review cards and canonical character/crowd pending prompts use the same eligibility rule. Version lists and statuses are preserved; no auto-rejection/deletion/approval. This uses version chronology, not the time the user clicked approve: deliberately approving an earlier version still leaves later candidates reviewable.

Verified status/age/target combinations and unchanged input rows in the asset-versions JS regression; source/progress checks and syntax/diff checks pass. Live served static files match disk. Replayed the screenshot's old pending staircase candidate in memory against the actual newer approved version: no reminder, all six versions retained. No database mutation or service restart. Evidence: data/acceptance/review-version-cutoff/verification.json.

## H3 Director all-in-one workflow — archived and researched (2026-09-09)

User requested saving the exact Downloads `MiniMax+H3+导演台全能工作流 (1).json` and investigating usage/fit before integration. Byte-identical original archived at `workflows/minimax-h3-director/original/` with SHA-256 `fa40539a002264b292e6bcb990bc8d2f3788f90f4cadcaf6c67f2a726aa925be`, source/version manifest and pinned upstream research files. See `docs/H3_DIRECTOR_WORKFLOW.md` for usage, mode/model mapping, pack schema and staged acceptance.

Exact-file inventory found an r2v task with an active fl2va UNET (incompatible pairing), three missing example images (runtime /view returns 404), and bypassed Turbo/attention/refine branches. Listed model files and execution node types are present; Ref2VA model also exists. Runtime task options include mixed while on-disk task_modes does not, so loaded-version parity remains unverified. Research/archive acceptance passed; no claim that this original can run unchanged. Evidence: `data/acceptance/h3-director-research/verification.json` and `parent-review.json`.

The installed Director already supports `.mmxpack.zip`; its pack.py matches upstream f1f68bc9 exactly. Recommended next integration is a real per-Scene/model-family Director pack export from the unified video preparation page, then a separate explicit ComfyUI video provider with output recovery. Existing handoff JSON is not an importable workflow, and this archival/research task does not implement pack export or video generation. Installed 7de4a952 trails upstream by five commits, including Linux segment-output, reference-audio and continuity-padding fixes; plugin was not updated. No live canon, assets, prompts, services, authentication or GPU settings modified, and no generation submitted.

## Selected image references — vision preparation and design priority corrected (2026-09-09)

User's c_leyan job a252992c263641bf selected a stockier man in a green jacket/backpack, but the assembler attached the old slim gray-shirt identity as authoritative and told text-only preparation to use its old description. The generated image visually followed that old identity. Selection and high-fidelity marker were present; legacy Codex JSON events do not expose internal image-tool arguments, so a pure-T2I transport failure was not established.

New single-image-brief-v3 preparation sees actual reference pixels. Selected entity design inputs govern the proposed candidate ahead of older visual descriptions; the old same-target approval is omitted from competing identity/dependency inputs, unless explicitly selected as an edit source with subordinate authority. Style inputs retain their limited scope, frames retain approved identities and shot state, and approvals/canon are never silently changed. Both preparation and rendering use checked immutable pixel snapshots; mismatched reference lists or changed saved pixels stop execution. Review follows the same candidate design authority. Cards now show uploaded inputs as well as assets/edit source, and the high-preservation option honestly describes an instruction rather than a model fidelity parameter.

Validation: 292 full-suite tests and 35 final focused tests passed (two existing dependency warnings); actual Astra vision-to-text test recognized stocky build, green jacket, facial hair and khaki backpack and excluded old slim/gray-shirt appearance. JS reference evidence, prompt assembly, upload and progress checks passed. No task-initiated image generation or asset decisions. Deployed after both queues became idle; deployment snapshot/integrity evidence is in data/acceptance/reference-design-fix/. Production backup: data/backups/reference-design-20260909/. New render visual quality remains for the user's next generation/review; historical jobs remain intact.

## Unified video preparation — deployed, static update

影片準備 now owns the only navigation entry for per-Shot mode/material/prompt handoff. I2VA and FL2VA retain their existing frame review/editor; REF2VA embeds Scene shared references/global and the selected Shot's local references/prompt/guidance as two scoped tabs in the same workspace. The outer Scene/Shot list is the only Shot selector. Old #h3 and #h3-legacy links resolve to #video without changing adopted modes. Existing prompt settings, per-mode drafts, handoff receipts, regeneration/review/adoption, ComfyUI transfer and backend contracts are preserved. Ref2VA list readiness derives from its actual preparation/reference/issue/guidance state instead of the old always-pending external-workbench reason.

Validation: 17 Python video-workflow/delivery tests pass. Unified-video, workflow, guidance startup, Director handoff, prompt editor, Scene/Shot regeneration and prompt-assembly JS checks pass; syntax and diff checks pass. Isolated browser on 4761 verified same-page Scene Save, Shot dirty-copy guard, switching to FL2VA and back, draft survival after reload, Shot Save and successful copy receipt; API/ZIP contain exact saved strings. Exact OS clipboard-byte comparison was not established. Full-page screenshot inspected for layout. Live 4760 shows one sidebar entry and the existing locked I2VA choice intact. Test server/tab stopped. No live creative settings, assets or modes changed; no media generation or production-service restart.

## Prompt assembly boundaries — corrected, deployed and verified (2026-09-09)

Audited planning → image preparation → rendering/reference transport → review and H3/Ref2VA/frame-mode paths. New `image_prepare` text capability (Astra default, provider-switchable) interprets historical mixed style once and returns bounded target-specific visual fields. Renderer instructions contain only the completed single-image brief and relevant layout/reference/fidelity rules; preparation failures stop before media. Actual renderer input/hash is frozen before execution and used by asset prompt, sidecar and export. Codex tool instructions stay adapter-only; HTTP receives clean prose. Review includes uploaded inputs/edit source with fresh role indices. Matching Scene appearance/time takes precedence over legacy cross-scene light. Existing canonical dependency hashes, manual directions, assets and job history are preserved.

New planning keeps Production.style visual-only. One-Scene H3 refinement excludes full screenplay/unrelated scenes, per-Shot neighbor context is bounded, raw legacy H3 drafts omit hidden canon/biography and export under h3/drafts with copy disabled until prepared/refined. Base-mode dialogue is placed in the integrated timeline. Feedback/source conflicts no longer silently reuse a different pending image job. See PROMPT_ASSEMBLY.md.

Verification: final full suite 288 passed (two existing dependency warnings), JS prompt-assembly/generation/editor/reference checks and syntax pass. Five real Astra text cases were reviewed: staircase, phone, character, opening frame and predawn corridor. Early v1 corridor preparation still borrowed later firelight; v2 adds matching Scene context and explicit time/location precedence, with the real corrected result retaining only skylight. No task-initiated image/video/audio generation. Browser verified distinct new-render versus historical-source labels and preserved review controls.

Final deployment checked both queues idle; all 3 projects, 7 revisions, 30 asset rows, 129 jobs, one chapter, 12 voice profiles/takes and 13 settings rows were identical immediately before/after restart, SQLite integrity ok. Concurrent live image/approval/rejection activity occurred during development; it was preserved, not rolled back to the older task-start snapshot. Evidence: data/acceptance/prompt-assembly/verification.json; backup: data/backups/prompt-assembly-20260909/. Old mixed source and old rendered outputs remain history; new requests use the corrected path.

## Grouped asset review and explicit rejected-version deletion — deployed

Review room and all-assets view now show one card per target with a version list; review selects the latest pending/stale version and the sidebar counts groups. Each version remains individually inspectable. Only rejected versions expose deletion, with an explicit permanent-delete control. DELETE /api/assets/{aid} guards asset references/shared paths, active jobs and persisted settings, constrains paths to the asset library, stages files for database rollback, deletes original/sidecars and refreshes catalogs while retaining job audit history. User explicitly authorized a deletion capability, superseding the blanket preservation default for this scoped action; no live version was deleted by the agent.

Two deletion tests passed (approved protection, reference protection, actual temporary-file removal, settings/active-job guards), plus version-grouping and syntax checks. Live browser verified one staircase review card, version selection and the rejected-version delete dialog, cancelled without deleting. The user concurrently rejected the remaining candidates during validation. Studio restarted only while both job and voice queues were idle. Evidence: data/acceptance/asset-versions/verification.json.

## Multiple production-reference upload — deployed

The shared generation/upload drop control now accepts multiple files through picker or drag/drop, accumulates selections with duplicate protection, previews each image and allows removal. Batch save uploads sequentially through the existing endpoint, shows progress and removes successful files from the pending batch so failures can be retried without duplicating completed uploads. Saved references remain individually selectable in generation, preserving existing feedback and fidelity drafts. Saving does not generate images. Multi-file control regression and existing input-reference Python tests passed; live browser confirmed multiple picker and copy, cancelled without upload.

## Visible regeneration on review cards

Image review cards now show target-specific regeneration status (queued/running/manual/failure), spinner, elapsed time and a job-details link, explicitly identifying the displayed image as the existing version. Revision is disabled while a replacement is active. Existing periodic review-page refresh updates progress. No generation or asset decision triggered by display.

## Mode selection layout — deployed

Saved Shot modes now display locked with grey disabled alternatives. Recommendation reason appears inline, explicitly distinguishing pending adoption from adopted strategy. Review opens detailed evidence/blueprints; regenerate opens AUTO by default with optional fixed I2VA/FL2VA/REF2VA and feedback. The chosen constraint is frozen in jobs, included in request deduplication, preserved on retry and validated before adoption. Current settings remain until adoption.

8 workflow plus 1 choice regression tests passed; workflow JS checks cover lock/pending states. Live browser verified both locked Shot01 and pending Shot02 plus the default/selectable regeneration form, cancelled without submission. Waited for existing image generation to complete before an idle-only Studio restart; live OpenAPI confirms choice support. No generation/adoption by this change. Evidence: data/acceptance/strategy-layout/verification.json.

## Shot reference images in frame preparation — deployed

Reference cards now use pale green backgrounds, green borders and a ✓ 已批准 pill for ready approved assets. Other states use pale amber with an ! status pill, retaining explicit status text. Styling follows readiness rather than an unchecked approval label. Syntax and existing workflow checks pass.

Section 2 now lists the selected Shot's canonical visual references before its keyframe: thumbnail, role, readiness, view/approval, generation form and import. It uses the same visual dependency context as frame rendering, omitting unrelated Shot props and voice-only identities. Current approved images take precedence over newer pending candidates; stale hashes and missing files are labelled honestly. Missing references are summarized before the user attempts keyframe generation.

14 existing workflow/reference tests and one new scope/status regression passed, along with workflow JS and syntax checks. Live browser verified Shot 02 has six references and only 住宅消防樓梯 missing. Its generate button opens the optional production form, cancelled without submitting; existing character reference opens review controls. No media generated or approval changed. Qwen could not start because GPU was busy (503); parent completed directly. Studio restarted only with job/voice queues idle; no GPU service change. Evidence: data/acceptance/shot-prerequisites/verification.json.

## Visible Shot duration — deployed

The video prompt section now shows the canonical Shot duration beside its heading and repeats the exact seconds to enter in the Director beside the copy controls. This is dynamic per Shot, not a hardcoded 10 seconds. JavaScript syntax and existing workflow checks passed.

## Complete copyable H3 prompts — deployed

The earlier conflict veto below is superseded: approved-frame generation now always returns a complete valid H3 prompt, with image concerns displayed separately as advisory warnings. Full candidates appear inline with adopt-and-copy; adopted prompts retain the editable copy flow. Missing/unapproved/stale frames, invalid structure/dialogue and stale drafts/candidates remain guarded. Exact alignment accepts a harmless missing blank line; dialogue tags must remain in the main description.

Live Astra job 58d3212eb39a42fd produced the complete 5099-character I2VA prompt for 下一站：長洲 Shot 01; the user concurrently adopted it. Browser shows alignment and all three official fields, and native paste after the copy button returned the 5099-character prompt. The blank feedback form used for paste verification was cancelled. No image approval or media render by this change. 24 focused Python tests passed before final formatting refinement, then all 8 workflow tests passed; workflow JS regression and syntax checks passed. Evidence: data/acceptance/h3-copyable-prompt/. Backup: data/backups/h3-copyable-20260909-201442/.

## DeepSeek creative provider choice — deployed, credentials pending

Settings now has an explicit Powered by Astra / Powered by DeepSeek profile. One atomic apply routes every creative capability, enabled skill and future capability default; per-capability changes display Custom. DeepSeek V4 Pro (or explicitly V4 Flash) handles text, and V4 Flash Vision Exp handles every request containing images, including review and frame-aware H3. No fallback to Astra. Jobs retain frozen provider configurations; existing results/canon are preserved. Generic creative roles/UI no longer misattribute DeepSeek work to Astra, and generated Scene provenance uses its actual saved provider.

DeepSeek does not supply image rendering. Its profile requires a separately selected non-Codex HTTP image renderer or explicit manual import; existing VoxCPM remains independent. Configuration stores only the credential environment variable name (default DEEPSEEK_API_KEY). The connection check lists available models without paid generation. The running service has NO configured DeepSeek key: actual DeepSeek generation remains unverified until credentials are supplied. Live mode remains Astra; the user requested adding a choice, not changing saved defaults.

259 tests passed, then 46 focused tests after the final provenance correction; 13 JavaScript checks passed. Isolated browser exercised DeepSeek apply/reload, missing-key check, custom override, and switching back. Live settings inspected visually after an idle-only service restart. All live table rows match the backup exactly: three projects, 25 assets, 115 jobs, 12 voice profiles/takes, revisions/events/settings unchanged; SQLite integrity ok. No media generated or GPU/auth/sibling changes. Qwen admission failed for insufficient free VRAM; parent completed the work without service intervention. Evidence: data/acceptance/deepseek-settings/verification.json. Backup: data/backups/deepseek-settings-20260909-191630/. See DEEPSEEK.md.


## Blank image approval note — fixed and deployed

Explicit image approval now accepts an empty/whitespace note even when Astra returned revise/uncertain or there is no review. Backend records an automatic human-adoption note while preserving the original review. The dedicated approval form displays the review and labels the note optional; it no longer reuses the misleading creative-feedback placeholder. Canon/reference freshness checks and the existing character-sheet acknowledgement remain enforced.

31 relevant Python tests and the approval JS submission/escaping check pass. Live browser verified the optional-note form and preserved review, then cancelled without changing any image decision. All original projects/assets/voices/settings rows match the deployment backup; one concurrent user image arrived during verification. Both work queues were idle before Studio restart. Evidence: data/acceptance/blank-approval/verification.json.

## Local Comfy MCP — installed and connection verified

Official `comfy-mcp` 0.10.0 and `comfy-cli` 1.20.0 installed in `/home/navievroom/.local/share/comfy-mcp/venv`; original shell comfy-cli 1.11.1 remains intact. Global Codex server `comfy-mcp` is enabled in `/home/navievroom/.codex/config.toml`, with absolute server/COMFY_BIN paths, COMFY_LOCAL_URL=http://127.0.0.1:8188 and telemetry disabled. Comfy's default workspace now points to the existing `/home/navievroom/comfy/ComfyUI`.

Actual stdio handshake passed in 2.47 seconds; 39 tools enumerated. `server_info` confirmed the live local server, `which` confirmed the persisted default workspace, and `nodes` returned the live KSampler schema. Optional freshness/update-status lookup timed out after 15 seconds; connection and node access passed. Evidence: `data/acceptance/comfy-mcp-setup/verification.json`. Reload/restart Codex to load the new tool connection into its session. No render submitted, ComfyUI/GPU service restart, source/model change or Studio canon mutation. Existing VRAM Manager and project generation rules still govern future execution.

## H3 frame preparation revamp — deployed and accepted

New 影片準備 (#video) provides per-Scene/Shot I2VA / FL2VA / Ref2VA selection, Astra source-grounded recommendations and frozen-frame blueprints, repair/import/review actions, explicit missing-endpoint creation, frame-aware English prompt generation, candidate comparison/adoption, persistent manual drafts and exact copy/JSON/ZIP handoffs. Original Ref2VA route/settings and scene-global/Shot regeneration stay intact. Missing/rejected/stale frames never silently fall back to text-only. Astra pair/source conflicts are separate Traditional Chinese repair issues and block adoption/ready copy/export. See FL2VA_WORKFLOW.md.

Final suite 237 passed (7 new API tests), 11 JS regression scripts and syntax checks passed. Live Astra strategy c5890b166d7b45b8 recommends I2VA for 下一站：長洲 Shot 01; parent reviewed/adopted its mode and blueprint. Browser verified pending-image gating and edit-form injection of the blueprint plus latest wrong-hand/blackout-lighting review issues. No images were approved or generated. Live projects, all 24 asset rows/files, 12 voice profiles/takes, voice lines and all prior settings match the backup. SQLite integrity ok.

There are currently NO approved live keyframes: Smallest Fix frames are rejected (despite earlier historical acceptance), and 下一站 frames remain pending. Positive prompt UI/transport was therefore tested in a clearly labelled isolated SQLite/image copy at port 4761, never by approving live images. Authentic Astra e9115a6177714f09 exercised draft/adoption/copy/ZIP equality and exposed cross-frame drift. Final policy validation used authentic bc3a70d8378b4291, which returned empty text plus concrete Pip-foot and lantern-base mismatch repairs; API/UI adoption, packet and ready export were blocked. Isolated approvals are fixture metadata only, not creative acceptance. Temporary acceptance server was stopped after both isolated queues were idle.

Evidence PASS: data/acceptance/fl2va-workflow/verification.json. Backup: data/backups/fl2va-workflow-20260909-022317/. Final Studio restart checked both live job/voice queues idle. No video render, speed benchmark, GPU-service change or sibling modification. No reliable evidence supports the pasted 10–30% speed claim or a fixed FL2VA quota. Frame readiness controls endpoints; intermediate continuity still needs output review.


## Guidance pending-state deadlock — fixed, deployed and exercised

H3 auto-analysis wrongly waited for every Scene's English preparation; Scenes 05–06 blocked ready Scene 02, while pending falsely claimed running. Removed that gate, marked unfinished Scene inputs for storyboard-first reasoning, and distinguished no-job/queue/running/stale-active/manual/failure states with start/retry/job-details controls. Older active analysis finishes before a new source is submitted; freshness and manual overrides remain enforced. See GUIDANCE_START_FIX.md.

Real browser auto-started Astra job d640995eb3fc4cc0 with Scenes 05–06 still unprepared. It succeeded with all 29 decisions: first=start, 27 cuts, Shot 12 uncertain about continuous final camera position versus tighter two-shot. Shot 03 is off: 35mm side medium-wide changes to 50mm eye-level medium close two-shot and pan reveal. Browser shows the specific recommendation/reason and evidence control; actual ZIP guidance matches. No prompts or delivery settings changed; all three projects, 23 assets and 12 voice profiles/takes preserved. Full suite 230 passed, entry-point/status and Director JS checks passed. Evidence data/acceptance/guidance-start-fix/verification.json; backup data/backups/guidance-start-fix-20260909-015028/. Service restarted only with both job queues idle. No media generated.


## Keyframe reference transport fix — deployed and live acceptance passed

Original Scene 01 frame job c44b833cf36942ef failed before generation because seven references exceeded the adapter's five-image guard. Explicit paths also have a five-input tool limit and hit bwrap read errors on this host. Final fix preserves first four originals and packs remaining originals into a labelled, native-pixel transport board; sends five conversation attachments through the existing num_last_images_to_include route. No original references dropped, no sandbox/GPU changes. New storyboard source no longer imposes a five-entity budget. See KEYFRAME_REFERENCES.md.

227 full-suite tests passed, then 24 focused checks passed after final attachment-route change. Safely restarted only with jobs/voice queues idle. Final real retry 5cad19b25f7d4363 succeeded for 下一站：長洲 / 28ef05efab4e42b8__fr01s, yielding actual 1672×941 PNG asset ae41ccbe954d4ed9. Browser shows the saved image and review/download actions; HTTP bytes match the original. All seven canonical reference IDs persist. Original 22 assets and three projects/voices unchanged. Astra review 8e7fe117e5684e83 requests creative corrections (jug in left hand; Chang inside doorway); image remains pending, no automatic approval or further paid render. Parent inspected actual frame and review. Verification is PASS in the evidence folder. Backup: data/backups/keyframe-reference-fix-20260909-013524/. Evidence folder: data/acceptance/keyframe-reference-fix/. Earlier path attempts 441769ad0a5c415d and a00ac24dc6804a27 failed without images; concurrent user retry 76d1728b7f194cb0 also failed under the old path route before final deployment.


## Scene global prompt regeneration — deployed and exercised

Scene 公共參數 now has 重新生成全域提示詞 with optional feedback and current browser-draft support. Persistent Astra h3_global candidates can be compared after reload and explicitly adopted; all effective Shot text is preserved, props remain Shot-local, reference mappings stay fixed, and stale source/draft checks prevent overwrite. See SCENE_REGENERATION.md.

Live Scene 01 candidate 813722c8b4244cf8 succeeded (1488 → 1419 characters), remains unadopted, and its comparison is open in the in-app browser. First attempt's Chinese location name was rejected; instructions now explicitly use English location names. An independent concurrent Shot edit changed dialogue to 「樂仔。」「行得。」; global-only validation now preserves such unchanged manual text while Shot issues still report canon differences. The second authentic output was recovered without modifying provider prose. Earlier Shot candidate c0b5c127388d437b may now be stale because of that edit.

Full suite 221 passed; final 24 focused tests cover the validation scope correction, including exact preservation of manual dialogue. JS Scene/Shot regeneration, Director/editor/references and syntax pass. All three projects, 22 asset rows and 12 voice profiles/takes unchanged; current global and Shot prompts match frozen generation input. No media generated. Evidence: data/acceptance/scene-regeneration/verification.json. Backup: data/backups/scene-regeneration-20260909-012648/. Every restart checked jobs and voice_takes idle; latest code is live.


## ComfyUI SaveVideo audio sanitization — verified on disk, restart pending

User explicitly requested patching the external ComfyUI SaveVideo AAC failure. Added CPU-side NaN/Inf replacement and [-1, 1] clipping with sample-count warnings in `/home/navievroom/comfy/ComfyUI/comfy_api/latest/_input_impl/video_types.py`; source tensor is preserved. No silent audio-track removal. New actual encode/decode regression tests reproduced error 22 in 8 mono/stereo cases before the patch; all 120 focused/new and existing video/bit-depth tests pass after it. Backup and acceptance evidence: `data/acceptance/comfy-audio-patch/`. Existing unrelated ComfyUI model edits preserved. No service restart, GPU work, generation or canon changes. Running ComfyUI has NOT loaded this patch; next safe restart is still needed. The failed waveform was not found in disk segment caches, so NaN/Inf is a reproduced failure mechanism, not proof of this specific waveform's contents or upstream root cause. Cleaning invalid samples cannot recover the original sound.

## Single-Shot prompt regeneration — deployed and exercised

Each H3 Shot now has 重新生成分鏡提示詞 with optional feedback, using the current browser draft when present. Astra produces one persistent, reviewable candidate; current direction is retained until explicit adoption. Comparison preserves the starting text, reload restores pending/completed status, and adoption changes only the selected Shot while preserving effective Scene/sibling prompts, media and canon. Source/hash and browser-draft checks prevent stale overwrites; complete six-section/dialogue/label validation remains active. See SHOT_REGENERATION.md.

Real browser submitted Shot 01 of 下一站：長洲 to Astra: c0b5c127388d437b succeeded, reducing redundant prose from 4875 to 3716 characters while retaining ten seconds, exact dialogue and established hand ownership. Parent reviewed the actual result and comparison layout. Candidate remains unadopted for the user. All three projects, 22 asset rows, 12 voice profiles/takes and WAV bytes match the pre-deployment backup. API adoption/export/idempotence and other-Shot preservation verified in isolated tests. Full suite 216 passed, final six focused tests passed after shared-name context and API status-hash corrections; JS controls/Director/editor/reference checks passed. Evidence: data/acceptance/shot-regeneration/. Backup: data/backups/shot-regeneration-20260909-011349/. All restarts checked both job queues idle; no media generated or GPU services changed.

## H3 per-Shot prop references — deployed and browser verified

Scene 公共參數 now excludes props from reference images and subject definitions. Each Shot has only its own canonical props, with local slots after the shared images, and leading prop definitions before summary. Scene 02 "折返" includes the jug and screwdriver, with no bucket reference; the other Shots that use buckets still reuse their original image. API/ZIP expose local_references and per-Shot missing references. Existing prepared Entity prose is reused with correct per-Shot labels; no screenplay, production revision or media changes. See SHOT_REFERENCES.md.

Verification: 210 full-suite tests passed, then six focused tests including explicit manual-text protection passed; Shot UI, Director and prompt editor JS checks pass. Browser verified common exclusion, three props in Shot 02, two props in Shot 05, labels and copy-success receipt. API and ZIP prompts/manifests match. All three project rows, 22 assets, 12 voice profiles/takes and original WAV hashes preserved. Backup: data/backups/shot-references-20260909-005758/. Evidence: data/acceptance/shot-references/verification.json. Service restarted only while jobs and voice_takes were idle. Browser subsequently triggered normal guidance analysis; do not interrupt any new active jobs.

Qwen's pre-crash UI worker survived, reached its 24-call limit and produced an inaccurate completion summary without the claimed files. Parent inspected actual results, implemented the remaining UI and executable checks and accepted the live result. No duplicate Qwen task or GPU intervention occurred.

## Public / chapter Scene assets and audio-only identities — deployed

角色與場景 now groups public identities above chapter → Scene references. 陳樂言 is explicitly public in 下一站：長洲 revision 4. Four non-visible speakers (官方廣播聲、民間廣播男人、求救母親、受傷孩子) are audio-only voice canon; their cards use existing audition/profile actions, with image generation/import rejected at the backend. Voices retain dialogue and sound timing while being excluded from visual reference budgets, missing-image requirements and visible speaker mapping. Radio equipment remains a visual prop. Scope changes preserve approved-image hashes and shared IDs. See ASSET_ROLES.md.

Live browser/API/export verification PASS: 24 visual identities, four voices, one public identity, seven Scenes; source, screenplay, shots, chapters, 22 existing image rows and all 12 voice profiles/takes preserved exactly. All 12 original 48 kHz WAVs match stored SHA-256 through HTTP. Other works unchanged; no media generated. Full suite 201 passed before final compatibility adjustment, then 20 focused tests passed, asset-group JS/locale/syntax checks passed. Evidence: data/acceptance/asset-roles/; backup: data/backups/asset-roles-20260909-003923/. Final scope-insensitive English-preparation compatibility and original-name preservation deployed together after both queues were idle. Live Scene 01–04 and 07 prepared directions are ready; Scene 05–06 correctly require updated preparation because audio-only kinds changed.

## VoxCPM2 postproduction voices — completed and exercised

New 後製配音 workspace provides persistent per-character voice profiles, real local VoxCPM2 design, WAV reference import, audition/adoption, exact shot-dialogue cloning, version/staleness protection, playable/downloadable originals, natural-duration overrun flags and selected-current audio/provenance in production exports. Profiles do not alter image canon or production revisions. Explicitly silent mouth-only dialogue is excluded. See VOXCPM.md.

Isolated real API/browser acceptance generated two 48 kHz voice references and two canonical Cantonese dialogue takes; adoption, playback and all four WAVs in exported ZIP verified. 177 tests passed initially, followed by all 12 audio tests after the silent-dialogue fix. 85 locale assertions, Director checks and JS syntax pass. Existing source, production and assets match exactly across live deployment. No active job was interrupted or GPU service modified. Local Qwen did investigation only; parent implemented and verified the feature.

Astra authored distinct audition profiles and completed all twelve real 48 kHz WAV candidates for the canonical voice/character entries in 下一站：長洲 (2.24–7.84 seconds each). These are audition samples, not added screenplay lines, and remain unselected for the user's listening decision. Crowd is one representative timbre; radio voices are clean sources for later effects. Live job IDs and evidence: data/acceptance/voxcpm/. Backup: data/backups/voxcpm-20260908-231824/. Do not rerun seed_character_voices.py to generate duplicates; it skips saved profiles. Per-line synthesis starts after voice selection. All 12 voice jobs succeeded, with distinct verified WAV hashes, no automatic selection and no remaining voice work. The final safe restart preserved all voices and source/asset data; live browser shows 12 audio players and the one mouth-only dialogue remains silent. data/acceptance/voxcpm/verification.json is PASS; character-auditions.zip contains readable-name listening copies. Future restarts still require checking all current job queues.


## Explicit Shot speech boundaries — 2026-09-08

Reviewed the user's 25-second `MiniMaxH3_Director_t2v_00010_.mp4`: two local CPU ASR models agree on the intended “Definitely broken.” at about 5.2–6.3 seconds and detect additional, inconsistently transcribed speech around 16–19.5 seconds, where canonical shot_03 has no dialogue. Frames show Ada opening her mouth. Embedded generation settings confirm Ref2VA and reference-previous enabled on shots 02/03; this alone does not establish the cause of added speech.

Ref2VA effective copy/export prompts now include a canonical, per-Shot speech/mouth direction for existing saved/provider prose as well as new drafts, without changing stored creative text. Speaking shots specify exact-once scripted speech, original language, timed speaker windows, visible-speaker articulation and non-speaking character behavior. Silent shots explicitly forbid speech and speech-like mouth movement while allowing specified nonverbal acting/breathing. Keyframe drafts and Astra refinement instructions use the same direction. Dialogue validators reject extra, duplicate, reordered, untagged or missing utterances. This does not guarantee generated lip sync or exact synchronization to a later replacement voice track; no media is generated and audio-mode settings remain user controlled.

Verification: 38 focused Python tests passed (two existing dependency warnings), including API/export propagation to older saved prompts, preservation of canon/settings and zero generated jobs, canonical timing and nonverbal exceptions. Current The Smallest Fix combined prompts are 6261/5882/6176 characters, all within the existing 7000-character limit with no validation issues. The Smallest Fix revised Shot prompts are LIVE via delivery revision 2 and browser-verified on Shots 02/03. General Python policy deployment awaits a safe service restart: another project’s narrative job 238a7527ba2f44ed was still running, so no restart was attempted. See docs/ACCEPTANCE.md; next maintainer must load these general rules after active jobs finish.

## DirectorSKILL content-led recommendations — completed and exercised

製作手冊 now offers 按劇情推薦導演. Astra compares 20 pinned DirectorSKILL methods against story, emotional/reveal beats, medium and timing, then returns three ranked options with exact source evidence, concrete camera/blocking/editing/light/keyframe methods and tradeoffs. User selection persists independently; one complete chosen module shapes the next reviewable proposal. Outline works continue chapter by chapter. Canon/source/user medium requirements take priority over stylistic prescriptions. See DIRECTOR_STYLES.md.

Live Rain demo `edb7da2991cb4a6c`, now revision 2: Astra recommendation `a6e3d3e054ad4747` returned Spielberg, Hou Hsiao-hsien, Fincher. Parent reviewed content-led reasons and mismatches; Hong Kong did not automatically select Wong Kar-wai. Browser expanded evidence, chose Spielberg and started storyboard `5a85052f182e4ea0`; first provider result passed without repair. Parent reviewed all four start frames, exact dialogue, hand ownership, reveal order and ending. Browser reviewed/adopted it and verified updated shot board and persisted applied-direction state after reload.

Actual changes: motivated blue-umbrella reveal after eye/hand direction, clear doorway depth, face/hand relationship, restrained reaction and 1.5-second ending hold. Original screenplay and canonical entities unchanged, all scene/shot/frame IDs preserved, four shots remain 9/7/7/5 seconds. Selected method's final-pose example correctly did not replace a start frame. No images/video generated. Other existing projects' title/source/style/revision/production match the pre-change backup.

Integrated verification: 162 Python tests, 85 locale assertions, JS syntax/Director regressions and diff checks pass (two existing dependency warnings). Eight director-focused tests cover source evidence, valid choices, cross-project/stale input rejection, single-module prompt, no automatic canon mutation, direction-change adoption checks, pending-job identity and export provenance. Applied direction is recorded in the adoption transaction; success alone cannot masquerade as adoption. `data/acceptance/director-style/verification.json` PASS, with real recommendations, proposal, downstream image prompts, project state and ZIP. SQLite integrity ok. Backup: `data/backups/director-style-20260908-213436/`. Deployment was coordinated with the outline task; no active job was interrupted and no GPU service changed.

## Outline-first works and ongoing chapters — deployed

Creation now defaults to 故事大綱 and offers six visual-medium cards plus Astra/custom, separate aspect ratio and notes. Story outlines are editable independently. Users can add, edit, develop, review and adopt individual story chapters within one work, then enter its shots. Astra requests include the series outline, prior adopted writing and shared canon; adoption preserves other chapters and rejects stale inputs or canonical redefinitions. Per-chapter original story/screenplay, chapter ownership and aggregate production are revisioned/exported. Legacy projects and the Director Scene → Shot/global model remain available.

Browser acceptance on isolated storage at 4761: created an outline work, selected 2D/9:16 with notes, added two chapters, reloaded and verified outline/style/both chapters persisted. API regression covers chapter append/revision, exact screenplay preservation, shared approved references, stale outline/draft/production conflicts, cross-project rejection, revision restore, exports and >40 aggregate shots. No generated media or test placeholders were inserted into live canon. Deployment backup: data/backups/outline-chapters-20260908-213957/. Live existing project core fields and asset records compared equal before/after restart; integrity ok. Local Qwen supplied the isolated visual picker module; its functional check passed, while the bridge protected-file check flagged concurrent parent/other-task edits, so parent reviewed and browser-tested the module. Final suite: 162 Python tests passed (two upstream warnings); 85 locale assertions, JS syntax, Director regression and diff checks passed. See OUTLINE_CHAPTERS.md for acceptance details and limits.

## Story / screenplay import → storyboard — completed

Top-bar 貼上／導入劇情 now accepts pasted text or TXT/Markdown/DOCX preview, distinguishes an idea from an existing story or complete screenplay, and can immediately queue Astra text planning. Saved source remains independent of adopted production. New storyboard capability uses a pinned MIT short-drama-storyboard snapshot through the Studio JSON adapter; no global provider changes. Proposal review includes source-to-shot links and stated assumptions. Adoption reuses existing canon, shots, images, revision and Ref2VA behavior. Source and current coverage/skill provenance are exported. See STORY_IMPORT.md for the five-skill comparison, choices, operating instructions and limits.

Validation: 140 Python tests passed (two existing upstream warnings), 85 locale assertions, JavaScript syntax, Director regression checks and diff checks pass. Real browser exercised paste → Astra → proposal/expanded original excerpt → adopt → existing shot board, plus DOCX upload and exact Chinese/Cantonese extraction in an unsaved draft. Limits: 2 MB upload, 20,000 characters, 200 nonempty lines, 40 shots; DOCX main body/table text only, no PDF/OCR or whole-novel adaptation. Structural coverage cannot prove semantic completeness. Image-reference counts are checked against the current Astra five-input limit before adoption.

Live demo `edb7da2991cb4a6c` **導入示範｜雨傘**, revision 1: two scenes, four shots, 28 seconds; six source units covered, original screenplay and both Cantonese utterances preserved exactly. Astra job `9d22ea56b62b4a05` succeeded after one text correction: the first candidate omitted an offscreen prop relationship and redundantly required two locations in exterior shots. Added a bounded schema/source/reference correction path using saved Codex output; this saved candidate was resumed once through that path. Original and correction requests/results remain separate in data/jobs/. Human-readable geography and plot were retained; every shot now needs five canonical references. Parent reviewed reveal order, right-hand umbrella transfer, opening before looking back, and the final closed blue umbrella/nod. Browser adoption and shot board confirmed. No live image/video generation or image approval occurred.

`data/acceptance/story-import/verification.json` is PASS; project-state.json, exact source.txt, storyboard-coverage.md and production-package.zip saved there. ZIP contains two Scene globals/four Shot texts as existing draft handoff output, not Astra-refined H3 or rendered video. Reference images remain to be produced and approved through the existing flow. Existing The Smallest Fix production/revision/assets/H3/delivery compared equal before and after; SQLite integrity ok. Pre-deployment backup: data/backups/story-import-20260908-210714/.

Local Qwen extraction task could not start because GPU was busy; no worker edits or independent checks occurred. Parent implemented and verified the parser, without touching GPU services. Studio service restarted for deployment with no active Studio generation; during the separately resumed text correction, a delayed startup recovery marker was cleared after confirming its process remained active. The correction completed successfully; no unfinished acceptance job remains.

## Research follow-up — cut-aware segment guidance (not implemented)

See [SHOT_GUIDANCE_RESEARCH.md](SHOT_GUIDANCE_RESEARCH.md). Read-only inspection of the live ComfyUI Director and Studio production confirms that hard cuts should not automatically inherit the previous segment's AV context. The installed Director tooltip explicitly says to disable reference-previous for shot changes. The Smallest Fix specifies straight cuts at shot_01→shot_02 and shot_02→shot_03, but Studio currently defaults both incoming guides on. Recommended correction: retain shared identity/state references and enable Motion Context only for intentional continuous extension. This is a researched product recommendation, not a deployed change. Existing generation, ComfyUI workflow, canon and delivery settings were not changed; no A/B render was run.

**MVP accepted.** Continuity Studio is running at http://127.0.0.1:4760 via the enabled user service `continuity-studio.service`. Full objective is in USER_BRIEF.md; design rationale in DECISIONS.md and source research in RESEARCH.md.

## Implemented and exercised

Transactional SQLite projects, immutable revisions, canonical identities, scene/shot/keyframe relationships, queued jobs, reference provenance, approvals/rejections, targeted image revisions, stale dependency propagation, revision conflict handling and result recovery. Astra (`gpt-6-astra`) through authenticated Codex is the default for narrative, actual image rendering, vision review, H3 refinement and QC. Configurable HTTP/manual providers and a versioned instruction-skill registry are available. Browser workflows cover production writing, canonical assets, shots, review, H3, history and extensions. ZIP exports contain actual images, exact image prompts, screenplay, project state and ordered H3 references.

## Representative production

`91b816c3ac9549a2` — **The Smallest Fix**, revision 1. Astra-developed 25-second lighthouse comedy: Ada, Pip, one workshop, one lantern, three shots. All four canonical references and all five planned keyframes have actual approved images and real vision-review records. The same four canonical references were reused across every shot. All three H3 outputs were refined by Astra: I2VA for shot 01; FL2VA for shots 02 and 03. No active or pending manual jobs remain at acceptance.

Specific visual defects were corrected through the app. Ada’s puppet proportions/patterned coat and minor curved-bob rendition variation were explicitly accepted; small model variation is not a guarantee of identical pixels. Rejected alternatives and failed jobs remain as evidence. See ACCEPTANCE.md for defects, corrections and limitations.

## Verification and checkpoints

- Full suite: 74 passed after Ref2VA implementation; subsequent six focused Ref2VA tests passed after marker normalization and incoming-transition fixes. Two upstream test-client deprecation warnings.
- Live acceptance verifier PASS: 9 approved assets, 5 distinct frame images, all planned frames present, current canonical references reused, real review records, H3 field/dialogue/anchor checks and exported file membership.
- `data/acceptance/acceptance.json`: machine-readable report.
- `data/acceptance/production-package.zip`: portable production package (regenerated after asset reorganization).
- `data/acceptance/project-state.json`: complete API snapshot at acceptance.
- `data/acceptance/studio-backup.sqlite3`: online database backup; SQLite integrity check passed. Assets/jobs/skills remain in data/. Back up the whole data directory for editable-workspace recovery.
- Live production and assets survived a service restart unchanged. Clean-storage browser test created/reloaded a separate project and switched narrative to manual. Local HTTP transport tests cover text, rendering and multipart edits; external paid API accounts were not configured.
- performance-notes 1.0.0 installed, enabled and invoked through Astra; a new capability appeared without changing defaults. Shell/image tools disabled for non-image creative jobs; live extension invocation passed with those settings.

## Continue safely

Use `./run.sh` for a manual launch or `systemctl --user status continuity-studio`. Do not start another server against the same data directory. Service file: `/home/navievroom/.config/systemd/user/continuity-studio.service`. Run `.venv/bin/python -m pytest -q` after relevant changes. Re-run `.venv/bin/python scripts/verify_project.py 91b816c3ac9549a2 --output data/acceptance` to inspect the real saved production without generating or approving anything.

Preserve data/ and the service. Do not regenerate paid outputs blindly; recover a saved result first when available. Do not modify sibling projects or GPU services. Future work must retain Astra defaults and explicit provider selection. Bounded Qwen delegation was attempted for skills, reached its iteration cap and was repaired/verified by the parent; no unverified worker completion is claimed.

## Scope limits and next opportunities

Single-user local MVP, not a cloud service. No H3 video render, audio synthesis, NLE assembly, multiplayer, video/audio reference attachments or arbitrary executable plugins. Astra renders accept up to five image attachments. Follow-up priorities are richer asset variants, reference-slot planning for larger casts, and optional video-provider integration. None is required for the accepted small-project workflow.

## Follow-up — production folder access

Added top-bar Production folder: file list, image/text preview, download, copy path and native Open in Files. Real versioned output snapshots are stored under data/productions/PROJECT_ID/SNAPSHOT/. Unchanged state reuses the snapshot; user-added files are never overwritten and previous snapshots remain. Snapshot edits do not mutate canon. Pre-plan ideas are supported. File routes reject traversal and symlink escapes.

Validation: 21 relevant production/folder tests passed, JavaScript syntax check passed, browser screenplay/image preview exercised, and live desktop opener returned success (HTTP 200). Service restarted with no active generation jobs. A bounded Qwen delegation was rejected before execution by its template-marker validator; parent implemented and verified the change directly.

### Browser cache fix

A real Chrome tab retained the older JavaScript without Production folder. Root HTML now fingerprints JavaScript/CSS URLs from their content, and HTML/static responses require cache revalidation. Five folder/cache tests pass. Reloaded the affected Chrome tab and confirmed Production folder plus its file-list dialog were visible.

### Direct reference transfer to ComfyUI

H3 references now display larger exact-image previews with opening/ending labels, Copy image (PNG ClipboardItem) and descriptive PNG downloads (shot/title/Picture/moment). The shot viewer and full-size asset preview also expose image transfer. Folder entries show target names alongside IDs. Browser verified all five H3 reference cards, proper asset links and download names; the live browser reported successful clipboard.write. Browser automation's separate virtual clipboard cannot verify native paste, so ComfyUI paste itself is not claimed tested. Its running workflow was left untouched. JavaScript syntax check passed.

### Reveal the original image (corrected user intent)

User explicitly wanted to locate/select the existing original image, not download or copy it. Show in folder is now the primary action on H3 reference cards, the shot viewer and full-size previews. POST /api/assets/ASSET_ID/reveal resolves the validated original asset path and invokes org.freedesktop.FileManager1.ShowItems via gdbus, selecting that exact file in Files. It does not create a snapshot/copy/download. Live browser click succeeded for 17ed2bc4b6224d62.png in data/assets/91b816c3ac9549a2/. 23 related tests passed, including original-file identity/no-copy checks and failed opener handling.

### Human-readable original storage — completed

Original library: `/home/navievroom/Documents/ChatGPT/Video Production Pipline/data/assets/The Smallest Fix`. All 13 original images (9 approved, 4 rejected alternatives) migrated into Characters, Locations, Props and Scenes/S01-A quiet repair/SH01…SH03 folders. Files have readable sequential versions; every image has prompt/provenance sidecars, each folder has Versions.md, and the root has Asset library.md plus asset-index.json. New images use this layout automatically. UI: Show in folder selects the original; Production folder → Open asset library opens the named root.

Backup: data/backups/asset-library-20260908-092010-307269/ contains the pre-migration SQLite database, original-images.zip, before.json and migration-report.json. Migration PASS: all image SHA-256 values, asset IDs/statuses/reference IDs and other asset metadata unchanged; old paths are compatibility symlinks. Live acceptance PASS after migration; canon, revision, jobs and all Astra-refined H3 texts unchanged, with fresh named reference paths in exports. Full suite passed 68 tests before the final library-root UI addition; subsequent 10 relevant tests passed including the added root-opening test.

Asset folder names are assigned once and remain stable when creative titles change. Use asset_library.py for future storage changes; do not manually rename files or rewrite frozen job evidence. This is a readable output library, not an alternate source of canon.


### Ref2VA scene globals and Director chapters — completed

User clarified that chapter means a Director segment / prompt group. H3 delivery now defaults to Ref2VA: each scene supplies a shared global; each shot is a chapter with its own timed prompt and optional global override. Canonical scene reference slots remain stable across chapters; optional storyboard images are composition references. Prior frame-mode outputs remain accessible unchanged.

Master segment guidance defaults on with 22 context frames (5/22/39/56 selectable); chapters independently record reference-previous, predecessor, scene boundary and editable incoming direction. First chapter has no predecessor. Incoming transition notes now come from the previous shot's outgoing transition. These are handoff settings, not automatic ComfyUI edits or a completed video render. Different globals in the same timeline require common prompt prefixing off and each chapter's complete prompt. See REF2VA.md.

Live Astra job `fd4a79666cca461f` succeeded for scene_workshop: global 1994 characters; complete chapter prompts 5620/5762/6003 characters. Fixed summary task markers were normalized without rewriting provider prose; original provider result.json retained. Six-field order, dialogue, image labels, shared slots and 7000-character limit passed. Browser confirmed deployed scene/global/chapter editing, canonical reference slots and guidance options. Cross-scene inheritance/overrides are tested with a two-scene fixture; the actual sample has one scene.

Live acceptance PASS, production revision/canon/assets and legacy Astra H3 text unchanged. Data backed up before deployment at data/backups/ref2-20260908-173925/ and before format normalization. Acceptance ZIP/state/SQLite backup refreshed; integrity check passed. Production folder snapshot 32d01ef4fc7ff45972e8f4f9 includes 15 Ref2VA prompt/manifest files plus approved images. No image or video generation was triggered by this follow-up; only text prompt generation.

### Refresh and visible Ref2VA navigation

A stale in-app tab still showed the old shot board while Chrome already had current Ref2VA controls. Reload also reset all navigation to Production book. Navigation now writes a validated URL fragment and restores it on load; `/?view=ref2#h3` opens Ref2VA directly. The sidebar now says “H3 / Ref2VA 提示詞” and the page heading is “Ref2VA｜全域與分鏡提示詞”. Opened a visible in-app tab, exercised Production book → Ref2VA → reload, and confirmed the heading and all scene/chapter/guidance controls persisted. JavaScript syntax and diff checks passed. No backend restart or data changes needed.


### Required character four-view sheets — implemented

Character generation/import/approval now follows the user's four-panel example: front head-and-upper-torso close-up; full-body profile facing image left; full-body profile facing image right; full-body rear. Same identity, costume, proportions and anatomical asymmetries; pale backdrop, complete heads/feet, no captions. User example saved at data/guides/character-four-view.png, attached explicitly as layout-only and included in exports. Locations/props/storyboards retain their own single-image rules. Downstream shots extract one character identity, never the sheet panels or four clones.

New image review schema has a separate character_sheet result. New character approval requires character_sheet=pass even with a director override note. Legacy approved portraits remain unchanged as identity/provenance sources and are labeled needs_four_view; this code update does not generate or approve replacement images. Ada/Pip both currently need new four-view sheets. The character page has the supplied layout guide, full uncropped previews, 製作新版四視圖 / 匯入四視圖 / Show in folder. H3 also links to characters needing updates. New sheet filenames use Ada-four-view-v001.png etc., with existing version/provenance sidecars.

91 tests passed (two existing upstream deprecation warnings), including prompt conflicts, asymmetric views, layout-only input roles, non-character behavior, required review/approval, legacy preservation and version/export rules. Browser verified the actual Chrome character page, saved example, and generation dialog. No live image generation was triggered by the implementation checks. A subsequent app action started Ada job f1f1d2387a994e4f with the new layout, still running at the code checkpoint. Original data backed up at data/backups/character-four-view-20260908-175912/. Service restarted with no queued/running Studio jobs; ComfyUI untouched.

A bounded Qwen prompt/test task reached its iteration limit without independent acceptance. Parent inspected the focused diff, fixed a reference-role instruction that contradicted character-sheet generation, and completed integration plus the full test suite. See CHARACTER_SHEETS.md for operation and legacy transition details.

Live post-deployment comparison confirmed all pre-existing asset records, production and legacy H3 text unchanged. The served layout example matches the user attachment byte-for-byte. Acceptance export includes the requirements and guide and explicitly reports Ada/Pip as needs_four_view until replacement sheets are reviewed and approved. Character cards display pending sheet candidates ahead of the legacy approved preview so the new result is visible without hiding approval status.

Live completion: Ada job f1f1d2387a994e4f succeeded; asset 9ca22d17cccd4353 saved at assets/The Smallest Fix/Characters/Ada/Ada-four-view-v001.png. Actual four-panel image inspected by parent. Astra vision job ee75b92e6ccb4f2d succeeded with verdict=pass and character_sheet=pass. Correct left-to-right close-up/left profile/right profile/rear, full heads/feet and retained puppet/costume design. Asset remains pending for the director's approval; the old approved identity and dependent storyboards have not been replaced. Pip still needs a sheet.


### Scene → Shot terminology corrected

User clarified the intended hierarchy and supplied replacement text verbatim. Ref2VA now shows that exact Scene/Shot description; cards say SHOT and Edit Shot Prompt. Shot editing explains supplementation through the independent director/storyboard prompt while inheriting Scene settings, and an explicit option to override the effective global. Guidance/reference/copy labels, current guide, README, handoff prose and future Astra instructions use Shot. Existing serialized chapters keys and file paths remain compatible; no additional Chapter hierarchy is presented and no saved prompt or production data was migrated. Browser verified both exact sentences and the Shot editor. JavaScript syntax/diff checks and six existing delivery tests passed.

## Traditional Chinese interface follow-up

The default interface is now Traditional Chinese (`zh-Hant`, dates `zh-HK`): navigation, forms, reference assets, shots, review, Ref2VA, folders, settings and activity. A display-only locale module translates known states, capabilities and errors without renaming persisted IDs or rewriting creative text. See INTERFACE_LANGUAGE.md. In-app Ref2VA and the user's Chrome shot-board tab were reloaded and confirmed in Chinese. No service restart was needed.

Verification: 85 locale assertions, JavaScript syntax, six relevant folder/cache tests and live HTTP/cache checks passed. Browser exercised Ref2VA and its Shot editor, settings, character references, review, shots, the production-folder dialog and the new-production form. The live production, revision and asset records match the snapshot taken before localization. Existing stale frames and Pip's pending four-view candidate remain as they were.

## Human acceptance of character references

The review room now offers 人工確認採用 for character candidates without a passing four-view check. An explicit acknowledgement plus a nonblank reason is required; the saved note/event records human acceptance and the original review verdict remains intact. The regular approval path still requires a passing sheet check. Stale canon/reference protections and replacement invalidation remain effective. Character cards distinguish human acceptance from a passed four-view review.

24 character-policy and production tests passed (two upstream deprecation warnings), plus JS syntax/diff checks. Tests exercise the API acknowledgement, missing/blank reason rejection, retained review/event, superseded references and stale dependent frames, and refusal to bypass stale canon/input references. Studio restarted after verifying no queued/running jobs. The user's Chrome review tab was refreshed; Pip's enabled button and confirmation form were exercised, then cancelled. No live image was approved or generated by this change.

### No typing required for human acceptance

User rejected the mandatory acceptance-reason field. The sheet confirmation now has no text input; clicking 確認採用此圖 sends explicit acknowledgement and the server automatically saves a neutral human-acceptance record. Optional API notes remain supported. Review verdicts and stale-reference protections remain unchanged. Nine character-policy tests pass, covering omitted/whitespace/custom notes and required acknowledgement. JS syntax/diff checks pass. Restarted Studio with no active jobs and verified the user's Chrome dialog contains only cancel/confirm controls, with no required field. No live approval was submitted.

## Copy original image path

Added 複製圖片路徑 beside image controls in Ref2VA reference slots, shot frames, image lightboxes and character/location cards. A read-only asset path endpoint uses the same validated, resolved original file path as image serving/reveal. Clipboard text is the absolute filesystem path, preserving spaces and Unicode. Clipboard denial shows a selected readonly path for manual copying. No export snapshot, new image, desktop opener or approval is involved.

Eleven asset-library/folder tests pass, including the original resolved path, Unicode/spaces, unchanged asset records/pixels, missing files and symlink escape rejection. JS syntax/diff checks pass. Restarted Studio with no active jobs. Live in-app Ada button returned the successful copy toast with Characters/Ada/Ada-four-view-v001.png; Chrome was refreshed as well.

## Scene-only global prompts — final hierarchy correction

User clarified that only Scene owns a global Prompt; Shot has its own director/storyboard Prompt and no global override. Updated UI, edit schema, compiler, provider instructions, exports and current guide. Shot global copy/override controls and per-Shot global.txt exports removed. Combined text is always Scene global plus Shot prompt. Optional composition references are defined in a shared Scene reference list, keeping slots/global identical across Shots. Legacy global overrides stay in raw settings/history but are ignored in active configuration. Prompt compilation scope is included in freshness hashing so old compiled semantics cannot be silently reused.

Full suite: 98 tests pass with two upstream warnings. Coverage includes many Shots sharing one Scene global, independent Shot edits, cross-Scene guidance, API rejection of Shot globals, read-only legacy compatibility, shared optional reference slots and Scene-only global export. JS syntax/diff checks pass. Live API and ZIP verified one global per Scene and unchanged production/revision/assets. Both existing browsers refreshed; in-app page and Shot editor inspected, with no Shot global fields or controls. No live creative edit, approval or generation submitted. Studio restarted with no active jobs.

## Direct ComfyUI library transfer

After the user's GTK file picker returned Operation not supported for the suggested path shortcut, added 送到 ComfyUI 素材庫 to Studio image controls. It uploads original bytes to the existing local http://127.0.0.1:8188/upload/image API (input, overwrite=false), using a project-specific Continuity Studio subfolder and asset-specific filenames. Existing image picker is used afterward: 选已有, 缩略图, 使用所选文件. No changes to ComfyUI source, nodes, graph structure or prompts; no render submitted. Transport failures return Chinese errors and no success dialog.

Nine bridge/asset-library tests pass, plus JS syntax/diff checks. Live transfer of Ada, Pip, workshop and lantern succeeded. SHA-256 of ComfyUI /view bytes matches each Studio original. All four appeared at the top of the actual Director list_input_media response and visible existing-file picker. Studio UI button re-sent Ada successfully with the same server filename, exercising normal ComfyUI deduplication. Both Studio tabs refreshed. The user was actively editing ComfyUI; inspection selected no image or confirmation there. Studio restarted after confirming no active jobs; ComfyUI/GPU services were untouched.


## Ref2VA Director handoff page rebuilt

Replaced the all-scenes prompt document with a Scene selector and a sequential shared-parameters/Shot guide. Each step names the exact Director destination, exposes a bounded readonly prompt with one-click copy, and shows Shot duration/reference-previous settings. Scene alone owns the global prompt. Browser-only selection/copy receipts survive reload, are scoped by project and Scene, and match exact text so changed prompts are no longer marked copied. Clipboard denial selects the prompt for manual copying. Existing generation/edit controls remain available.

Scene references now support one sequential batch through the existing local ComfyUI bridge, per-image success/failure, and retry of only failed images. The UI explicitly distinguishes library upload from selecting Director image slots and copying from pasting. No ComfyUI workflow, provider, creative prompt, approval or guidance value was edited. Production/revision/assets/delivery matched before/after live API snapshots.

Validation: node scripts/check_director_page.mjs passes state persistence, project/Scene isolation, edited-text receipt invalidation, escaping and partial-transfer retry; 85 locale checks and JS syntax pass. Twelve delivery/Comfy bridge Python tests pass (two upstream deprecation warnings). In-app browser exercised shared/Shot copy, next-step navigation and reload persistence; actual Ctrl+V pasted all 3072 characters of the Scene prompt exactly into a temporary editor, then cancelled. Four-image batch displayed 4/4 success. Narrow 400px and desktop 1440px layouts visually checked. No service restart needed; Chrome disconnected during verification, so its existing tab could not be refreshed.


## Shot-aware reference-previous decisions implemented

Supersedes the earlier research-only/default-on behavior. New h3_guidance capability uses Astra to compare adjacent shot design and effective Scene/Shot prompts and returns per-shot start/cut/extend/uncertain, Traditional Chinese reason and source excerpts. Validators reject missing/duplicate/out-of-order shots, wrong previous-shot links and fabricated evidence. Results are matched to current policy/production/prompt/reference/notes hashes; stale results remain history, never current recommendations. Handoff automatically requests missing analysis, shows unknown while pending/uncertain/failed, and supports explicit retry and manual override. Legacy blanket guide booleans no longer count as overrides. Export includes decision/reason/evidence.

Live Astra job 81cebb2f00ef4cb6 succeeded: shot_01=start, shot_02=cut (50mm medium to 65mm tighter view), shot_03=cut (return to 50mm medium with a new push). API shows all three continuityFromPrev=false, with specific explanations and exact storyboard excerpts. Public-parameters guide says the master is unnecessary for this production. In-app Shot 2 displays 不勾選, Astra reason and expandable evidence; both existing Studio browser tabs reloaded. No production/creative prompt/approval or ComfyUI workflow was modified and no render submitted. Studio restarted only after verifying no active Studio jobs.

Validation: 107 Python tests passed (two upstream deprecation warnings), plus 85 locale assertions, JS syntax and Director state/render tests. New coverage checks exact review provenance, cross-shot links, exported decisions, legacy override handling, explicit manual/master interaction, uncertain vs off, prior-shot/prompt edit invalidation, immutable history and idempotent analysis requests. Frontend regression checks reviewed off reasons/evidence and unresolved labels. A final six-test guidance run also passed the additional empty-project/read-only-request regression.


## Explicit image generation and uploaded production inputs

User requires character/location/prop/frame images to start only after clicking Generate. Existing plan adoption remains text-only; removed the immediate bulk image action and made per-target generation forms explicit. Canon page has 上傳共用風格參考; each entity has 上傳製作參考 (style or subject/design input) and a separate 匯入完成圖片／四視圖 path. Uploaded inputs preserve original PNG/JPEG/WebP bytes with role, target, path, hash and JSON sidecar under the readable project asset library's Production References directory. Upload alone performs no generation/review/approval and leaves production/assets unchanged. Generation forms allow selecting applicable uploaded inputs; backend validates ownership, scope, duplicates, file integrity and total provider attachment budget, then supplies real image inputs with distinct style/design instructions. Job provenance and explicit retry preserve selected input IDs.

Also removed the 200-nonempty-line import rejection. A 350-line input is accepted and stored character-for-character; 20,000-character/file limits remain. Existing source traceability and storyboarding schema preserved.

Validation: 146 Python tests passed (two upstream warnings), 85 locale assertions, Director rendering/state tests and JavaScript syntax checks. Five focused input-reference tests passed again after retry-provenance additions. Tests verify upload creates no jobs/assets or production mutation, correct actual image paths and roles supplied only to explicit generation requests, unselected input exclusion, original byte retrieval, wrong-project/target/duplicate/tampered file rejection, and counting the layout in the actual provider input budget. Studio restarted with no active jobs. Live Chrome canon/upload/generation forms inspected and cancelled without generating or uploading any production image; the page clearly separates saved inputs from finished assets. Existing in-progress story-import work was preserved.


## Shot reference shortcuts — live 2026-09-08

The linked reference chips on 鏡頭與分鏡 are now keyboard-accessible buttons opening the existing generation confirmation dialog for that exact canonical entity. Character targets retain four-view generation; props and locations use ordinary image generation. The dialog exposes 上傳製作參考圖; saving a reference returns to generation with feedback and prior checkbox selections preserved and new references selected. 返回生成設定 supports a no-upload return. Upload target/project are captured explicitly; clicking a chip or uploading an input does not start image production. Existing keyframe approval requirements remain.

Validation: JavaScript syntax and diff checks passed. Live browser on 下一站：長洲 confirmed all seven requested targets, opened 陳樂言 / 五公升膠水樽 / 二十八樓走廊, opened the upload form and returned with the typed draft intact, then cancelled. No live reference upload or image generation was performed in this acceptance. Static update served immediately and was verified after reload; no service restart required.


## Drag-and-drop production references — live 2026-09-08

Generation and production-reference upload dialogs now offer a drop zone, ordinary file selection, local thumbnail/filename preview and explicit 保存參考圖. Dropping into generation opens the same target’s upload confirmation and retains generation draft/selected references for return. The original File is sent through FormData only on save; no image job starts. One PNG/JPEG/WebP at a time; invalid, empty, oversized or multiple drops show a message and retain the prior selection. The whole open dialog prevents file-drop navigation. Preview URLs and drag listeners are cleaned up on replacement/close.

Verification: JS syntax/diff checks and scripts/check_reference_drop.mjs pass. Event-handler regression checks exercise dropped/picked file identity, navigation prevention, preview, invalid/multiple-file retention, initial-file handoff and cleanup. Live Chrome verified drop zones and normal navigation/return in both dialogs on 下一站：長洲; no real desktop file drag or live reference upload was performed by the agent. Static changes are live after reload; no backend restart or generated media.


## Managed production-reference originals — deployed 2026-09-08

Uploads now store byte-identical originals under the existing stable asset-library target directory (Characters/Locations/Props or Scene/Shot) in Source References; project-wide style inputs use Style References. Names use target + purpose + sequential version, preserving original_filename, ID, SHA-256, upload time and purpose/target in sidecars plus project indexes. Never overwrite old versions. Explicit legacy organization retains former files for frozen job inputs. Production ZIP now includes uploaded original bytes and their metadata, independently of canonical approval. UI shows saved/original names, original-file path copy and reveal controls.

13 focused input-reference/folder tests passed, covering deleting the external source after upload, unique versions, original-byte ZIP export and idempotent legacy organization. JS syntax/diff checks passed. Both normal jobs and voice_takes were idle before stop/backup/organization/restart; no active work interrupted. Backup: data/backups/reference-library-20260908-232805/. One live 昌叔 input migrated to Characters/昌叔/Source References/昌叔-外觀參考-v001.png; IDs/hash unchanged, old path retained, project and canonical asset rows identical. Live path endpoint/ZIP SHA-256 verified, browser shows clear saved name, original screenshot name and original-file controls. No new image/audio generation. This safe restart also loaded the previously pending general speech-direction code.


## Visible image-generation state — live 2026-09-08

Canonical asset cards and keyframe panels derive per-target state from real image jobs and matching image-review jobs. Gold bordered cards, a spinner, explicit queue/generation/review/manual/failure labels, elapsed waiting time and job-detail access replace the misleading idle appearance. Successful candidates show ready-for-review; failed/interrupted work stays actionable. No percentage or estimated completion is invented. Active targets disable duplicate generation; Shot reference shortcuts open current work status. Submission immediately displays a disabled submitting button, then incorporates the acknowledged job before polling. Elapsed labels update each second without rerendering form inputs. Existing four-second backend polling handles phase transitions.

Verification: scripts/check_generation_status.mjs covers queue/run/manual/review/completion/failure/interruption/cancellation, target isolation, ignoring obsolete failures after new assets, newest active generation precedence, elapsed clocks, escaping and non-spinning terminal state. JS syntax/diff checks pass. Real browser on 下一站：長洲 showed two running/two queued user jobs with disabled generation buttons and advancing clocks; later observed 昌叔兩個膠桶 automatically transition to image-generated/waiting-review and 長身螺絲批 to running. No test jobs or renders submitted; no service restart.


## Optional high reference preservation — live 2026-09-08

Generation dialog now includes 高度保留參考內容 (off by default). Selection adds explicit role-aware fidelity directions to the existing persisted job feedback and image prompt, so retries retain the instruction without a new backend field. Identity/design inputs preserve recognizable features/materials/colors/details; style-only and layout-only roles remain distinct and required character four-view framing remains. User edits and selected refs are preserved. Upload/return retains checkbox state. A new entity with no selected or approved content reference is prompted to select/upload one before submitting this mode. This is prompt guidance, not an exposed underlying image-model fidelity parameter or a pixel-identity guarantee.

Verified checkbox default/click/upload-return state in the live browser, then cancelled. Pure toggle checks confirm instruction addition/removal/idempotence. Built actual 昌叔 image request locally to confirm feedback and selected image references reach image_prompt without enqueueing. JS syntax/diff checks passed. Static changes are live without restart; no render submitted.

## Collective cast / crowd references — 2026-09-08

Canon now supports `kind=crowd` separately from individual `character`. Crowd image generation uses a readable ensemble with distinct people and wardrobe, not one person's four-view turnaround; no layout example is attached. Review applies group identity/clone/wardrobe checks and returns character_sheet=not_applicable. A representative group for an indefinite crowd does not establish a universal count; Scene/Shot blocking and continuity set visible numbers, positions and scene-specific state. New planning instructions distinguish collective extras from independently featured characters. Ref2VA and keyframe instructions retain multiple identities without forcing the reference lineup into each scene. Crowd dialogue IDs and existing representative voice profiles remain valid. New crowd originals use the named Crowds asset category; existing frozen paths remain stable.

下一站：長洲 revision 2 changes only `c_neighbors.kind` from character to crowd. Names, descriptions, facts, IDs and all shots remain identical. It has no existing image to replace; no media was generated. Backup: data/backups/crowd-reference-20260908-234806/. Service restarted with both Studio and voice queues idle; live API confirms classification, and assets/jobs/voice profiles/takes compare identical to backup. 48 focused Python tests passed, including generation/review/approval, collective H3/keyframe interpretation, dialogue/voice compatibility and targeted dependency invalidation.

Crowd UI completion: live-browser verified group card, generation modal, upload-return high-fidelity preservation and identity editor's selected crowd type. Individual four-view controls remain. New material type selector allows explicit character/crowd reclassification through a normal production revision. Group previews use the full reference-sheet display; existing crowd representative voice remains visible in postproduction. JS syntax, 85 locale checks, reference-drop and generation-status regressions pass. No media or extra production revision was created by UI acceptance. See ACCEPTANCE.md for orphan-worker recovery and browser evidence.

## Inline Scene / Shot prompt editing — 2026-09-09

Ref2VA textareas are directly editable with 儲存提示詞 / 放棄修改 and explicit dirty status. Session-scoped browser drafts are keyed by project/scene/shot and survive navigation/reload; only Save writes versioned delivery settings. Save fetches fresh state, rejects changed source production or the same prompt edited elsewhere, preserves other effective Scene/Shot prompts/settings, and retains the draft on failure. Copy asks the user to save dirty text first. Saved manual prompts have `prompt_edited=true` and are preserved verbatim in delivery/export; deterministic speech/composition augmentation remains for generated/default/older prompts, while validation issues continue to be reported. Detailed modal editors mark their explicit prompt edits too.

19 focused Python tests and the new prompt-editor JS checks passed, plus existing Director handoff checks and JS syntax. Isolated browser acceptance on port 4761 exercised Scene Save, Shot draft/reload/Save, and discard. API and ZIP contain the exact saved strings. Clipboard comparison could not be established in the hidden IAB test, so it is not claimed as verified. Formal 4760 page visibly has editable fields and Save/Discard. No live creative text was changed; backend deployed with both queues idle after backup data/backups/prompt-editor-20260909-000202/. Test server/tab stopped. No media was generated for acceptance.

## English Scene/Shot prompts and explicit action ownership — deployed 2026-09-09

Original-name correction: characters/crowds/voice identities use exact canonical original names rather than translations/romanizations. Existing prepared results are adapted on read by stable entity ID, including known possessive name variants outside dialogue; raw jobs stay unchanged. Canonical name exceptions are passed to preparation, Ref2VA and legacy H3 language validation. Descriptions remain English and dialogue remains exact. New preparation/refinement instructions explicitly preserve names; name_en remains a compatibility field. 30 preparation/delivery/provider/asset-role tests passed, covering original-name definitions, missing-image fallback, legacy H3, strict language exceptions and unchanged dialogue/raw output.

Follow-up global audit: the user's pasted global was the historical raw draft; live configuration has no saved overrides, and all 7 prepared globals already omit that biography/approval/editorial text. Removed the remaining raw global_draft concatenation of canon description/facts, asset approval note, project style and Scene plot summary, including English sources that previously bypassed the script check. Unprepared drafts now expose only image-role mappings and pending Scene direction. Strengthened Astra preparation/refinement instructions to distinguish visible identity from hidden inventories, biography, reaction beats, provenance and changing prop state. Source canon/review evidence is retained. 23 preparation/delivery/crowd tests pass; live 7-global API/ZIP equality and English checks pass. IAB Scene 01 public-parameter textbox verified directly. Deployed after both text/image and voice queues were idle; no production or prompt result changes were needed for this follow-up.

Added Astra `h3_prepare` text jobs to produce scoped English directions before image approval. Scene globals contain reusable local appearance/light and approved reference definitions; Shots contain their own actions, camera, timing and sound. Whole production style/plot/adaptation notes are no longer injected into Shot defaults. Exact original dialogue and language tags survive preparation; actor/object ownership is explicit. Pending preparation is visible and copy is gated. Reference slots resolve when assets are approved. Saved manual edits remain editable and authoritative; invalid language is reported, not silently rewritten. Legacy H3 exports also consume prepared English direction. Silent mouth-only dialogue keeps silent articulation instructions.

下一站：長洲 revision 3 clarifies only Shot 01 action and its last two beats: Leyan checks his own phone's remaining 17% battery; Chang calls him. Original dialogue is unchanged. All 7 Scene globals and 29 Shot directions were prepared, reviewed and accepted; unrelated phone actions introduced into other scenes by an overbroad initial feedback instruction were corrected before acceptance. Raw provider outputs remain in job folders; reviewed-result.json and prompt_review events record corrections. Backup: data/backups/english-prompts-20260909-001428/studio.sqlite3. Asset rows and all voice profiles/takes/line selections match this backup exactly.

49 focused Python tests pass across preparation, delivery, speech, production, postproduction and providers; Director handoff and prompt-editor JavaScript regressions pass. Live API and ZIP verification covers all 7 globals/29 Shot prompts, English outside exact source dialogue, removal of whole-plan style, and exact export equality. Real browser after reload shows the new first Shot and editable Save/Discard controls. Remaining Scene issues concern unapproved references, not English preparation. Guidance analysis f76a75376cec48e1 succeeded against the finished directions. Final backend restart checked both jobs and voice queues idle in the same guarded process. An earlier deployment attempt interrupted one automatically started text-only guidance job because a shell command continued after an idle assertion failed; the fresh successful analysis supersedes it. No image/audio job was interrupted by that incident.

Original-name deployment acceptance: live API and ZIP globals for all 7 Scenes use original canonical names, including pending mappings, with no Chan Leyan / Chan Lok-yin / Uncle Chang substitutions. Existing prepared dialogue remains unchanged. Safe service restart completed after current preparation and guidance jobs finished; no active task was interrupted.

## 2026-09-09 — H3 one-click implementation

影片準備 now has per-Shot generation settings, explicit one-click submission, real ComfyUI queue/history state, recovery without resubmission, MP4 playback/download and current-source take adoption/export. New persistent video tables and immutable input/provenance folders preserve image canon. Active/uncertain video work blocks project deletion. Correct independent I2VA/FL2VA/Ref2VA graphs validated against runtime node schema; continuation-required Ref2VA remains a Director handoff. Plugin and archived workflow unchanged; no video generation initiated in acceptance. Full suite 337 passed before final integration checks. Deployment and final evidence: data/acceptance/h3-one-click/verification.json; usage/limits: H3_ONE_CLICK.md.

H3 one-click deployed after image work finished. Final focused checks: 32 passed. Live browser verifies approved first frame and pending prompt-adoption gate plus expanded settings. Existing production, assets, settings, old jobs and old events were preserved; subsequent concurrent demo deletion/new text-prompt job are recorded separately in evidence. Comfy PID 6610 unchanged, video queue/takes empty.

## 2026-09-09 — Expandable H3 video settings

影片設定 now exposes requested default Turbo8/8 steps, Kitchen, 16:9/0.4 MP, generated audio, no refine/scale/other LoRA. Users can choose LoRAs and strengths, edit preset steps, aspect/MP, silent output, attention, second pass with its own settings, and scale/model. Stored per browser/project/Shot/mode, frozen per generation. Real graph branches and output validation follow selections; unavailable selected nodes/models and wrong-family LoRAs fail before submission. 383 full tests and settings/workflow JS checks pass; actual runtime schema validates 12 configuration combinations. No media generated. See H3_ONE_CLICK.md and data/acceptance/h3-settings/verification.json for deployment and browser evidence.

Follow-up: 第 4 區顯示灰色不可修改的 canonical Shot 時長及 24 fps，外加後端計算的對齊幀數／預計時長。Generation API rejects duration/frame-rate overrides and tests trace the exact Shot duration through frozen input and graph.

Settings and locked duration deployed after all Studio queues idle. Backup data/backups/h3-settings-20260909-225842; every durable table compares identical immediately after restart, integrity ok. All 29 Shot durations match canonical source. Live browser: grey fixed10s/24fps,243frames/10.125s,collapsed8-step defaults. Comfy PID6610 and plugin/original workflow unchanged; queue empty.

## 2026-09-09 — Visible creative LoRAs and sourced strengths

Moved other LoRAs immediately below accelerator/steps with readable Motion/Realism labels, selected full filename and dynamically added slots (up to four). Camera Motion was already present in runtime catalog; previous placement/name obscured it. Choosing a different LoRA now initializes its own sourced strength instead of inheriting another LoRA's manual value; same selection/reload preserve overrides. Camera Motion author range0.8–1.0 uses default1.0; Realism People author default1.0. Show author links/trigger reminders; never rewrite saved prompts on selection. Unknown author strength is labeled unverified, fallback1.0. API fills omitted strengths from same registry. Evidence: data/acceptance/h3-lora-strength/.

LoRA visibility/strength update deployed with all production queues idle; 59 focused Python tests and JS checks passed. Every durable table unchanged across restart. Live dropdown shows Motion/Realism immediately below acceleration; no media or selection mutation. Backup and evidence: data/acceptance/h3-lora-strength/verification.json.

## 2026-09-09 — Inline LoRA purpose descriptions

Selected acceleration and creative LoRAs now show a short Traditional Chinese purpose directly beneath their selector. Camera Motion describes camera control, Realism People describes human detail, Turbo describes reduced-step generation and model family. Source-backed catalog text only; unknown adapters explicitly have no verified description. Existing strengths/triggers, defaults and generation settings unchanged. Existing 40 Python and two JS checks passed. Evidence: data/acceptance/h3-lora-descriptions/verification.json.

LoRA descriptions deployed after idle backup; all durable tables unchanged. Live API exposes descriptions for all nine installed H3 choices. Browser verified placement and Motion→Realism text updates in isolated data. No media generated.

## 2026-09-09 — Style LoRA excludes acceleration

User clarified the numbered LoRA slots are Style LoRA. Renamed heading/slots and exclude every Turbo weight, including unclassified-family V4, while retaining Motion/Realism descriptions and strengths. Catalog purpose is independent of family eligibility. Backend rejects Turbo in other_loras. Browser removes obsolete Turbo entries from those slots with visible notice, preserving the separate accelerator and non-Turbo choices; frozen historical jobs unchanged.

Style LoRA correction deployed after idle backup; durable tables unchanged. 61 focused Python tests and JS checks passed. Live dropdown verified: none, Camera Motion, Realism People; acceleration remains separate. Evidence: data/acceptance/h3-style-lora/verification.json. No media generation.

## 2026-09-09 — Real location, crowd and prop samples

Generated examples from 下一站：長洲 canon using the deployed managed local adapter: 二十八樓走廊 (Krea2 Turbo, then Klein reference edit), 屋苑倖存街坊群 (Krea2 Turbo, eight-person representative ensemble), 五公升膠水樽 (Krea2 Turbo). All three categories produce visible images without black outputs. Crowd count differs from the sample prompt's seven, and age distribution skews older; no canonical count was fixed. The initial corridor and Krea2 Muse edit retained forbidden lighting; explicit Klein reference editing removed the corridor lamp/indicator illumination, but distant lit windows remain. These are user-review examples, not approved canon or a claim of perfect constraint adherence. No production DB/asset changes. Evidence, exact prompts/plans/runtime receipts and outputs: data/acceptance/non-character-samples-20260909/verification.json.

## 2026-09-09 — H3 model selection

Added model selector above acceleration LoRA in expandable video settings. Existing mode-specific INT8 defaults remain; installed FL2VA FP8 scaled is selectable. Backend validates model family and actual UNETLoader availability and freezes the choice for both passes. LoRA/steps and locked Shot duration/fps remain intact. 66 focused Python tests and both video JS checks passed; deployed after idle backup with all durable tables unchanged. No plugin update or media generation. Evidence: data/acceptance/h3-model-choice/verification.json.

Live browser verified the model dropdown and FP8 selection with unchanged accelerator/steps; persisted setting inspected and restored to INT8. During reload, project-detail fetch was intermittently slow and one reload failed; health remained available. This broader loading issue is not resolved by the model-selector change.

## 2026-09-09 — Ref2V permits FL2VA base weights

User corrected the prior hard restriction: Ref2V may select a FL2VA base while preserving reference conditioning and shared/Shot prompts. Studio now permits this explicit combination; original Ref2VA default remains. LoRA eligibility follows selected base weights, not the Shot conditioning mode. Changing the base preserves settings, while changing accelerator brings its suggested steps. Official docs describe separate default variants; installed Director conditioning is selected by task and does not gate the checkpoint name. This change verifies graph wiring, not generated reference fidelity. No plugin update or media generation.

Correction deployed after idle backup; durable tables unchanged. 81 relevant tests (including owned-workflow checks) and both JS checks passed. Evidence: data/acceptance/h3-ref-fl-base/verification.json.

## 2026-09-09 — Show Style LoRA 2 immediately

Both Style LoRA 1 and 2 are now visible in expanded video settings before any selection; defaults remain none. Existing ordered multi-LoRA graph, per-adapter strength, description and per-Shot storage are reused. 43 relevant Python tests, both JS scripts and two-selection/independent-strength checks passed. Static file served live without restart or DB changes. No generation. Evidence: data/acceptance/h3-two-style-loras/verification.json.

## 2026-09-09 — Lock first-pass steps to accelerator

User superseded the earlier manual-override preference: 4-step Turbo means 4 first-pass steps, 8-step means 8, none means 20. Disabled grey field and backend normalization enforce this even with legacy manual values. Previously, the field waited for full project refresh; it now updates synchronously with the summary, with change-event fallback and input/change deduplication. Refine steps remain independent. Trigger-word assembly remains proposed, not implemented.

Deployed after idle backup; durable tables unchanged. 68 focused Python tests, JS settings/event checks and isolated browser verification passed. Browser confirmed disabled 8→4 field and summary without waiting for refresh. No generation. Evidence: data/acceptance/h3-locked-steps/verification.json.

## 2026-09-09 — Readable LoRA controls and save guidance

Style LoRA cards now show concise names, separate strength advice, purpose and a clear manual trigger action; filenames and author links are collapsed. No automatic trigger assembly yet. Video section explains automatic Studio storage, separate MP4 download and per-Shot adoption; completed results show saved status. Isolated browser layout and JS checks passed, static assets served live; no backend/DB changes, restart or media generation. Evidence: data/acceptance/h3-lora-readable/verification.json.

## 2026-09-09 — Automatically apply LoRA trigger words

User explicitly requires automatic activation after LoRA selection. Added a shared backend prompt assembler for preview and job creation. Preserve canonical source/hash; freeze prompt_assembly with exact outgoing text/common prompt and LoRA strengths/trigger sources. Worker uses frozen assembly for both graph validations/submission; old jobs retain their original source. Prefix known triggers before Shot text, or Ref2V common text when present; avoid duplicate leading triggers and never rewrite dialogue/body. Unknown trigger metadata stays unverified. UI shows automatic status and provides an escaped preview, with local settings rerender on selection instead of waiting for the slow project API. No generation or plugin update.

Auto triggers deployed after idle backup; durable tables unchanged. 64 focused Python tests, both JS checks and isolated browser select/add/remove verification passed. Preview/job/graph equality verified with a mock provider; no real generation. Evidence: data/acceptance/h3-auto-triggers/verification.json.

## 2026-09-10 — Prompt-based Style LoRA recommendations

Added h3_lora_advice as a routed creative text capability using the existing durable job/provider/mandatory-method pipeline. Evaluate full saved Shot/common prompts against installed Style candidates for the selected base; require one decision per candidate, exact quoted evidence and allow none. No keyword heuristic or provider fallback. UI displays reasons/evidence and only changes selections on explicit apply. Apply revalidates source, model and current catalog, uses preset strengths, preserves independent settings and automatic triggers. Jobs persist across reload; stale/failed/pending advice cannot apply. Recommendation and media generation are separate actions.

Deployed after idle backup; all durable tables unchanged. 52 focused Python tests and three JS checks passed. Actual Astra tests correctly reject negated motion/nonrealistic prompts and recommend both adapters for explicit photorealistic human dolly/orbit; evidence is quoted exactly. Browser verified actual result layout. No image/video generation. Evidence: data/acceptance/h3-lora-advice/verification.json.

## 2026-09-10 — Style LoRA fields on demand

User superseded the earlier always-visible/collapsed slot layout. No selected Style LoRAs means no fields. Applying advice shows exactly the selected count; the button below recommendations adds one empty manual choice without requiring recommendation or changing generation settings. Selecting it keeps that card; removing an adapter compacts remaining cards and preserves their strengths. Pending empty fields are UI-only, scoped by project/Shot/mode, and cleared on apply/cancel. Existing selections remain visible on reload; maximum four retained. Trigger activation is deterministic local text assembly, with no creative-engine request.

Three existing JS checks, targeted on-demand checks, four trigger tests and isolated browser add/select/remove verification passed. Static assets match live service; no restart, production DB changes or media generation. Evidence: data/acceptance/h3-style-on-demand/verification.json.

UI backend acceleration deployed with all queues idle; every DB table row matched before/after. Live project response timings: 0.184, 0.161, 0.162 seconds. Complete 0.1 backup includes current H3/LoRA updates and UI fix at backups/Continuity-Studio-0.1-20260910-004107. Verification: data/acceptance/ui-responsiveness/verification.json.


## 2026-09-13 — One-scene editorial trial

Added the explicitly requested 發現黃太 trial using the existing Production / director / edit contracts. Studio now has a visible topbar and directing-panel link to a Traditional Chinese timed-card preview with original-versus-candidate comparison, editable planned source ranges, source reuse, independent sound cue positions, versioned save/export and explicit formal adoption through engine.save_plan. New candidates do not generate media or change approved canon.

Live 下一站：長洲 revision 4 remains unchanged. Candidate: 9 continuous source Shots, 53.5 planned screen seconds versus 5 / 54 in the original; 61 planned generation seconds. Sources split actual floor jumps and the reaction/wide setups, retaining 10-second continuous discovery and reaction holds. H3 drafts are English except original names/dialogue, version-bound and explicitly unready with no automatic T2VA execution fallback. Named-identity evidence remains uncertain.

31 editorial/timing regressions pass; 72 existing directing, speech and workflow-ownership checks also passed. Source attribution and scope/limitations: docs/EDITORIAL_PILOT.md. Backup, immutable initial trial snapshot and deployment receipts: data/acceptance/editorial-pilot-20260913/. Knowledge selection is specific to this authored pilot; general automatic planning integration and actual-video edit/render are not claimed.

Browser acceptance completed in Chrome: live pilot/playback/sound-bridge/uncertain-identity display verified. An isolated temporary-data server verified actual UI edit/save to v2 with history and original comparison; it was stopped afterward. The live topbar link is visible and the user's production revision remains 4.


## 2026-09-13 — Editorial page rollout mismatch repair

User reported `Cannot read properties of undefined (reading 'map')` when opening editorial. The new scene selector expected `projectInfo.scenes`, but the prior running backend omitted that field while deployment waited for generation. The concurrently authored compatibility guard now explains a mismatched response; the updated backend is live and returns all seven scenes. This task did not restart services or alter production. Live revision 5 and the adopted nine-shot trial remain intact.

Live Chrome acceptance verified formal 9 segments / 53.5 seconds, original comparison 5 / 54 seconds, return to formal, and actual timed preview advancing to 折返 before pause. Served editorial.js matches disk; all 40 editorial/timing Python tests passed. New scripts/check_editorial_page.mjs executes the actual frontend module and passed bootstrap, comparison, scene-open request, empty-trial and escaped-error checks. Removing the compatibility guard reproduces the original undefined.map failure with an old response; retaining it shows the Chinese compatibility message. Local Qwen reached its iteration limit; parent replaced the incomplete draft and completed verification. Evidence: data/acceptance/editorial-page-map-20260913/verification.json.

## 2026-09-13 — Reject blank generated-image output

User reported a black opening frame displayed as successful. ComfyUI originals for jobs b770b8fbe69044b0 and a9894f385d304348 are RGB 1024×1024 with all channel extrema zero; their SHA-256 matches Studio's download receipts. Comfy execution reported success without a diagnostic error. Frozen canonical references were normal; the second attempt used the first black candidate as its edit source. Both candidates were subsequently rejected/purged by concurrent user activity, not by this repair. No original media was restored or new generation submitted. Underlying sampler/VAE failure remains unconfirmed.

Added exact pixel-content checks at Comfy download and common image persistence boundaries. All-black, all-white, fully transparent, and unreadable outputs fail; low-light texture and other constant swatches remain allowed. An invalid Comfy receipt is terminal and preserves the raw output/history without reposting. Historical pending/approved generated assets receive read-only diagnostics, failed presentation and blocked approval/identity adoption/edit-source reuse. Original stored reviews and job history remain unchanged. Generated-file checks are cached by path, size and modification time.

Validation: 51 relevant Python tests and three Node suites plus app JS syntax passed. Evidence and deployment verification: data/acceptance/black-image-output-20260913/. The fix detects invalid output; it does not claim to repair the renderer's unreported internal cause.

## 2026-09-13 — Real replay of the black-frame incident

After the user challenged the remaining renderer failure, performed two real isolated ComfyUI renders through normal VRAM Manager admission. Both use b770b8fbe69044b0's exact 4,636-character prompt, seed, four ordered input bytes, original model/attention/VAE and the same Klein process (PID 2932623). First run saves diagnostic latents; second uses the original graph with only input/output paths changed. Both produce identical visible 1024×1024 pixels, and both histories show no cached nodes. All four reference latents and the sampled latent in the instrumented run have no NaN/Inf. No GPU service restart, model change, production asset creation or adoption occurred. GPU contention was respected; diagnostic admission waited for existing Qwen work.

This establishes current rendering recovery, not a durable root-cause fix: the original all-black failure did not reproduce. The resulting image still depicts the lantern lit and is not an approved opening frame. Exact graph/input comparison, latent statistics, logs and rendered images: data/acceptance/black-image-output-20260913/replay-verification.json, replay-baseline/, replay-original/. Current production code is unchanged by these experiments.
