# H3 production craft

Use the supplied selected mode and adopted Scene/Shot direction. Legacy Ref2VA keeps Scene-global reusable identities separate from individual Shot action, props, dialogue and timing. For prompt_policy ref-demand-prompt-v1, use the supplied self-contained per-source six-field contract and exact subject_definitions; global text is empty. Use only its explicit reference_demands and stable supplied Picture/Subject mapping. Never import Scene slot numbers or unrelated references. For AUTO h3_strategy, return the shared ConditioningDecision including the minimal required shot-specific reference_demands with semantic roles and exact evidence, without assigning asset IDs/slots. Ordinary composed openings favour I2VA, explicit final-state constraints FL2VA; necessary reusable identities/objects/environments that an opening insufficiently conveys or unnecessarily stages favour REF2VA. Timestamp-critical intermediate states remain unresolved; references cannot enforce them. Frame-conditioned I2VA/FL2VA uses its own supplied three-field contract; never paste Ref2VA reference numbering into it.

Preserve exact dialogue words, language tags, stable speakers and allotted time. Keep speech in the requested dialogue section and ambient/physical sound in soundscape. Do not invent narration, lyrics, prop transfers or new events to conceal mismatching frames. With attached frames, inspect the actual pixels, explain discrepancies separately in the allowed review field, and still deliver the requested complete prompt when the app's contract treats discrepancies as advisory.

Specify who acts on what, which hand holds it, where the camera starts/ends and when major beats occur. A phrase such as “cinematic camera movement” cannot replace a specific motivated movement. A continuous bridge between endpoint frames must be physically plausible, not instantaneous morphing. Distinguish an editorial cut from a continuous extension when assessing previous-video guidance.

Example: for an adopted six-second slow push toward a character holding a bottle in the right hand, preserve that timing and ownership. Do not add a left-hand transfer merely to reconcile a discrepant image, and do not lengthen the shot. Report the discrepancy in the designated notes.

## Explicit native multi-shot generation groups

When the supplied source kind is generation_group, one H3 generation contains multiple adopted storyboard views. Follow its exact numbered shot headers and cumulative cut times. This is an explicit cut sequence: do not turn CUT into a pan, a continuous bridge, morph or dissolve. One source record is not one mandatory generation. The first supplied image anchors Shot1 only; in FL2VA the last image anchors the final ShotN. Ref2VA identity/storyboard pictures establish appearance/composition, not a guarantee of cut count, timing or intermediate states. Use role labels, preserve each member's selected source interval and remapped dialogue, and never replay entire original source actions. Group duration is 4–15 seconds, internal view holds can be shorter. Studio retains the whole clip and planned mapping, then records actual user-observed cut boundaries separately. Ordinary per-source prompts still execute a continuous take.

For h3_group_plan, choose native grouping or independent sources according to readability, new visual information, continuity and need for exact cutting. Do not minimize frame count at the expense of audience understanding. Storyboard view count, fresh image count and generation request count are three independent decisions.
