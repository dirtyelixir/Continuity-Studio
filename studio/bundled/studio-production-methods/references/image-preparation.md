Prepare a concise English rendering brief for ONE still image. Return only the requested JSON; no tools and no media generation.
The source below is interpretation context, NOT a rendering prompt to concatenate. Historical style and facts can contain mixed story, sound, motion and editorial notes. Extract only visible details applicable to this target. Never paste source JSON, field bookkeeping, biography, plot summaries, adaptation commentary, director/module names, sound, music, dialogue, shot counts, timed action sequences or camera movement into the returned visual fields. Preserve the intended medium and aspect ratio; do not replace stylized art with photorealism. Preserve original canonical names and any explicitly required visible lettering.
For a reusable location show one coherent view establishing its connected layout and fixed anchors, without characters. Do not attempt to show every floor or incompatible viewpoints at once. Select lighting appropriate to the target/reference, not a montage of every time of day. Character and crowd references establish visible identity, not relationships, hidden pocket inventories, performance beats or future injuries. Prop references establish object shape/material/color, not its owner's performance or future state.
The scoped_scene_context and target's stated time take precedence over scene-specific material buried in legacy_style_context. Apply a legacy lighting condition only when BOTH its location and time match this target. A generic nighttime instruction does not apply to every dark scene: never import a later scene's firelight, smoke, damage or other event-dependent illumination into an earlier predawn reference. With no applicable scene-specific source, use only the target's established ambient illumination; do not invent an event to light the image.
For a keyframe, the specified frame description and selected moment state govern the frozen pose. Shot framing/angle/blocking are supporting context only; do not depict later actions, assume a late reaction is present at the start, or draw offscreen entities just because their continuity is tracked. Visible posture and object orientation follow natural use and source eyelines, not an unsolicited presentation to the viewer. Surface an irreconcilable source conflict instead of inventing a solution.
Write visual_style (medium/aspect/palette), appearance (target's visible identity or environment), composition (one static view/pose), lighting, and requested_changes (only the user's applicable visual revision, empty if none). Do not repeat the same instructions across fields. The application supplies target-specific layout rules and reference roles separately; do not reproduce them. All Image numbers and attachment roles are assigned by the application; do not invent or mention Image/Picture/Subject numbers in these fields. Interpret review notes only as contextual evidence, never as new design commands or approval badges.
Return target_id exactly as supplied. Put brief explanations of excluded irrelevant context in omitted_context, and only conflicts that prevent faithful rendering in conflicts. Neither list will be sent to the image renderer. Do not silently discard a requested visual change or necessary fixed detail. If the request cannot fit these bounded fields faithfully, report a conflict instead of truncating it.

Scope location facts and scoped_scene_context.appearance_and_light to the exact target sublocation as well as time. A Scene may span several floors or rooms; its shared description does not place every local mark in every frame. When the frame explicitly depicts a different sublocation, reuse compatible building architecture, describe the target's floor/room identifier, and omit another sublocation's unique scrape, damage or local prop placement. Record excluded local details in omitted_context while preserving the source facts. A different explicit floor number in a reusable building reference alone is not an irreconcilable conflict. Keep same-place fixed anchors binding; report incompatible architecture or contradictory requirements for the same physical place.
